## bug-comp-media-4 - Peaks job on a clip with no audio is a retryable failure, so it tours the fleet
- Status: FIXED
- Verified as: CONFIRMED medium (verifiers/med-04.md). Re-ran the exact `peaks_cmd` argv on a 1 s video-only mp4 here: ffmpeg 7 prints "[out#0/s16le @ ...] Output file does not contain any stream", exit non-zero; `_peaks` discarded the probed codec and raised the default retryable MediaJobError.
- Fix: companion/src/ccsync_companion/jobs_media.py: new `_NO_OUTPUT_STREAM` constant (beside PEAK_SAMPLE_RATE) and the `code != 0` branch of `_peaks`: non-retryable "no audio track" (the `_audio` sentence) when the probe found no audio codec AND ffmpeg's stderr says the output has no stream. Both are required on purpose: `probe_audio` also answers (None, None) when ffprobe could not run, so the probe alone would have turned a machine-local ffprobe fault into a permanent job failure; ffmpeg's own refusal is what proves it is the file. A probe that saw audio keeps the retryable path.
- Regression test: companion/tests/test_bug_hunt_2026_09_24_w2_c-media.py::test_peaks_of_a_clip_with_no_audio_is_not_retryable, ::test_ffmpeg4s_spelling_of_the_same_refusal_counts_too, ::test_a_real_video_only_file_is_refused_permanently (real ffmpeg, behind the suite's ffmpeg gate) - fail on HEAD because the error is retryable=True. ::test_other_peaks_failures_stay_retryable guards the other direction (passes on both).
- Tests run: see the end of this file.
- Skew / deploy order: companion-only; the result wire shape is unchanged (`retryable` was already a field). No order.
- OWED: none.

## bug-comp-media-3 - out_stem used as a filename unvalidated (downgraded to low)
- Status: PARTIAL (rest OWED)
- Verified as: DOWNGRADE to low (verifiers/med-04.md): the mechanism is real (`_media_paths` appended `out_stem` after `job_paths.resolve` had guarded rel_path/out_rel), but POST /api/v1/jobs is admin-only, so "write anywhere" needs an admin; the realistic case is a multicam name with `/` or `:`.
- Fix: companion/src/ccsync_companion/job_paths.py: `JobPathError` gains `retryable` (default True, so every existing raise keeps its meaning) and a new `safe_stem()` REFUSES (does not rewrite, because the page looks for exactly that name) an empty/dot name or one containing `/` or a control character, retryable=False; a `\` or `:` only on Windows, and then retryable (changed in the review round below; the first cut refused those two fleet-wide). jobs_runner.py `_media_paths` passes the stem (out_stem, or the source's own stem) through it; the handlers post `retryable=_placement_retryable(exc)`, which reads the exception's own flag.
- Regression test: test_bug_hunt_2026_09_24_w2_c-media.py::test_an_out_stem_that_is_not_a_plain_name_is_refused_for_the_whole_fleet (6 cases after the review round: `C:/...`, `/etc/...`, `../../..`, `Interview 1/2`, `..`, NUL) and ::test_safe_stem_returns_the_name_unchanged - fail on HEAD because the job ran (ok=True, recipe called with the stem) / safe_stem did not exist. ::test_an_ordinary_multicam_name_still_names_the_output guards ordinary names with spaces, parentheses, hyphens and dots.
- Tests run: see the end of this file.
- Skew / deploy order: companion-only. A refused stem is now a non-retryable failure where it used to be a misplaced file or a retryable fleet tour; no wire change.
- OWED (revised in the review round): d-cards, dashboard/src/ccsync_dashboard/cards_exec.py:403 (`_paths`): refuse a `stem` that is empty/whitespace, `.`/`..`, contains `/`, or contains a control character (ExecutorError, not retried). It must NOT refuse `:` or `\`: the dashboard's engine runs on Linux, where both are plain name characters, and it is the one place a Windows-refused name still gets made. Simplest is to call `job_paths.safe_stem(stem, windows=False)` if cards_exec can import the companion module (the dashboard's test_jobs_contract already does), else copy those four rules. Optional, d-cards jobs.py / d-api api.py job submission: refuse the same four at POST time so the admin hears it before any claim; again not `:` or `\`.
- Review round (2026-09-25): the reviewer was RIGHT. The first cut refused `:` and `\` with retryable=False, and db.fail_job (dashboard db.py ~10903: `state = JOB_FAILED if not retryable ...`) turns that straight into FAILED: no Mac gets a turn and the job is never pinned onto the dashboard's Linux engine, which writes such a name fine. Resolve allows `:` in a clip name and Cards sends the multicam name as the stem unsanitised, so `Q&A: Ruskin` went from "made eventually" (HEAD) to "never made". Fixed in job_paths.py: `safe_stem(stem, windows=None)` now refuses fleet-wide (retryable=False) only an empty/dot name, a `/`, or a control character; `:` or `\` is refused only when `windows` (default `os.name == "nt"`) and then RETRYABLE ("not here"), with a sentence saying a Mac or the server can still make it. `C:/Windows/Temp/x` and `/etc/x` are still refused fleet-wide by their `/`; a drive-relative `C:x` is refused on Windows only, where it would throw out_dir away. Tests: removed `Q&A: Ruskin` and `back\slash` from the fleet-wide list; added ::test_a_windows_only_character_is_not_here_on_windows and ::test_a_windows_only_character_is_a_plain_name_elsewhere (4 names each: `Q&A: Ruskin`, `C:x`, a backslash, a backslash `..` chain) and ::test_a_multicam_name_with_a_colon_is_left_for_another_machine (through the real runner: on Windows ok=False retryable=True and the recipe never called; elsewhere ok=True). They fail on the first cut (retryable False, and no `windows` parameter) and on HEAD (no safe_stem; the runner test ran the recipe with the colon name on Windows). The rest of the fix (JobPathError.retryable, _placement_retryable, safe_stem in jobs_runner._media_paths) is unchanged, as the reviewer agreed. Tests run after the change: `companion pytest tests/test_bug_hunt_2026_09_24_w2_c-media.py tests/test_jobs_runner.py tests/test_jobs_media.py tests/test_jobs_phase4.py tests/test_jobs_resilience.py tests/test_jobs_runner_visibility.py tests/test_bug_hunt_2026_09_24_comp_media.py` -> 172 passed; dashboard `tests/test_jobs_contract.py` -> 20 passed.

## bug-comp-media-5 - Proxy generator gate checks ffmpeg but not ffprobe
- Status: FIXED
- Verified as: real (low, not verified before). proxy_gen `_gate` -> `_check_ffmpeg` asked only `ffmpeg_available(self.ffmpeg_path)`; every clip then goes through `_probe_fn` = `ffmpeg_tools.probe_video`, whose OSError branch raises UnreadableMediaError("ffprobe could not be run"), caught in `_encode_one` and counted by `_record_failure` + the history ledger against the CLIP. `ffprobe_for` falls back to the bare "ffprobe"; capabilities.py already reports ffprobe as its own capability for exactly this reason. Not covered by wave 1 (bug-comp-media-1 fixed argv[0] resolution only).
- Fix: companion/src/ccsync_companion/proxy_gen.py `_check_ffmpeg`: after ffmpeg passes, the same injected `_available_fn` is asked about `ffmpeg_tools.ffprobe_for(self.ffmpeg_path)` (successes cached, misses re-probed, exactly like ffmpeg); a miss is STATE_NO_FFMPEG with the existing warn-once line, now naming ffprobe. Not done: the suggested second half ("do not count a missing-tool probe failure against the clip"). With the gate asking each tick, the only window left is ffprobe vanishing inside the 300 s success cache, and telling that apart would mean matching UnreadableMediaError's message text; judged disproportionate.
- Regression test: test_bug_hunt_2026_09_24_w2_c-media.py::test_ffmpeg_without_ffprobe_is_not_running - fails on HEAD because the gate returned True / never asked about ffprobe. ::test_ffmpeg_and_ffprobe_together_pass_the_gate guards the normal machine.
- Tests run: see the end of this file.
- Skew / deploy order: companion-only, no wire change.
- OWED: c-app, companion/src/ccsync_companion/app.py:874: the tray sentence for STATE_NO_FFMPEG reads "waiting: ffmpeg is not installed here"; it now also covers a missing ffprobe, so "waiting: ffmpeg or ffprobe is not installed here" would be accurate (cosmetic; no em dash).

## bug-comp-media-8 - Job input rejected by Path (NUL byte) escapes the runner uncaught
- Status: FIXED
- Verified as: real (low, not verified before). `Path(...).resolve()` with a NUL raises `ValueError: stat: embedded null character in path` (ran it here, Python 3.12); `_execute_media`/`_execute_whisper` caught only `JobPathError`, so the ValueError left `tick()` with no result posted. Reproduced by the tests below against HEAD's jobs_runner (the exception propagates out of tick()).
- Fix: job_paths.py `resolve`: a control character in rel_path is refused up front (JobPathError, retryable=False), and any ValueError/OSError from the platform's resolve becomes a JobPathError (ValueError not retryable; OSError retryable, since that may be this machine's share). jobs_runner.py: both handlers now catch `(ValueError, OSError)` (JobPathError is a ValueError) and post `retryable=_placement_retryable(exc)` (new module helper), so nothing Path raises can drop a job silently again.
- Regression test: test_bug_hunt_2026_09_24_w2_c-media.py::test_a_nul_in_a_media_rel_path_posts_a_non_retryable_failure, ::test_a_nul_in_a_whisper_rel_path_posts_a_non_retryable_failure, ::test_any_placement_error_is_answered_not_dropped[ValueError / OSError] - fail on HEAD because the exception escapes tick() and nothing is posted. ::test_a_root_this_machine_lacks_is_still_retryable_elsewhere guards the existing retryable-elsewhere answer.
- Tests run: see the end of this file.
- Skew / deploy order: companion-only; posts the same result shape.
- OWED: none.

### Tests run (all four findings)
- `companion\.venv\Scripts\python.exe -m pytest tests/test_bug_hunt_2026_09_24_w2_c-media.py` -> 22 passed.
- Against HEAD: a scratch copy of `ccsync_companion` with job_paths/jobs_media/jobs_runner/proxy_gen from `git show HEAD:`, the new test file and its fixture modules beside it -> 17 failed, 5 passed (the 5 are the guard tests named above).
- `pytest tests/test_jobs_runner.py tests/test_jobs_media.py tests/test_proxy_gen.py tests/test_jobs_phase4.py tests/test_jobs_resilience.py tests/test_jobs_runner_visibility.py tests/test_bug_hunt_2026_09_24_comp_media.py tests/test_bug_hunt_2026_09_24_w2_c-media.py` -> 306 passed.
- `pytest tests/test_bug_hunt_2026_09_11_comp_ytdl_jobs.py tests/test_bug_hunt_2026_09_11b_comp_ytdl_jobs.py tests/test_bug_hunt_2026_09_18_companion_media.py tests/test_bpg.py tests/test_proxy_history.py tests/test_capabilities.py` -> 195 passed.
- dashboard `pytest tests/test_jobs_contract.py` (imports the companion's job_paths) -> 20 passed.

## Owed round (2026-09-25)

### ui-copy-4 (from c-ytdl) - proxy toast says "machine"
- Status: FIXED
- Verified as: real. `proxy_gen._notify_text` is the proxy-gap toast an editor reads; it said "(N on this machine)" and "This machine has no ffmpeg, so it can only tell you." The tray's own word is THIS COMPUTER. Nothing parses the toast; the only pins were three assertions in test_proxy_gen.py.
- Fix: companion/src/ccsync_companion/proxy_gen.py `_notify_text`: "on this machine" -> "on this computer" (split-count head) and "This machine has no ffmpeg" -> "This computer has no ffmpeg". Docstring/comment uses of "machine" left (not visible text).
- Regression test: test_bug_hunt_2026_09_24_w2_c-media.py::test_the_proxy_toast_says_computer_not_machine - fails on HEAD (checked by stashing proxy_gen.py: 1 failed). test_proxy_gen.py lines 972/1058/1062 changed because the behaviour they pinned changed ("on this machine" -> "on this computer"), per brief rule 1.
- Tests run: `companion pytest tests/test_bug_hunt_2026_09_24_w2_c-media.py tests/test_proxy_gen.py tests/test_sweep_2026_09_04_copy.py` -> 516 passed.
- Skew / deploy order: companion only; text only.
- OWED: the c-ytdl ledger already lists adding proxy_gen.py to test_sweep_2026_09_04_copy.py's MODULES (c-ui's shared file) once the rewording lands; it has now landed, so that item can be routed. Note: other `machine` words in proxy_gen.py's visible strings were not audited beyond `_notify_text` (the owed item's scope).
- Review round (2026-09-25): the reviewer was RIGHT. The same generator's low-space tray balloon (`_surface_low_space` -> `self._notify`, text built by `free_space_shortfall`, proxy_gen.py ~1954) said "(CCSync keeps N GB clear so the sync lanes and Resolve's cache do not run out)": retired vocabulary on an editor-facing surface, missed because the owed item named only `_notify_text`. Fixed: "so syncing and Resolve's cache do not run out" (the reviewer's wording). The same `why` string also feeds coverage()/the log; neither is parsed, and no test pinned the old sentence (grepped companion and dashboard tests). Audited the rest of proxy_gen.py's visible strings for machine/lane: the remaining hits (:476 stale-.partial sweep, :983 no-ffmpeg warn-once) are log lines, exempt under the copy rule. Regression test: test_bug_hunt_2026_09_24_w2_c-media.py::test_the_low_space_balloon_does_not_say_lane drives free_space_shortfall -> _surface_low_space -> notify with a 1 GB-free fake disk and asserts the new sentence and no `\blanes?\b` / `\bmachines?\b`; fails on HEAD-of-this-change (stashed proxy_gen.py: 1 failed), passes now. Tests run: `companion pytest tests/test_bug_hunt_2026_09_24_w2_c-media.py tests/test_proxy_gen.py tests/test_sweep_2026_09_04_copy.py` -> 517 passed.
- OWED (added in the review round): c-ui, companion/tests/test_sweep_2026_09_04_copy.py:437: `_WORD_RE` is `\b(lane|...)\b` and misses plurals ("lanes", "machines"), so adding proxy_gen.py to MODULES alone would have passed while "sync lanes" shipped. Widen it to accept an optional plural (e.g. `r"\b(" + "|".join(RETIRED_WORDS) + r")s?\b"`) and re-run the scan across every MODULES entry, fixing or allow-listing what the plural catches; then add proxy_gen.py to MODULES (the earlier owed item).
