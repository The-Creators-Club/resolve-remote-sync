# These scripts run as root against the production NAS, so these are floors,
# not "whatever pip resolves today". Raise them deliberately; never drop them.
# Verified against: paramiko 4.0.0, requests 2.34.2, urllib3 2.7.0, pytest
# 9.1.1 (2026-07-25). The floors are the oldest releases the current code is
# written for -- paramiko 3.4 for the modern SSHClient/PKey API used by
# common.ssh_client (host-key pinning falls back to per-type key classes on
# builds without PKey.from_type_string), requests 2.31 / urllib3 2.0 for the
# CVE-2023-32681 and 1.x proxy fixes respectively.
paramiko>=3.4
requests>=2.31
urllib3>=2.0
pytest>=7.4
