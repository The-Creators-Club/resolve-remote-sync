# Bug hunt 2026-09-24 (eleventh fleet hunt: whole repo, three waves)
Whole-repo hunt at HEAD `63d4290` (dashboard 0.7.56 / companion 0.9.77),
working tree clean when it started. The owner asked for "a fleet of opus 5.5
bug hunters across all aspects of this repo, one wave looking for bugs,
another looking for logical errors, usability issues, and another wave for
UI problems". Thirty-seven Opus hunters ran at once on disjoint territories
(brief: `docs/bug-hunt-2026-09-24/HUNTER_BRIEF.md`): nineteen `bug-*` (nine
companion, six dashboard, b-roll, music/ytdl, ops, and a cross-cutting
`bug-wire` lens), ten `logic-*` organised by user journey, and eight `ui-*`
(dashboard main/admin/static, the b-roll, music and ytdl SPAs, the companion's
windows, the onboarding wizard, and a cross-surface `ui-copy` lens). Every
HIGH was then put to one adversarial Opus verifier told to refute it.
Read-only throughout; raw reports are in `docs/bug-hunt-2026-09-24/hunters/`,
the machine-readable merge in `results.json`.

**Tally: 266 raw findings (16 high, 124 medium, 126 low; 239 CONFIRMED by the hunter, 27 PLAUSIBLE).** No duplicate pass was run, so expect some overlap between a territory hunter and a journey or lens hunter.

| Wave | High | Medium | Low |
|---|---|---|---|
| Wave 1 - bugs | 11 | 55 | 46 |
| Wave 2 - logic and usability | 4 | 35 | 24 |
| Wave 3 - UI | 1 | 34 | 56 |

**Verifier pass on the 16 highs: 11 CONFIRMED as high, 5 real but DOWNGRADED, 0 refuted.** Mediums and lows were not verified; the 2026-09-18 verifiers refuted about 3% of theirs.

**STATUS (2026-09-25): the eleven confirmed highs, plus `bug-comp-core-2`, are FIXED as KNOWN_BUGS CR-322..CR-332 (companion 0.9.78 / dashboard 0.7.58), each group reviewed twice by adversarial Opus reviewers and the whole wave by Fable; CR-333 records what Fable found still open. The 124 mediums were VERIFIED (below), not fixed. Ledgers: `docs/bug-hunt-2026-09-24/ledger/`.**

Note on timing: the Timeline Cards picker redesign (cards_landing / new
cards_catalog) was written in the same session while the hunt ran, uncommitted.
The hunters read HEAD's picker; `logic-cards-5` (the admin-only footer) is
already fixed by that redesign, and `logic-cards-4` (?want= ignored) and
`logic-cards-7` (an [ OPENING ] with no timeout) still apply to it.

## The confirmed highs

### bug-comp-app-1 - A halt's paused project folders are unpaused by the next sequencer stop()/pause() (tray Quit, self-upgrade, Resolve-exit restart, tray Pause, drive unplugged)
- Where: `companion/src/ccsync_companion/app.py:6456-6461, 10705, 9245-9247, 2890-2892; sync/sequencer.py:733-758`
- What: halt_all_sync stops the sequencer and then pauses every lane C folder. Sequencer.stop() and pause() both end in _unpause_all(self._last_selection), which checks neither _is_halted() nor the halt latch. Every later stop()/pause() in the same process therefore releases every selected project's Syncthing folder while the halt is still active. A restart with a persisted halt never re-pauses them. CR-48 fixed this for the shared folders and the release path, not for project folders going through stop/pause.
- Failure scenario: A fleet halt is active. The editor quits the tray, a pushed update restarts it, the editor quits Resolve (so _maybe_recover_stale_bridge restarts the companion), the editor clicks Pause, or the SSD is unplugged. Any of these calls sequencer.stop()/pause() and unpauses every project folder. Syncthing then syncs project files both ways during the halt while the tray and the grid still say STOPPED. Probe output: pause calls during shutdown while halted: [('s-a', False), ('s-b', False)].
- Verifier: The mechanism holds. halt_all_sync (app.py:7631) first calls _stop_lanes, which calls sequencer.stop(). It then pauses every folder in halt_folder_ids via _pause_lane_c_folders(True). But Sequencer.stop() and pause() (sequencer.py:733-758) both end in an unconditional _unpause_all(self._last_selection). _last_selection is kept after a stop, and _unpause_all (1785) filters only on item validity, upload_only and unconfirmed ignores. It never checks _is_halted(), and _is_halted() is only consulted at 2190, inside the pass loop.  Every later path in the same process therefore releases the selected project folders while the halt is still active: - shutdown() calls _stop_lanes() unconditionally (around line 10705), so tray Quit and the self-upgrade hand-off both reach sequencer.stop(). - toggle_pause (9245) calls sequencer.pause() in managed mode with no halt check. - _root_pause_lanes (2890) calls sequencer.pause().  Syncthing runs as its own service (the docstring of _pause_lane_c_folders says so), so the unpaused folders really do sync while the halt is on.  On restart the halt is re-read from disk. The _start_lanes HALT gate returns before sequencer.start(), so _startup_unpause never

### bug-comp-rclone-1 - A blocked repath is finished on the next pass by pointing Syncthing at the directory that lane B or the structure clone created
- Where: `companion/src/ccsync_companion/sync/repath.py:567; companion/src/ccsync_companion/sync/sequencer.py:2045-2067`
- What: When the move fails, the folder is left paused, but _process_project still runs the structure clone and lane B for the project's NEW rel. Both of those create local_root/Projects/<new rel>, and lane B fills its Proxy dirs. On the next pass, _move_dir sees a live source and an existing destination and takes the 'conflict, re-point anyway' branch (returns True). Reconcile then re-points the folder at the proxy-only directory, unpauses it, and records a false 'moved your copy' event.
- Failure scenario: The admin renames 2026/Old to 2026/New while the editor has the project open in Resolve. Pass 1: the move fails and the project is marked blocked, and lane B pulls proxies into New. Pass 2: the Syncthing folder is re-pointed at New and unpaused. New holds proxies only: no .stfolder and no lane-C assets. The real project stays at Old, outside every lane's scope, so lane A stops uploading its originals. The blocked sentence is replaced by a false 'CC Sync moved your copy'. Resolve is relinked to paths that do not exist. The probe reproduced this with a fake admin.
- Verifier: The mechanism holds at HEAD. repath.reconcile (repath.py ~432-455) records a blocked event when _move_dir fails and leaves the folder paused. But sequencer._process_project (2045-2067) does not check the blocked state before continuing. It goes on to _maybe_clone_structure(subpath) (on the first pass, when every<=1, or when the N-pass age is due) and to _run_lanes_a_and_b for the NEW subpath. Lane B mirrors proxies into local_root/Projects/<new rel>, and the clone mkdirs the skeleton there. On the next pass, reconcile sees actual (the old path) != expected. _move_dir finds src.is_dir() True and dst.exists() True, and takes the 'target already exists ... re-pointing the folder anyway' branch, which returns True (repath.py 564-569). Reconcile then calls set_folder_path(expected), records moved_note (a false 'moved your copy'), relinks Resolve old->new, and calls set_folder_paused(False) directly, with no .stignore or .stfolder check. The blocked branch's own comment names this exact hazard: 'the structure clone then creates it -- so Syncthing sees the whole project's lane-C set as deleted' (AUDIT_2 DEL-5/L-8). The conflict branch reintroduces it one pass later. KNOWN_BUGS SYNC-10 onl

### bug-comp-broll-1 - A batch holding two clips with the same name indexes the first clip twice and never the second
- Where: `companion/src/ccsync_companion/broll_ingest.py:1891-1901 (_item_from_manifest); broll/web/static/ingest.js:438-463; broll/web/app/ingest_batches.py:569`
- What: The claim manifest carries no local_id, so the companion pairs manifest rows with staged files by (orig_name, rel_dir) and takes the first match. Two staged items with the same name and rel_dir both resolve to the first item's local_path, while each row keeps its own hash.
- Failure scenario: Editor drops card A's C0001.MP4, then card B's C0001.MP4 (the page appends drops into one batch, no name dedupe) and runs the batch. Card A's bytes are proxied, described and uploaded as both C0001 and C0001_2 (the second row carrying B's hash). Card B is never indexed, and its staged copy is later pruned. Nothing reports a failure. Probe1 confirmed this: both rows came back with local_path S/i1.mp4.
- Verifier: The mechanism holds end to end. The page never removes duplicate names. ingestAddItems (ingest.js:438-463) appends each drop to ing.items. ingestCollect strips a single dropped folder's name from rel_dir, so two Sony/Canon cards dropped one after the other both produce the same rel_dir (for example PRIVATE/M4ROOT/CLIP/C0001.MP4). prepare stages every item under its own local_id path (broll_ingest.py:1551). create_batch (ingest_batches.py:569) stores orig_name and rel_dir per item, and the claim manifest carries no local_id. _item_from_manifest (broll_ingest.py:1891-1901) pairs rows with staged files by (name, rel_dir) and breaks on the first match, so both rows get item 1's local_path. The only other check is line 2292, where item["hash"] = item.get("hash") or self._hash_fn(source). That keeps the manifest hash (card B's) and never checks the file bytes against it, so nothing fails. _allocate gives the second row a _2 name, and card A's footage is indexed and uploaded under both slots. KNOWN_BUGS.md has no entry for this, and nothing in CLAUDE.md makes it intended. _staging_holding uses the same name+rel_dir key and has the same blind spot. The result is silent wrong content in the

### bug-comp-ytdl-1 - A conversion longer than 120 s leaves the pre-conversion original eligible for lane A and the Resolve importer under its final name
- Where: `companion/src/ccsync_companion/ytdl_executor.py:2421,2551; companion/src/ccsync_companion/sync/rclone_lane.py:1739; companion/src/ccsync_companion/youtube_import.py:667`
- What: The executor converts in place beside the finished original, and the original is not written again while ffmpeg runs. 120 s after it lands it passes lane A's --min-age gate and the importer's settle test. The YT-3 --no-mtime fix only closes the case of a conversion shorter than 120 s, and the work-file excludes never cover the original under its final name.
- Failure scenario: A 10-minute 1080p clip arrives as VP9 and the libx264 re-encode takes 10+ minutes. Lane A uploads the VP9 file under the final name, and --ignore-existing keeps that undecodable copy on the NAS for good (/ytdl/fetch then hands it to other editors). youtube_import files the VP9 original into Master/Youtube. Resolve then holds the file open, swap_in falls back to '.converted [id].mp4', and the result is two clips per download.
- Verifier: The mechanism holds. How it happens:  - **Download location.** `_download_one` downloads into `destination_for(...)`, which is the canonical in-tree `<project>/Youtube/<term>` folder. Lane A carries `Youtube/` up on purpose (`build_filter_rules_up` has no Youtube exclude, and `build_filter_rules_down`'s docstring says requester-first clips reach the NAS through lane A). - **Conversion.** `_ensure_edit_ready` (ytdl_executor.py ~2512-2551) writes `<stem>.editready.mp4` beside the finished original `<stem> [id].mp4`. While ffmpeg runs (the timeout allows hours), nothing writes to the original again. `--no-mtime` stamps it with the moment it landed, so after 120 s it passes lane A's `--min-age 120s` (rclone_lane.py:1739). - **Lane A filter.** `YTDL_WORK_EXCLUDE_RULES` excludes only `*.editready.*`, `*.original.*`, `*.temp.*`, `*.fNNN*.*` and `*.failed`. The original under its final name is not excluded. - **No coordination.** Nothing connects the executor to lane A or the importer: there is no in-flight exclude path and no pause. The only `exclude_paths` caller is the express lane. - **Importer.** youtube_import.py:667 settles on age of 120 s or more plus an unchanged size and mtime. A

### bug-comp-core-1 - A crash-loop revert does not stop the machine re-installing the build it just fled
- Where: `companion/src/ccsync_companion/upgrade.py:1012-1019; app.py:6671-6686, 7780-7795, 7907-7939`
- What: revert_to_previous_build records the bad version only as reverted_from. It is never a failed attempt, and no gate reads it: _upgrade_attempt_blocked, auto-update and the pushed-update path check only attempts/backoff, and the dashboard's _upgrade_info keeps offering and pushing the same version.
- Failure scenario: With auto_update on or an admin push, vX crash-loops and is reverted to vX-1. The restored build's first report brings the vX offer/push back and it reinstalls vX at once. In round two the supervisor has used 2 of its 3 relaunches for the hour, so it refuses after one more. vX then has only 2 starts, crash_loop never trips, and the machine has no companion until the next logon, after which the cycle repeats.
- Verifier: The mechanism holds. revert_to_previous_build (upgrade.py:1012-1019) resets last_version.json and calls note_reverted_from, which only sets reverted_from/reverted_at/reverted_announced. Nothing in companion/src reads reverted_from as a gate. The only uses are the toast (app.py:11022), the report payload (app.py:7055, 8817) and _note_report_accepted, which clears the marker after the first accepted report (app.py:6660-6665).  _upgrade_attempt_blocked (app.py:6671) checks only upgrade_attempts_exhausted and upgrade_retry_due. When vX started successfully, the ledger load (app.py:~6648) called clear_upgrade_attempts because record.version == VERSION. So after the revert, vX-1 sees attempts 0 and no last_attempt_at, and retry is due.  Auto path: _on_upgrade_available -> compare_to_running says vX is NEWER -> _maybe_auto_update. It passes the blocked check, the offer matches, and apply_upgrade runs.  Push path: _upgrade_attempt_blocked is the same gate. On the dashboard, _upgrade_info (api.py:5772) offers the channel's current or targeted build with no machine_state.upgrade_reverted_from check. That field is only used in soak/alerts (db.py:4602, alerts.py:1800). The push command at api.

### bug-comp-media-1 - Media jobs and proxy generation spawn the bare name 'ffmpeg', so a machine whose only ffmpeg is the managed sidecar copy advertises the capability and fails every encode
- Where: `companion/src/ccsync_companion/jobs_media.py:251-312,469; jobs_runner.py:1167; proxy_gen.py:558,326,1726,1989; vs ffmpeg_tools.py:128-139,187-224`
- What: ffmpeg_available/_resolve_binary fall back to the sidecar-installed ffmpeg when the bare default is not on PATH, and capabilities report ffmpeg+ffprobe true from that probe, but jobs_media and proxy_gen put the unresolved config value ('ffmpeg') in argv[0]; Popen only searches PATH. ffprobe resolves (ffprobe_for), ffmpeg never does. music_clap_sidecar and ytdl_executor resolve first; these two do not.
- Failure scenario: Editor laptop with only the vendor-installed managed ffmpeg: reports ffmpeg:true, is offered proxy-480p/audio-extract/peaks, claims, fails in ~1 s with 'could not start ffmpeg: [WinError 2]' (retryable), earns the 120 s cooldown, job tours the fleet. proxy_gen on the same machine gates RUNNING and fails/caps every clip with the same spawn error; remote editors never get proxies.
- Verifier: The finding holds on every point I checked. In ffmpeg_tools.py, ffmpeg_available() (lines ~187-224) and _resolve_binary() (~128-139) fall back to sidecar_tools.managed_path('ffmpeg') when the bare 'ffmpeg' is not on PATH. capabilities._ffmpeg and _ffprobe therefore report true on a machine whose only ffmpeg is the managed one. KNOWN_BUGS confirms this was intended: the 0.7.9 note says "capabilities() turns ok the moment ffmpeg lands", and CR-53 says every vendor-build machine fetches the pinned ffmpeg-static unless it already has one.  The spawn paths never resolve the name. jobs_runner.py:1167 passes str(cfg.get('ffmpeg_path','ffmpeg')) into MediaJob. jobs_media's audio_copy_cmd, audio_opus_cmd, proxy_cmd and peaks_cmd put str(ffmpeg_path) at argv[0], and _run_ffmpeg Popens that argv. proxy_gen.py:558 stores the raw config value. own_proxy_cmd, preview_proxy_cmd and verify_decodes_cmd (ffmpeg_tools:909 returns [ffmpeg_path, ...]) put it at argv[0] unresolved, and proxy_gen:326 runs subprocess.Popen(cmd). Nothing in the companion adds the tools dir to os.environ['PATH'] (grep found no PATH mutation). So a bare 'ffmpeg' spawn raises FileNotFoundError / WinError 2 whenever PATH has n

### bug-music-ytdl-1 - The 'share vanished' guard runs after the download phase has created the project folder, so it can never fire
- Where: `ytdl/web/ytdlweb/worker.py:1617-1619 and 1683; ytdl/web/ytdlweb/routes_api.py:1650-1666`
- What: _phase_download calls ensure_outdir (os.makedirs of <PROJECTS_ROOT>/<label>/Youtube/<term>) before tree_missing_note. tree_is_gone answers 'not gone' as soon as <PROJECTS_ROOT>/<label> is a directory, which ensure_outdir has just created. So on an empty mount point the CR-286M guard always answers False. _no_room_note's tree check is reached after the same makedirs.
- Failure scenario: The NAS dataset is unmounted while the host has room. An editor presses DOWNLOAD and the worker creates the tree in the container overlay, finds it 'present', and downloads every clip there. ledger_add records NAS-relative paths with no file behind them, so every later search or paste of those ids across the fleet is skipped as already held, and the files are lost on the next container recreate. The overlay folder also fools the press-time check on every later press. Probe: tree_is_gone True -> ensure_outdir -> tree_is_gone False, tree_missing_note None.
- Verifier: The mechanism holds exactly as described. In ytdl/web/ytdlweb/worker.py, _phase_download calls ensure_outdir(outdir) at line 1619. That is os.makedirs of PROJECTS_ROOT/<label>/Youtube/<term>, so it creates PROJECTS_ROOT/<label>. The ytdl-web-5 guard runs later (tree_missing_note, line 1683), and so does _no_room_note's own tree_missing_note call (line 1567). Both reach routes_api.tree_is_gone (routes_api.py:1638), whose first test is `if project_dir.is_dir(): return False`. The directory ensure_outdir just made passes that test. So once the job reaches the guard, the guard always answers "not gone": on an emptied mount point the worker downloads into the container overlay and ledger_add records NAS-relative paths, which is the very scenario the ytdl-web-5 comment says it prevents. The folder left in the overlay then also makes the press-time _refuse_if_the_tree_is_gone answer "present" on every later press. Nothing earlier guards this: tree_is_gone/tree_missing_note are called only from these two worker sites and from the disk_full-only press path. KNOWN_BUGS has no open entry and nothing intends this behaviour. The regression test (tests/test_bug_hunt_2026_09_18_webapps_tools.py:1

### bug-ops-1 - publish_db.py --rollback picks the .prev-<ts>-wal sidecar and renames it over the live index
- Where: `server/publish_db.py:556 (list_prev_command), :578 (newest_prev); consumer build_rollback_script :501`
- What: The publish moves the live file's -wal/-shm along with it, to <target>.prev-<ts>-wal/-shm. The listing is find -name '<file>.prev-*', which matches those sidecars too. newest_prev sorts by name and takes the last one, and '...T101500' < '...-shm' < '...-wal', so the newest prev is the -wal file whenever the swapped-out index had a WAL beside it (the normal case, because the container holds it open in WAL mode).
- Failure scenario: After a bad publish the operator runs the documented recovery, publish_db.py --which broll --rollback --apply. The script moves the live broll.db aside and runs mv broll.db.prev-<ts>-wal broll.db, which installs a WAL journal as the database. It prints 'rolled back', and /broll (or /music) is serving a file that is not a database. Probe: newest_prev on a realistic listing returned '/d/broll.db.prev-20260821T101500-wal'. The only test lists bare .prev names, so it would not catch this.
- Verifier: The mechanism holds. The publish swap (install_dashboard_app.build_db_swap_script, around lines 3322-3339) moves <target>-wal and <target>-shm to <old>-wal and <old>-shm, where old = <target>.prev-<stamp> (publish_db.py:916). list_prev_command (publish_db.py:554-556) runs find -name '<file>.prev-*', which also matches those sidecars. newest_prev (:576-577) sorts the lines as plain strings and takes the last one. A bare name is a prefix of its own sidecar names, so '...T101500' sorts before '...T101500-shm', which sorts before '...T101500-wal'. I ran that sort on a realistic listing in the dashboard venv, and it returned /d/broll.db.prev-20260821T101500-wal.  The WAL sidecar is there in the normal case. The container holds the index open read-write in WAL mode (CLAUDE.md), and the swap is a rename done under the running container, so the -wal file exists when the swap happens.  build_rollback_script (:513-538) then does the following: - The existence check on prev passes. - It moves live broll.db and its sidecars aside. - It runs mv <prev>-wal broll.db. - It looks for <prev>-wal-wal and <prev>-wal-shm to bring back. They do not exist, so it skips them. - It prints 'rolled back'.  Th

### logic-plans-3 - Section 4b trashes a whole local folder including never-uploaded originals; trash is kept 14 days, not the documented year
- Where: `companion/src/ccsync_companion/file_moves.py:522-568; docs/HAND_MOVES_ON_THE_SERVER.md:95 vs sync/lane_guard.py:73`
- What: When the destination is not synced on this machine, apply_move moves the whole source path into .ccsync-trash. It does not check whether the folder holds originals that lane A has not uploaded yet, and it answers ok=True. The design promises the copy is 'recoverable for a year', but prune_trash defaults to 14 days, and the size cap and disk-pressure rule can prune sooner.
- Failure scenario: An upload-only laptop is part-way through uploading X/Card_07. The owner moves Card_07 on the NAS into project Y, which the laptop does not sync. The whole local folder goes to trash, and lane A never uploads from trash. The clips that were never uploaded are then deleted by prune_trash after 14 days, while the project page shows this machine as done.
- Verifier: The mechanism holds. In companion/src/ccsync_companion/file_moves.py:522-568, apply_move takes the section 4b branch whenever _dest_is_synced_here is False. That branch moves the whole `src` (for an is_dir move, the entire local folder) into .ccsync-trash/<stamp>/Projects/... with src.replace(trash). It records an intent row with new_local="" and returns (True, DETAIL_NOT_SYNCED_HERE, None).  Nothing checks whether the local folder holds files the NAS does not have, meaning originals lane A has not uploaded yet. The caller (app.py around 8160-8196) records ok=True, answers the dashboard with state not_synced_here, and does no pending-upload check.  The dashboard sends the command to the right machines for this to happen. db.file_move_target_machines (db.py:5842) sends it to "every computer syncing the source project in either mode", which includes upload_only, plus machines whose manifest holds the path. So an upload-only machine that is mid-upload of a card folder the owner moves on the NAS into a project it does not sync gets the whole folder trashed. That includes the clips the NAS never received. Lane A only walks Projects/, so it never uploads from .ccsync-trash, and the inten

### logic-release-1 - Documented key rotation signs the overlap release with the NEW key, and the REL-7 refusal tells the operator to override it
- Where: `docs/RELEASE.md:675-678; tools/release_key.py:109-121; tools/publish_feed.py:1250-1265`
- What: Rotation step 1 says run `release_key.py new --force` + `bake --add`, then ship 'with the OLD key still signing'. But `new --force` moves the old key to release.key.superseded and writes the new key at release.key, which every signer reads. No doc names how to sign with the old key, and publish_latest has no --key option.
- Failure scenario: The operator follows step 1. REL-7 refuses and says an overlap release is the answer and to pass --allow-key-rotation 'if this is that deliberate step'. They pass it. Every machine on the current build refuses the build permanently. The feed channel is also re-signed with the new key, so every customer dashboard pinning the old key rejects the whole channel: fleet-wide stranding with no OTA recovery.
- Verifier: The mechanism holds. docs/RELEASE.md:675-678 tells the operator to run `release_key.py new --force` and `bake --add`, then ship "with the OLD key still signing". But cmd_new (tools/release_key.py:109-121) moves the old key to release.key.superseded and writes the new key at release.key. That default path is what every signer reads: key_path() in sign_release.py:414, publish_feed.write_channel:412 for the channel signature, the release.ps1:530 probe, and publish_latest.preflight:107. None of the rotation docs or refusal texts says how to keep signing with the old key. There is a workaround nobody mentions: CCSYNC_RELEASE_KEY pointed at release.key.superseded would work, because key_path() honours it and publish_latest's preflight only checks that release.key exists.  The gates do not catch it: - release.ps1's baked-key check passes, because the new key is baked alongside the old one. - The REL-7 check fires, but its message steers the operator the wrong way. publish_feed.py:1259-1262 says "`bake --add`, ship THAT (it trusts both keys) ... Pass --allow-key-rotation if this is that deliberate step". build_editor_package.ps1:460-462 says the same with -AllowKeyRotation. An operator who

### ui-dash-admin-1 - A project name with an apostrophe breaks the ARCHIVE confirm, so the project archives with no question (a crafted folder name can run script)
- Where: `dashboard/templates/admin_assignments.html:166`
- What: onsubmit="return window.confirm('Archive {{ p.label }}? ...')" puts the label into a JS single-quoted string. Jinja's &#39; is HTML-decoded back to ' before the handler compiles. p.label is the project's relative folder path (collector upsert_project), a name anyone with write access to the tree chooses.
- Failure scenario: A folder named "Editor's Cut" makes the handler a SyntaxError, so it is null and [ ARCHIVE ] submits with no confirm. A folder named x');fetch(...);(' runs script in the admin's session when they click ARCHIVE on that row.
- Verifier: The mechanism holds. In dashboard/templates/admin_assignments.html:166, `{{ p.label }}` sits inside a single-quoted JS string in an `onsubmit` attribute. Jinja2Templates (ui.py:60) only HTML-autoescapes, so `'` becomes `&#39;`. The browser decodes that back to `'` before it compiles the handler, so the escaping gives no protection in a JS-string context. I found no `\|tojson` filter, no JS escaping and no Content-Security-Policy anywhere in src.  The label is attacker-influenced. `upsert_project` is called from collector.py:1020 and api.py:3546 with `rel` (the project's relative folder path on the NAS), and from collector.py:1257 with the Syncthing folder label, which the collector itself sets to `rel`. Folder names are not sanitised on the way in. A folder named "Editor's Cut" makes the handler a SyntaxError, so the attribute handler is null and the form submits with no confirm. A crafted name such as `x');fetch(...);('` runs as script in the admin's session.  Mitigations: - The page is admin-only. - The payload needs write access to the Projects tree. - Archive is reversible.  Even so, stored XSS from a folder name into an admin session is a privilege escalation from anyone who c

## Highs the verifier downgraded

| Id | Now | Title | Why |
|---|---|---|---|
| bug-comp-core-2 | medium | APP-5 rollback copy is deleted seconds after start, so the crash-loop revert cannot fire on an online machine | The mechanism is real. In app.py:11073-11078 the keeper runs with `self._report_accepted.is_set`. keep_old_exe_until_healthy (upgrade.py:927-944) checks healthy() before its first 30 s wait. The reporter posts its first report after INITIAL_DELAY_SECONDS = 2.0 (reporter.py:101), and _note_report_accepted (app.py:6655) sets the event. So on a machine that can reach the dashboard, `<exe>.old` is del |
| bug-dash-auth-1 | low | The anonymous first-run window opens every setup route, not only the EULA and create-admin steps | The code does what the finding says, but the attack in the failure scenario can't happen on the deployment this applies to.  What holds up: in setup_routes.py, require_setup_access (lines 87-99) and first_run_open (lines 47-84) return None, meaning an anonymous caller is allowed, for every route that calls them. Under DASH_AUTH_METHOD=local with an empty users table, that includes tasks/{id}/run ( |
| bug-music-ytdl-2 | medium | A browser upload can take a filename a fleet batch already promised, and the companion's upload then overwrites it | The mechanism holds. When a fleet result is posted, allocate_name (ingest_batches.py:226-280) reserves dest_name in three places: the ledger, the tracks table and the disk (_candidate_free, which checks the reserved set, then _disk_state, then _track_state). The companion's upload lands in the same flat share root, which is music_ingest's LIBRARY_REMOTE_REL (the plan doc says "uploads stay flat un |
| logic-onboarding-1 | medium | The wizard still takes a machine's role from the PERSON, so a re-run dismantles an admin's remote laptop and loops a non-admin's wired desktop | The mechanism is real. dashboard api.py:2226 still sends `"role": "base" if auth.is_admin(...) else "editor"`. onboarding/steps.py:1752 `effective_install_role` lets any recognised verified role beat the radio button. onboard.py:802-806 snaps role_var to that role, and steps.py:3165 `ensure_config` forces it into config.toml `mode` (for base it also sets local_root and canonical_prefix to the base |
| logic-plans-1 | medium | Unticking a bucket-inherited project is a no-op that answers changed=true, and the tray then removes it from the person's other computers | I reproduced it on a scratch DB using the dashboard venv and the real db functions. Setup: B is ticked into the bucket before any report; DESK and LAP are registered; A is ticked on DESK. That gives DESK=[B,A] and LAP=[B]. Then remove_selection(B, machine=LAP) returns True, and selections_for_machine(LAP) still returns [B]. The reason is in db.py:6841-6852: materialise_bucket copies [B] onto LAP a |

## Every finding, by wave and hunter

Severity after verification for the highs. Full text in `hunters/<hunter>.md`.

### Wave 1 - bugs

**bug-comp-app** (7)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-comp-app-1 | high | C | A halt's paused project folders are unpaused by the next sequencer stop()/pause() (tray Quit, self-upgrade, Resolve-exit restart, tray Pause, drive unplugged) | `companion/src/ccsync_companion/app.py:6456-6461, 10705, 9245-9247, 2890-2892; sync/sequenc` |
| bug-comp-app-2 | medium | C | Consolidate ignores the halt, the licence gate and the sign-in gate, and runs lane A upload and lane B rclone sync directly | `companion/src/ccsync_companion/app.py:3802-3832, 4108, 4123; sync/rclone_lane.py:3505-3570` |
| bug-comp-app-3 | medium | C | An admin's diagnostics request is marked answered before the upload, so a failed upload is never retried | `companion/src/ccsync_companion/app.py:8724-8733; dashboard/src/ccsync_dashboard/api.py:988` |
| bug-comp-app-4 | low | C | The pending-relink 'done' answer can never land: the dashboard only updates rows with applied_at IS NULL | `companion/src/ccsync_companion/app.py:8390-8395; dashboard/src/ccsync_dashboard/db.py:6236` |
| bug-comp-app-5 | low | P | An off-cycle report runs _on_report_response concurrently with the reporter thread's own | `companion/src/ccsync_companion/app.py:7611-7629, 7579, 2269-2350; reporter.py:1223-1227` |
| bug-comp-app-6 | low | C | _queue_file_move_answer appends outside the lock res-companion-5 added | `companion/src/ccsync_companion/app.py:8253-8268` |
| bug-comp-app-7 | low | P | The posix single-instance pid file never checks that the live pid is a companion | `companion/src/ccsync_companion/app.py:503-542, 438-446` |

**bug-comp-rclone** (5)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-comp-rclone-1 | high | C | A blocked repath is finished on the next pass by pointing Syncthing at the directory that lane B or the structure clone created | `companion/src/ccsync_companion/sync/repath.py:567; companion/src/ccsync_companion/sync/seq` |
| bug-comp-rclone-2 | medium | C | Server-followed moves on small passes stay on the breaker's cumulative counter and trip 'slow leak' | `companion/src/ccsync_companion/sync/lane_guard.py:786-793; companion/src/ccsync_companion/` |
| bug-comp-rclone-3 | medium | C | The disk-floor park keeps a stale sentence and never clears after the trash prune frees space | `companion/src/ccsync_companion/sync/lane_guard.py:1019-1031; lane_guard.py:1286-1298` |
| bug-comp-rclone-4 | low | P | The stray-project scan and express attribution compare disk paths to dashboard rels without NFC folding | `companion/src/ccsync_companion/sync/rclone_lane.py:3402-3410; rclone_lane.py:2390-2395` |
| bug-comp-rclone-5 | low | C | A file lane B moves to the trash counts as 'transferred' as well as deleted | `companion/src/ccsync_companion/sync/rclone_lane.py:2138-2146` |

**bug-comp-syncthing** (7)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-comp-syncthing-1 | medium | C | A proxy that cannot follow turns a completed move into a failed one, and the retry drops the relink | `companion/src/ccsync_companion/file_moves.py:574-579 (resume arm :446-485)` |
| bug-comp-syncthing-2 | medium | C | An upload-only borrower still builds a borrowed lender, so the tray tells the editor to ask an admin who cannot fix it | `companion/src/ccsync_companion/sync/sequencer.py:1628-1664 (_build_borrowed); sync/borrowe` |
| bug-comp-syncthing-3 | medium | C | A full borrower whose lender is ticked upload-only never receives the borrowed subtree (both sides) | `companion/src/ccsync_companion/sync/sequencer.py:1651,1659; dashboard/src/ccsync_dashboard` |
| bug-comp-syncthing-4 | medium | P | A sequencer thread that outlives stop() can unpause a folder after the halt paused it | `companion/src/ccsync_companion/sync/sequencer.py:2811-2812; sync/syncthing_admin.py:577-57` |
| bug-comp-syncthing-5 | low | P | Borrowed-folder negations are escaped with a backslash, which Syncthing on Windows reads as a path separator | `companion/src/ccsync_companion/sync/syncthing_admin.py:198-206, 235-239` |
| bug-comp-syncthing-6 | low | C | The cached plan is not keyed by editor, so a sign-in as someone else can run the previous person's projects | `companion/src/ccsync_companion/selection.py:361-373, 442-449, 573-585` |
| bug-comp-syncthing-7 | low | P | Re-pointing a shared asset folder does not move its contents or create the path and marker, yet reports 'repaired' | `companion/src/ccsync_companion/sync/shared_folders.py:369-374` |

**bug-comp-ui** (6)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-comp-ui-1 | medium | C | Ingest file picker builds a Tk root outside _popup_active_lock, and its timeout abandons a live dialog whose answer is dropped | `companion/src/ccsync_companion/popup.py:2694-2774 (_tk_pick, pick_media_sources); broll_se` |
| bug-comp-ui-2 | medium | P | macOS: async menu rebuild landing while the menu is open drops its weak delegate, leaving ui_state.menu_open set forever | `companion/src/ccsync_companion/tray_native.py:1767-1816; tray.py:4936-4973, 279-315` |
| bug-comp-ui-3 | low | C | WorkProgressWindow.close() before the root exists reports closed, then the window opens anyway, untracked | `companion/src/ccsync_companion/popup.py:2160-2205; app.py:235-260, 9836-9851` |
| bug-comp-ui-4 | low | C | Work-progress window is a Tk root outside _popup_active_lock | `companion/src/ccsync_companion/popup.py:2207-2213; app.py:9829-9848` |
| bug-comp-ui-5 | low | C | Five tray.py dialog builders abandon a mapped root when widget construction raises (comp-ui-6 shape fixed only in Settings) | `companion/src/ccsync_companion/tray.py:1134-1231, 1921-1976, 2189-2273, 1688-1722, 1781-18` |
| bug-comp-ui-6 | low | P | _WindowsIcon._teardown iterates _hicon_cache unguarded while loops may insert; a raise skips _stopped.set() | `companion/src/ccsync_companion/tray_native.py:923-927, 1474-1489, 1088-1107` |

**bug-comp-resolve** (9)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-comp-resolve-1 | medium | C | An admin's undo sent during Resolve's launch window is recorded FAILED and never retried | `companion/src/ccsync_companion/resolve_undo.py:228-235 (resolve_bridge.py:507)` |
| bug-comp-resolve-2 | medium | C | The 20 s project-name cache files a relink under the PREVIOUS project, which brings back the cross-project undo hazard | `companion/src/ccsync_companion/resolve_bridge.py:2130-2153, 2223-2245, 3050; app.py:3258, ` |
| bug-comp-resolve-3 | medium | C | Cards sweep_items runs the API walk under _API_LOCK when no library is available, throws the result away, and speeds up the watcher's full-walk valve | `companion/src/ccsync_companion/timeline_cards_bridge.py:239-260; resolve_bridge.py:1235-12` |
| bug-comp-resolve-4 | medium | P | Restarting the Cards role after ONE loop dies leaves the other loop running on the released engine | `companion/src/ccsync_companion/timeline_cards_role.py:782-800, 805-817, 866-938` |
| bug-comp-resolve-5 | low | C | On macOS the CR-68 probe spawns lsof while holding _API_LOCK | `companion/src/ccsync_companion/resolve_bridge.py:351-361 -> script_server.py:306-318` |
| bug-comp-resolve-6 | low | C | An SMB blip at the reservation re-check leaves the original 0-byte reservation orphaned for good | `companion/src/ccsync_companion/fixer.py:1026-1040, 1358-1361, 831-904` |
| bug-comp-resolve-7 | low | P | The uid-map cache is keyed on `project is <cached object>`, which a fresh GetCurrentProject() wrapper probably never satisfies | `companion/src/ccsync_companion/resolve_bridge.py:2016-2019` |
| bug-comp-resolve-8 | low | P | The library finds the project by name only, so a same-named project in another folder supplies the pool | `companion/src/ccsync_companion/library.py:629-639` |
| bug-comp-resolve-9 | low | C | LUT adoption leaves `<name>.ccsync-tmp` behind on a failed copy | `companion/src/ccsync_companion/luts.py:377-382` |

**bug-comp-broll** (6)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-comp-broll-1 | high | C | A batch holding two clips with the same name indexes the first clip twice and never the second | `companion/src/ccsync_companion/broll_ingest.py:1891-1901 (_item_from_manifest); broll/web/` |
| bug-comp-broll-2 | medium | C | keep sub-folders unticked, or a folder name safe_name rewrites, fails every sub-folder clip with 'not on this computer any more' | `companion/src/ccsync_companion/broll_ingest.py:1897-1898 and :2282-2285; broll/web/app/ing` |
| bug-comp-broll-3 | medium | C | Insert's download-done branch is unreachable: finished fetch jobs leak, and the stand-in row is never rewritten before import | `companion/src/ccsync_companion/broll_server.py:1155, :1177, :1284-1314; companion/src/ccsy` |
| bug-comp-broll-4 | medium | C | Staging held after one original failure is held for ever, and the disk-full message names a button that cannot free it | `companion/src/ccsync_companion/broll_ingest.py:3521-3522, :3634-3651, :3704-3714, :3677-36` |
| bug-comp-broll-5 | medium | P | Ingest encodes have a fixed 15-minute ceiling, so long clips on CPU-only machines lose their proxy or the whole item | `companion/src/ccsync_companion/broll_ingest_media.py:376-421; companion/src/ccsync_compani` |
| bug-comp-broll-6 | low | C | Upload queue survives a normally finished batch, carrying its paused state and done/failed ledger into the next batch | `companion/src/ccsync_companion/broll_ingest.py:3252-3295, :2767-2788, :1843-1844, :3345-33` |

**bug-comp-ytdl** (5)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-comp-ytdl-1 | high | C | A conversion longer than 120 s leaves the pre-conversion original eligible for lane A and the Resolve importer under its final name | `companion/src/ccsync_companion/ytdl_executor.py:2421,2551; companion/src/ccsync_companion/` |
| bug-comp-ytdl-2 | medium | C | Stop, quit or lease loss during 'converting' leaves the unconverted original as a finished clip | `companion/src/ccsync_companion/ytdl_executor.py:2539-2545, 2422-2424, 2976-2984` |
| bug-comp-ytdl-3 | medium | C | A mid-job 403 (sign-out, expired or revoked identity) never stops the executor; it downloads the whole job unrecorded while the server re-downloads it | `companion/src/ccsync_companion/ytdl_executor.py:1188-1199, 1251-1257, 2074-2087; ytdl/web/` |
| bug-comp-ytdl-4 | medium | C | One failed yt-dlp install or floor update switches local downloads off for 24 hours; nothing retries sooner | `companion/src/ccsync_companion/ytdlp_manager.py:1266, 1123-1147` |
| bug-comp-ytdl-5 | low | P | swap_in's last-resort deliverable is the sweepable .editready temp name | `companion/src/ccsync_companion/ytdl_executor.py:1680-1685` |

**bug-comp-core** (7)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-comp-core-1 | high | C | A crash-loop revert does not stop the machine re-installing the build it just fled | `companion/src/ccsync_companion/upgrade.py:1012-1019; app.py:6671-6686, 7780-7795, 7907-793` |
| bug-comp-core-2 | medium (was high) | C | APP-5 rollback copy is deleted seconds after start, so the crash-loop revert cannot fire on an online machine | `companion/src/ccsync_companion/upgrade.py:907-950; app.py:11074-11075; reporter.py:101` |
| bug-comp-core-3 | medium | C | Site manifest (feature flags, smb_unc, sftp tuning) refreshed once per process start | `companion/src/ccsync_companion/app.py:9342-9356; site.py:278-298; config.py:1829-1866` |
| bug-comp-core-4 | medium | C | Sign-in /verify path carries no arch and no upgrade_none_reason: offers a wrong-CPU build and clears a standing refusal | `companion/src/ccsync_companion/identity.py:320-360; app.py:6505; dashboard/src/ccsync_dash` |
| bug-comp-core-5 | medium | P | Manifest scan that overlaps a drive unplug reports zero files for the remaining projects | `companion/src/ccsync_companion/manifest.py:316-347, 186` |
| bug-comp-core-6 | low | C | Repaired machine id not reported until the tray restarts, although the toast says 'from the next check in' | `companion/src/ccsync_companion/reporter.py:704-717; settings_window.py:497-506` |
| bug-comp-core-7 | low | C | Sign-in with an unusable reply token overwrites a working identity.json | `companion/src/ccsync_companion/identity.py:514-541` |

**bug-comp-media** (8)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-comp-media-1 | high | C | Media jobs and proxy generation spawn the bare name 'ffmpeg', so a machine whose only ffmpeg is the managed sidecar copy advertises the capability and fails every encode | `companion/src/ccsync_companion/jobs_media.py:251-312,469; jobs_runner.py:1167; proxy_gen.p` |
| bug-comp-media-2 | medium | C | Companion job gate ignores the dashboard's per-kind idle floor (60 s for audio-extract/peaks) and the base-rig exemption | `companion/src/ccsync_companion/jobs_runner.py:262,591-594,729-747; dashboard/src/ccsync_da` |
| bug-comp-media-3 | medium | C | out_stem is used as a filename unvalidated: absolute stems write anywhere, '/' or ':' in a multicam name land in the wrong place | `companion/src/ccsync_companion/jobs_runner.py:1226; jobs_media.py:814,824,828,884,924; twi` |
| bug-comp-media-4 | medium | C | Peaks job on a clip with no audio is a retryable failure, so it tours the fleet cooling every machine down before it pins | `companion/src/ccsync_companion/jobs_media.py:916-939 (vs _audio :817-821)` |
| bug-comp-media-6 | medium | P | Music insert at playhead fails whenever a clip straddles the playhead on the target track (ripple moves it into the new cue) | `companion/src/ccsync_companion/music_worker.py:361-412` |
| bug-comp-media-5 | low | C | Proxy generator gate checks ffmpeg but not ffprobe; with no ffprobe every clip fails and is capped while the state reads RUNNING | `companion/src/ccsync_companion/proxy_gen.py:949-970,1000,2082-2098; ffmpeg_tools.py:144-16` |
| bug-comp-media-7 | low | C | Music ingest decode holds ~3x the track's PCM in the tray process, and tracks over 2 h get a false 7200 s duration | `companion/src/ccsync_companion/music_clap_sidecar.py:72,796-822,946-969` |
| bug-comp-media-8 | low | C | Job input rejected by Path (NUL byte) escapes _execute_media/_execute_whisper uncaught; no result posted, every machine silently drops the job | `companion/src/ccsync_companion/jobs_runner.py:1102-1109,1053-1060; job_paths.py:112-113` |

**bug-dash-db** (3)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-dash-db-1 | medium | C | The scheduler only ever looks at the first 200 queued jobs, so a block of jobs nobody can run starves every job behind it | `dashboard/src/ccsync_dashboard/db.py:10255 (queued_jobs limit=200), used by db.py:10534 cl` |
| bug-dash-db-2 | medium | C | A project with more than 2,000 proxies shows the extra proxies as downloads owed for ever, and the note says the total may be too low | `dashboard/src/ccsync_dashboard/db.py:9699-9766 (fetch_sync_backlog down spec); companion/s` |
| bug-dash-db-3 | low | C | Forgetting a computer or a person leaves their diagnostics bundles behind | `dashboard/src/ccsync_dashboard/db.py:8435-8439 (_MACHINE_STATE_TABLES) and db.py:1114 (dia` |

**bug-dash-api** (4)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-dash-api-1 | medium | C | Approving a second computer's SSH key erases the first computer's key on the NAS | `dashboard/src/ccsync_dashboard/api.py:4555 (approve_pending_ssh_key); nas/truenas.py:266, ` |
| bug-dash-api-2 | medium | C | File move that renames leaves the proxy under the old name; a same-folder rename reports PARTIAL falsely | `dashboard/src/ccsync_dashboard/api.py:2770-2806 (_move_proxy_siblings); companion/src/ccsy` |
| bug-dash-api-3 | low | C | Create/link over an ARCHIVED project answers ok but it stays archived and invisible | `dashboard/src/ccsync_dashboard/api.py:3614-3656 (create_tree_project), 3659-3731 (adopt_fo` |
| bug-dash-api-4 | low | C | Local-mode admin password reset does not revoke the account's existing sessions | `dashboard/src/ccsync_dashboard/api.py:4298-4304 (api_admin_set_password)` |

**bug-dash-diag** (4)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-dash-diag-1 | medium | C | A NAS that does not answer turns a MISSING protection line into a 'this has cleared' mail and closes its notice | `dashboard/src/ccsync_dashboard/protection.py:881-906; dashboard/src/ccsync_dashboard/alert` |
| bug-dash-diag-2 | medium | C | check_failed alerts can never recover, so they stay open forever and a later failure is reported as weeks old | `dashboard/src/ccsync_dashboard/alerts.py:4565-4569, 3686-3688` |
| bug-dash-diag-3 | medium | P | A snapshot restore's .restored-<ts> folder is replicated by Syncthing to every editor who has the project ticked | `dashboard/src/ccsync_dashboard/recovery.py:88-95, 424-458; dashboard/src/ccsync_dashboard/` |
| bug-dash-diag-4 | low | C | A failed db.migrate in the collector loop is never retried and leaves the collector on the un-migrated connection | `dashboard/src/ccsync_dashboard/collector.py:310-324` |

**bug-dash-auth** (7)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-dash-auth-1 | low (was high) | C | The anonymous first-run window opens every setup route, not only the EULA and create-admin steps | `dashboard/src/ccsync_dashboard/setup_routes.py:97 (require_setup_access); used by :164, :2` |
| bug-dash-auth-2 | medium | C | The session touch blocks the event loop for up to 20 s and then 500s a valid session | `dashboard/src/ccsync_dashboard/sessions.py:231-236; app.py:1324 (async login_gate), app.py` |
| bug-dash-auth-3 | medium | C | UNDO of a site change empties the project template and the shared asset folder list | `dashboard/src/ccsync_dashboard/site_store.py:788-789 and :459; setup_routes.py:435-443` |
| bug-dash-auth-4 | medium | C | A shared asset folder name with no ASCII letter or digit 500s /api/v1/site and every manifest reader | `dashboard/src/ccsync_dashboard/provision.py:208 (slugify raises); site_store.py:239-258 (v` |
| bug-dash-auth-5 | low | C | Setup reports the admin step done on an OIDC admin claim that grants nothing | `dashboard/src/ccsync_dashboard/setup_engine.py:586-591` |
| bug-dash-auth-6 | low | C | The boot log misdescribes DASH_COOKIE_SECURE=true/yes/on/false/no/off | `dashboard/src/ccsync_dashboard/auth.py:887` |
| bug-dash-auth-7 | low | P | A trusted proxy's X-Forwarded-For is read from the client-controlled left end | `dashboard/src/ccsync_dashboard/auth.py:238` |

**bug-dash-cards-jobs** (6)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-dash-cards-jobs-1 | medium | C | A double slash walks past CardsGate: /cards/p/<slug>//api/root reaches engine.set_root() | `dashboard/src/ccsync_dashboard/cards.py:425 (CardsGate BLOCKED_PATHS match) and cards.py:5` |
| bug-dash-cards-jobs-2 | medium | C | An agent's traffic follows whichever of its editor's devices made the last request, so pending and result land on different engines | `dashboard/src/ccsync_dashboard/cards_pool.py:347 (note_visit) and :324 (engine_for); cards` |
| bug-dash-cards-jobs-3 | medium | C | Pinned jobs wait for ever, with no alert, whenever no episode happens to be open | `dashboard/src/ccsync_dashboard/cards_exec.py:283 (tick); alerts.py:2755-2808; jobs.py:411` |
| bug-dash-cards-jobs-4 | medium | C | After a session-secret rotation the b-roll and music gates mint no identity, so ingest panels and client folders 401 | `dashboard/src/ccsync_dashboard/broll.py:232 and music.py:242 vs auth.py:606` |
| bug-dash-cards-jobs-5 | low | C | The API-path session trim drops corpus parts 2..n once a conversation passes 40 turns | `dashboard/src/ccsync_dashboard/cards_ai.py:1182 (_trimmed)` |
| bug-dash-cards-jobs-6 | low | C | POST /cards/open walks the vault four levels deep on the event loop | `dashboard/src/ccsync_dashboard/cards_landing.py:199-214` |

**bug-dash-ops** (9)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-dash-ops-1 | medium | C | The 'same Message-ID is in my Sent folder' proof is an IMAP substring search, so a forged From passes the authentication check | `dashboard/src/ccsync_dashboard/triage_mail.py:599 (the search), :499 (the override)` |
| bug-dash-ops-2 | medium | C | /ytdl drops the identity of every session signed with a previous DASH_SESSION_SECRET | `dashboard/src/ccsync_dashboard/ytdl.py:171 (gate built at :555 with only settings.session_` |
| bug-dash-ops-4 | medium | C | crash_report.redact misses the secrets its own docstring names (env-style and quoted keys) | `dashboard/src/ccsync_dashboard/crash_report.py:66` |
| bug-dash-ops-3 | low | P | The ytdl gate reads the FIRST ccsync_session cookie while login_gate reads the LAST, and the gate never checks revocation | `dashboard/src/ccsync_dashboard/ytdl.py:307-324` |
| bug-dash-ops-5 | low | C | Reply poll cap counts already-handled messages, so an older unhandled reply can be starved forever | `dashboard/src/ccsync_dashboard/triage_mail.py:653-663` |
| bug-dash-ops-6 | low | C | The CCT reply token is stored in plaintext beside its hash | `dashboard/src/ccsync_dashboard/triage.py:694-696` |
| bug-dash-ops-7 | low | P | Authentication-Results is trusted without checking its authserv-id | `dashboard/src/ccsync_dashboard/triage_mail.py:212-236` |
| bug-dash-ops-8 | low | C | A second CLI update inside the grace hour deletes the still-in-grace version at once | `dashboard/src/ccsync_dashboard/cli_tools.py:1231-1254, 1300` |
| bug-dash-ops-9 | low | C | Two crashes of the same thread in the same second overwrite one crash file | `dashboard/src/ccsync_dashboard/crash_report.py:133-139` |

**bug-broll** (4)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-broll-1 | medium | C | Client-folder item matching ORs a stale stored id with the current name: remove/note hit the wrong clip, add refuses a new one | `broll/web/app/client_folders.py:675-698 (remove_item, set_note), :605-627 (add_items); rou` |
| bug-broll-2 | medium | C | HttpBackend posts the LOCAL shadow id to /api/ingest/index and /api/ingest/moved, so descriptions land on a different clip and it gets renamed | `broll/indexer/broll_index/storage/http_backend.py:101-130, :184-191; server side broll/web` |
| bug-broll-3 | medium | P | Posters/sprites are keyed by videos.id in one shared folder while the base rig and the live dashboard mint ids independently, and the drain re-mints ingested ids without moving their stills | `broll/web/app/ingest_batches.py:157-158; routes_media.py:50-59; routes_share.py:250-269; b` |
| bug-broll-4 | low | P | A cancelled release is accepted from any of the editor's machines with no leaseholder check, deleting the live holder's in-flight rows | `broll/web/app/routes_fleet.py:255-261; ingest_batches.release :1376-1388; companion broll_` |

**bug-music-ytdl** (4)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-music-ytdl-1 | high | C | The 'share vanished' guard runs after the download phase has created the project folder, so it can never fire | `ytdl/web/ytdlweb/worker.py:1617-1619 and 1683; ytdl/web/ytdlweb/routes_api.py:1650-1666` |
| bug-music-ytdl-2 | medium (was high) | C | A browser upload can take a filename a fleet batch already promised, and the companion's upload then overwrites it | `music/web/musicweb/routes_ingest.py:377-379; music/web/musicweb/db.py:524-537; music/web/m` |
| bug-music-ytdl-3 | medium | C | The first fleet drop into a new library refuses itself from item 2 onward | `music/web/musicweb/config.py:539-560; music/web/musicweb/ingest_batches.py:249-251` |
| bug-music-ytdl-4 | medium | C | Cancelling a batch strands every indexed-but-not-uploaded track as a permanent row with no audio | `music/web/musicweb/ingest_batches.py:1252-1257 (release cancelled branch), 1238-1242, 568-` |

**bug-ops** (4)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-ops-1 | high | C | publish_db.py --rollback picks the .prev-<ts>-wal sidecar and renames it over the live index | `server/publish_db.py:556 (list_prev_command), :578 (newest_prev); consumer build_rollback_` |
| bug-ops-2 | medium | C | ship.cmd -Resume cannot resume past the publish and tells the operator to bump the version | `tools/ship.ps1:423-472 (already-published fail-fast), :925 (advice to re-run with -Resume)` |
| bug-ops-3 | medium | C | The recall command publish_latest.py prints does not parse | `tools/publish_latest.py:495-497; tools/publish_feed.py:1010, :1147` |
| bug-ops-4 | low | C | macOS --full uninstall reports the sign-in as removed without checking | `installer/macos_uninstall.sh:273-287` |

**bug-wire** (7)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| bug-wire-1 | medium | C | The "Resolve relinked after all" answer is dropped by the dashboard, so a move shows "Resolve not repointed yet" for ever | `dashboard/src/ccsync_dashboard/db.py:6237; companion/src/ccsync_companion/app.py:8394` |
| bug-wire-2 | medium | C | A session-secret rotation drain keeps the report and jobs alive but cuts every un-re-signed machine out of b-roll ingest, music ingest and ytdl | `broll/web/app/fleet_auth.py:173; music/web/musicweb/fleet_auth.py:169; ytdl/web/ytdlweb/ro` |
| bug-wire-3 | medium | P | Lane last_error/detail/current_project caps still raise, and the companion never caps them: one long lane error 422s the whole report | `dashboard/src/ccsync_dashboard/api.py:6832-6858; companion/src/ccsync_companion/sync/synct` |
| bug-wire-4 | low | C | /verify builds its upgrade offer without the arch, the machine or the withheld reason, and sign_in adopts it as if it were a report reply | `dashboard/src/ccsync_dashboard/api.py:2232; companion/src/ccsync_companion/identity.py:320` |
| bug-wire-5 | low | C | standins_known is never sent empty, so a companion keeps the last non-empty set of stand-ins for the life of the process | `dashboard/src/ccsync_dashboard/api.py:9949-9952; companion/src/ccsync_companion/proxy_reli` |
| bug-wire-6 | low | C | _queue_file_move_answer appends outside the lock it was given | `companion/src/ccsync_companion/app.py:8253-8268` |
| bug-wire-7 | low | P | A computer whose hostname is not Latin-1 cannot make any b-roll or music ingest fleet call | `companion/src/ccsync_companion/broll_ingest.py:400-406,244` |

### Wave 2 - logic and usability

**logic-onboarding** (5)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| logic-onboarding-1 | medium (was high) | C | The wizard still takes a machine's role from the PERSON, so a re-run dismantles an admin's remote laptop and loops a non-admin's wired desktop | `onboarding/steps.py:1752 (effective_install_role), onboarding/steps.py:3160 (ensure_config` |
| logic-onboarding-2 | medium | C | On a site with the shared report token retired, the wizard says DONE for a machine with no fleet credential | `onboarding/onboard.py:771-775,1334-1339; dashboard/src/ccsync_dashboard/api.py:2216` |
| logic-onboarding-3 | medium | C | windows_upgrade.ps1 sends a licence-only problem to the full reinstall wizard, alongside the companion's own licence dialog | `installer/windows_upgrade.ps1:655-735` |
| logic-onboarding-4 | medium | C | The wizard and both bootstraps still ask for a 'TrueNAS username and password' | `onboarding/onboard.py:417,427,740; installer/windows_bootstrap.ps1:2463; installer/macos_b` |
| logic-onboarding-5 | low | C | The editor finish page contradicts itself about signing in and asks for a key the wizard already sent | `onboarding/onboard.py:1336-1343,1072-1080` |

**logic-sync-truth** (6)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| logic-sync-truth-1 | medium | C | A laptop that is merely asleep gets a red "Upload has stopped" headline after 15 minutes | `dashboard/src/ccsync_dashboard/health.py:230 (lane_chip), health.py:1096-1143 (_lane_fault` |
| logic-sync-truth-2 | medium | C | The per-kind manifest cap still makes phantom proxy downloads, the UI says 'may undercount', and safe-to-close can miss uploads | `companion/src/ccsync_companion/manifest.py:37,208-223; dashboard/src/ccsync_dashboard/db.p` |
| logic-sync-truth-3 | medium | C | Tray says 'downloading N files' and pulses amber for ever when lane C has no connected peer | `companion/src/ccsync_companion/sync/syncthing_lane.py:776-851; reporter.py:245-272; tray.p` |
| logic-sync-truth-4 | medium | C | Disk-floor park never clears on its own: the trash prune stops at 1x the floor, the latch clears at 2x | `companion/src/ccsync_companion/sync/lane_guard.py:1005-1050, 1281-1296; rclone_lane.py:453` |
| logic-sync-truth-5 | low | C | 'Idle, nothing owed' is claimed without consulting what is owed | `dashboard/src/ccsync_dashboard/health.py:1153-1158; api.py:1121-1122` |
| logic-sync-truth-6 | low | C | Tray counts lane C files twice ('up 40 · down 40'); drive reminder nags about downloads the dashboard calls safe | `companion/src/ccsync_companion/tray.py:2491-2502; drive_reminder.py:77-81,128-165; dashboa` |

**logic-plans** (5)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| logic-plans-1 | medium (was high) | C | Unticking a bucket-inherited project is a no-op that answers changed=true, and the tray then removes it from the person's other computers | `dashboard/src/ccsync_dashboard/db.py:6841 + db.py:7327; companion/src/ccsync_companion/sel` |
| logic-plans-3 | high | C | Section 4b trashes a whole local folder including never-uploaded originals; trash is kept 14 days, not the documented year | `companion/src/ccsync_companion/file_moves.py:522-568; docs/HAND_MOVES_ON_THE_SERVER.md:95 ` |
| logic-plans-2 | medium | C | Project page's person-level mode buttons tick the project onto computers that never had it, with no confirm or capacity check | `dashboard/templates/partials/project_detail.html:90-101 -> ui.py:1413-1418 -> db.py:6791` |
| logic-plans-4 | medium | C | A file moved out of a borrowed (shared) folder is never announced to the machines that borrow it | `dashboard/src/ccsync_dashboard/db.py:5842; companion/src/ccsync_companion/manifest.py:173` |
| logic-plans-5 | low | C | Companion's unknown-machine untick fallback never fires; unknown hostname gets a silent 200 no-op | `companion/src/ccsync_companion/selection.py:490; dashboard/src/ccsync_dashboard/api.py:265` |

**logic-resolve** (5)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| logic-resolve-1 | medium | C | RETRY FAILED and 'run FIX ALL again' copy the whole file in a second time when only the relink failed | `companion/src/ccsync_companion/fixer.py:1449-1491; companion/src/ccsync_companion/popup.py` |
| logic-resolve-2 | medium | C | UNDO LAST FIX only undoes the last 2-minute burst of a FIX ALL, and a second press falsely says the clips left the media pool | `companion/src/ccsync_companion/resolve_journal.py:60-63,386-420; companion/src/ccsync_comp` |
| logic-resolve-3 | medium | C | Share LUTs offers Resolve's factory LUTs, and a Resolve version mismatch turns that into a permanent offer that copies nothing | `companion/src/ccsync_companion/luts.py:266-315,346-383,451-457; companion/src/ccsync_compa` |
| logic-resolve-4 | medium | C | With no matched project, FIX ALL's default destination is the tree root, which lane A never uploads, yet the dialog reports Fixed | `companion/src/ccsync_companion/fixer.py:296-317,426-442; companion/src/ccsync_companion/po` |
| logic-resolve-5 | low | C | A relink stopped part way is reported as 'you skipped it, nothing was copied in or relinked', with no undo pointer | `companion/src/ccsync_companion/fixer.py:1449-1464; companion/src/ccsync_companion/popup.py` |

**logic-admin** (7)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| logic-admin-1 | medium | C | The "fleet-wide stop expired" alert can never fire | `dashboard/src/ccsync_dashboard/alerts.py:1409 ; dashboard/src/ccsync_dashboard/db.py:8201` |
| logic-admin-2 | medium | C | The halt's release time reads "releases itself 0s ago", on every page | `dashboard/templates/partials/fleet_halt_banner.html:13 ; dashboard/templates/partials/flee` |
| logic-admin-3 | medium | C | An expired fleet stop leaves a banner on every page that nothing in the UI can clear | `dashboard/templates/partials/fleet_halt_banner.html:18-25 ; dashboard/templates/partials/f` |
| logic-admin-4 | medium | C | After a restore, the recovery page still shows the same files as missing and invites the same restore again | `dashboard/src/ccsync_dashboard/recovery.py:259-283, 355-490, 874-880 ; dashboard/templates` |
| logic-admin-5 | low | C | When only "changed" files exist, the restore form's defaults are refused, and the confirm quotes the wrong count | `dashboard/templates/partials/recovery.html:180-200 ; dashboard/src/ccsync_dashboard/recove` |
| logic-admin-6 | low | C | The Packages page tells a customer's admin to run tools\ship.cmd, a vendor-only script | `dashboard/templates/partials/admin_packages.html:71,137,259-261,553-558 ; dashboard/src/cc` |
| logic-admin-7 | low | C | Cancelling a pinned job says "the computer running it" will stop it, but this server runs it | `dashboard/src/ccsync_dashboard/ui.py:3474-3478 ; dashboard/src/ccsync_dashboard/db.py:1108` |

**logic-alerts** (9)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| logic-alerts-1 | medium | C | A mount a site deliberately left OFF raises a permanent 'check the bind mounts' warning | `dashboard/src/ccsync_dashboard/notices.py:720-746 (_check_feature_mounts); ytdl.py:651-654` |
| logic-alerts-2 | medium | C | versions_behind counts staged/feed builds, so a computer on the CURRENT build is told to UPDATE NOW | `dashboard/src/ccsync_dashboard/alerts.py:2379-2427 (_check_versions_behind)` |
| logic-alerts-3 | medium | C | platform_channel_stale says 'build it' when the Mac build already exists staged, and names repo commands on customer sites | `dashboard/src/ccsync_dashboard/alerts.py:2986-3055 (_check_platform_channel_stale)` |
| logic-alerts-4 | medium | C | Replies to all-clear/fallback server-check emails are refused and leave a warn card with an impossible fix | `dashboard/src/ccsync_dashboard/triage.py:733 and 522-525; triage_mail.py:500-506, 426-439` |
| logic-alerts-5 | low | C | Invariant 9 is OK when any snapshot task exists, checks the dashboard's dataset, and its fix names the tree | `dashboard/src/ccsync_dashboard/invariants.py:627-666, 1102-1112` |
| logic-alerts-6 | low | C | Invariants collector job is permanently amber ('2 not checked here') | `dashboard/src/ccsync_dashboard/invariants.py:1376-1386 (_note); db.collector_health` |
| logic-alerts-7 | low | C | file_move_detected with zero followers keeps its card for 7 days | `dashboard/src/ccsync_dashboard/notices.py:1602-1628 (_file_move_followed)` |
| logic-alerts-8 | low | P | Disk alerts for a computer that syncs nothing name an impossible consequence and fix | `dashboard/src/ccsync_dashboard/alerts.py:1440-1467; notices.py:593-610` |
| logic-alerts-9 | low | C | Reply poll caps at the newest 20 BEFORE skipping handled messages; older replies are never read | `dashboard/src/ccsync_dashboard/triage_mail.py:628-640 (poll)` |

**logic-broll-music** (4)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| logic-broll-music-1 | medium | C | After an index rebuild, removing one client-folder clip can delete a second clip, and adding a clip can be refused as 'already' | `broll/web/app/client_folders.py:675 (remove_item), :689 (set_note), :619-627 (add_items); ` |
| logic-broll-music-2 | medium | C | settle_intents calls a stand-in 'the real original' for any heavy original of 1080 lines or less | `companion/src/ccsync_companion/broll_standins.py:663-673 (_what_landed); broll/indexer/bro` |
| logic-broll-music-3 | medium | C | Cancelling a music batch mid-upload leaves tracks that stay searchable but have no audio and cannot be retried | `music/web/musicweb/ingest_batches.py:1250-1255 (release cancelled), :930-1000 (write_item_` |
| logic-broll-music-4 | low | C | The editor's own 'open' check of a client link is counted as the client opening it | `broll/web/app/routes_share.py:171-189 (record_view); broll/web/static/clientfolders.js:285` |

**logic-ytdl-jobs** (6)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| logic-ytdl-jobs-1 | medium | C | The scheduler judges a computer by its LAST report however old it is, so a machine switched off weeks ago keeps a job schedulable/transient for ever | `dashboard/src/ccsync_dashboard/jobs.py:610 (fleet_facts), :974-1112 (explain), :722-747 (f` |
| logic-ytdl-jobs-2 | medium | C | An editor's own requester-first download blocks that editor's queued searches while the server worker has nothing to do | `ytdl/web/ytdlweb/db.py:348 (BUSY), :930-975 (claim_next_job), :537 (busy_job); routes_api.` |
| logic-ytdl-jobs-3 | medium | C | The page never shows why the editor's computer gave a download back: handed_back_reason has no consumer, and claim refusals set no reason | `ytdl/web/static/app.js:2593, :2668-2750; companion/src/ccsync_companion/ytdl_executor.py:2` |
| logic-ytdl-jobs-4 | low | C | The download-terms toast sends the editor to a right-click menu item that no longer exists (CR-88) and ignores the companion's correct route | `ytdl/web/static/app.js:2406-2411; companion ytdl_executor.py:365, ui_copy.py:56` |
| logic-ytdl-jobs-5 | low | C | The collector's lease sweep ignores DASH_JOBS_COOLDOWN_SECONDS | `dashboard/src/ccsync_dashboard/db.py:8914; compare api.py:10970-10972` |
| logic-ytdl-jobs-6 | low | C | The why page says a lower-ranked machine is 'offered the job anyway once it has waited 60s' for forced, targeted and old jobs that have no grace | `dashboard/src/ccsync_dashboard/jobs.py:1052-1059` |

**logic-release** (7)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| logic-release-1 | high | C | Documented key rotation signs the overlap release with the NEW key, and the REL-7 refusal tells the operator to override it | `docs/RELEASE.md:675-678; tools/release_key.py:109-121; tools/publish_feed.py:1250-1265` |
| logic-release-2 | medium | C | Retracting the current build makes every policy=current site auto-publish a newer build the vendor only STAGED | `tools/publish_feed.py:723-743; dashboard/src/ccsync_dashboard/release_feed.py:648-674` |
| logic-release-3 | medium | C | Staged vendor records (and a pointer moved back) count as 'offered': invariant 11, the versions-behind alert and the HEALTH box cry wolf | `dashboard/src/ccsync_dashboard/release_feed.py:909-937; invariants.py:812; alerts.py:2379;` |
| logic-release-4 | medium | C | Dashboard auto-update ignores the channel's current pointer, so a STAGED dashboard bundle is applied at every policy=current site | `dashboard/src/ccsync_dashboard/dashboard_update.py:1652-1692; release_feed.py:1078-1135` |
| logic-release-5 | medium | C | publish_latest's SYS-7 ordering gate judges a requires_dashboard that sign_release strips, and swallows the note that says so | `tools/publish_latest.py:428-445, 80-89, 276-282; tools/sign_release.py:379-389` |
| logic-release-6 | medium | C | 'Re-run with --make-current when you are ready' is a no-op: the re-run skips the version as already published | `tools/publish_latest.py:491-494, 446-450` |
| logic-release-7 | low | C | --allow-older is documented as the rollback, but publish_latest cannot roll the feed back and no tool moves the pointer alone | `docs/RELEASE.md:182; tools/publish_latest.py:452-460; tools/publish_feed.py:1323-1325` |

**logic-cards** (9)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| logic-cards-1 | medium | C | Anyone in a shared episode can close it under the other person, and the confirm names the person pressing the button | `dashboard/src/ccsync_dashboard/cards_pool.py:459-463; cards_landing.py:117-125, 243-245` |
| logic-cards-2 | medium | C | An always-on companion keeps its editor 'in' their last episode indefinitely, so the idle release never fires | `dashboard/src/ccsync_dashboard/cards_pool.py:359-379; cards_tunnel.py:143-157` |
| logic-cards-3 | medium | C | Routing is by person, not by computer: opening a second episode (even one still loading) moves your Resolve, and one editor's two machines cannot be in two episodes | `dashboard/src/ccsync_dashboard/cards_landing.py:224; cards_pool.py:324-357; cards_tunnel.p` |
| logic-cards-4 | low | C | The landing page ignores ?want=, and 'carry on' only remembers episodes you personally built | `dashboard/src/ccsync_dashboard/cards_landing.py:179,188,232,373; dashboard/templates/cards` |
| logic-cards-5 | low | C | The landing footer says closing is admin-only, next to a refusal telling non-admins to close an idle one themselves | `dashboard/templates/cards_landing.html:116-120; cards_landing.py:243-245` |
| logic-cards-6 | low | C | With the Cards mount absent, the tunnel tells agents to set DASH_CARDS_SERVER_URL instead of giving the mount's reason | `dashboard/src/ccsync_dashboard/cards_tunnel.py:184-186, 247-259; companion/src/ccsync_comp` |
| logic-cards-7 | low | P | An episode build has no timeout: a hung vault read is [ OPENING ] forever, refreshing every 3 s and holding a seat | `dashboard/src/ccsync_dashboard/cards_pool.py:415-418, 467-486; cards_landing.html:31-39` |
| logic-cards-8 | low | C | /api/v1/health reports a Cards agent as attached if one has ever pushed, forever | `dashboard/src/ccsync_dashboard/cards.py:777, 783, 790` |
| logic-cards-9 | low | C | The Cards role calls a routine stale-result refusal 'the dashboard is discarding this computer's pushes' | `companion/src/ccsync_companion/timeline_cards_role.py:940-965, 1039-1046` |

### Wave 3 - UI

**ui-dash-main** (10)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| ui-dash-main-1 | medium | C | Ticking a project in the sidebar folds the whole project tree back up, and on a phone closes the sheet | `dashboard/templates/partials/sidebar.html:21-22; dashboard/templates/base.html:76-86` |
| ui-dash-main-2 | medium | C | The DUI-6 'refusal next to the button' mover does nothing on the outerHTML panels it was written for | `dashboard/static/htmx_errors.js:225-256` |
| ui-dash-main-3 | medium | C | The project page's 10 s poll wipes what an admin is typing into MOVE and SHARE A FOLDER | `dashboard/templates/project.html:11-13; dashboard/templates/partials/project_detail.html:1` |
| ui-dash-main-4 | medium | C | The fix-root computer picker drops ?as=, so an admin checking an editor's second computer lands back on their own queue | `dashboard/templates/partials/fix_root.html:18-21; dashboard/src/ccsync_dashboard/ui.py:620` |
| ui-dash-main-7 | medium | C | [ UNDO ] on a TICKED row takes a project off computers with no confirm, unlike every other untick | `dashboard/templates/partials/plan_changes.html:47-50; dashboard/src/ccsync_dashboard/ui.py` |
| ui-dash-main-8 | medium | C | The sidebar's projects sheet shuts under a phone user every 30 s | `dashboard/templates/fleet.html:6-7 (every page's sidebar poll); dashboard/templates/partia` |
| ui-dash-main-5 | low | C | On a phone the fix-root computer chips run off the screen and cannot be reached | `dashboard/templates/partials/fix_root.html:16-24` |
| ui-dash-main-6 | low | C | The project-roots 30 s poll closes an open [ BROWSE ] folder picker and resets a changed select | `dashboard/templates/fleet.html:75; dashboard/templates/partials/project_roots.html:21-35` |
| ui-dash-main-9 | low | C | [ UNTICK ] in the sync queue re-renders the panel as the person's view and drops the 'safe to close' line | `dashboard/src/ccsync_dashboard/ui.py:1457-1459; dashboard/templates/partials/my_queue.html` |
| ui-dash-main-10 | low | C | '(s)' plurals survive on the fleet and transfers pages, against UX-10's own rule | `dashboard/templates/partials/fleet_grid.html:78,80,239,271; dashboard/templates/partials/t` |

**ui-dash-admin** (14)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| ui-dash-admin-1 | high | C | A project name with an apostrophe breaks the ARCHIVE confirm, so the project archives with no question (a crafted folder name can run script) | `dashboard/templates/admin_assignments.html:166` |
| ui-dash-admin-2 | medium | C | The fleet halt says it "releases itself 0s ago" for the whole time it is active | `dashboard/templates/partials/fleet_halt.html:38; dashboard/templates/partials/fleet_halt_b` |
| ui-dash-admin-3 | medium | C | The Users and Packages polls wipe typed input and orphan in-flight writes (undoing DUI-4's busy labels) | `dashboard/templates/admin_users.html:16, :29-30; dashboard/templates/admin_packages.html:1` |
| ui-dash-admin-4 | medium | C | OTHER VERSIONS HELD (the rollback list) collapses every 30 s, and PREVIOUS STOPS every 60 s | `dashboard/templates/partials/admin_packages.html:459; dashboard/templates/partials/fleet_h` |
| ui-dash-admin-5 | medium | C | Results and refusals on Alerts, Recovery and Protection render far above the button that caused them | `dashboard/templates/partials/admin_alerts.html:15-16; recovery.html:16-17; protection.html` |
| ui-dash-admin-6 | medium | C | Site Settings SAVE answers at the top of a long page, and a stale 'saved' stays beside a later error | `dashboard/templates/admin_settings.html:14-15, :183; dashboard/static/site_settings.js:31-` |
| ui-dash-admin-7 | medium | C | HEALTH says every check answered when a source raised | `dashboard/src/ccsync_dashboard/ui.py:1866-1941; dashboard/templates/admin_health.html:113-` |
| ui-dash-admin-8 | low | C | An Android validation refusal throws away the pasted fingerprints | `dashboard/src/ccsync_dashboard/android.py:180-187, :129-148` |
| ui-dash-admin-9 | low | C | SMTP SET PASSWORD / CLEAR report success when the environment password is the one in use | `dashboard/templates/partials/admin_alerts.html:177-188; ui.py:2247-2267; alerts.py:503-513` |
| ui-dash-admin-10 | low | C | Protection refuses today's date as 'in the future' before 08:00 in UTC+8 | `dashboard/src/ccsync_dashboard/protection.py:800-803` |
| ui-dash-admin-11 | low | C | A minted report token runs off a phone screen | `dashboard/templates/partials/minted_secret.html:26; dashboard/static/style.css:442, :1900` |
| ui-dash-admin-12 | low | C | Raw ISO timestamps and raw seconds beside humanised times | `partials/admin_report_tokens.html:54-55; fleet_halt.html:29; admin_alerts.html:157-218; re` |
| ui-dash-admin-13 | low | C | The restore confirm counts only missing files, so it can ask 'Copy 0 file(s)' | `dashboard/templates/partials/recovery.html:167-178` |
| ui-dash-admin-14 | low | C | Button wording that contradicts the action: UNDO LAST IMPORT, and a rollback that asks to 'Update' | `dashboard/templates/admin_settings.html:251; dashboard/static/dashboard_update.js:157-166` |

**ui-dash-static** (9)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| ui-dash-static-1 | medium | C | Refusal-banner mover (DUI-6) and chip tabindex pass never work on outerHTML-swapped panels | `dashboard/static/htmx_errors.js:219-243 (and :167-170)` |
| ui-dash-static-2 | medium | C | Every 4xx refusal shown as 'THIS PAGE HAS STOPPED UPDATING'; server detail discarded; 403 called an ended session | `dashboard/static/htmx_errors.js:62-79; templates/partials/sidebar.html:21` |
| ui-dash-static-3 | medium | C | Settings [ UNDO ] confirm quotes a stale history entry after an in-page save, then reverts the save just made | `dashboard/static/site_settings.js:737-771, 790-797; dashboard/src/ccsync_dashboard/setup_r` |
| ui-dash-static-4 | medium | C | Refused dashboard update/rollback reason is written then immediately erased by reloadPanel | `dashboard/static/dashboard_update.js:157-160, 62-83` |
| ui-dash-static-5 | low | C | --muted text below AA contrast; drawer close button and help notes at 1.8:1 | `dashboard/static/style.css:12, 441, 184, 2005` |
| ui-dash-static-6 | low | C | Offline page [ RETRY ] goes to / instead of reloading the page that failed | `dashboard/templates/offline.html:15; dashboard/static/sw.js:93-104` |
| ui-dash-static-7 | low | C | Stale banner covers error toasts and page bottom, ignores safe-area inset | `dashboard/static/style.css:1878-1887 vs 1163-1170` |
| ui-dash-static-8 | low | C | [ COPY ] sticks on '[ COPIED ]' after a double click and gives no feedback without the clipboard API | `dashboard/static/copy_value.js:29-33, 44-50` |
| ui-dash-static-9 | low | C | AI provider pin select and CLI-provider checkbox not rolled back when the PUT fails | `dashboard/static/site_settings.js:117-126, 131-145` |

**ui-broll-web** (18)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| ui-broll-web-1 | medium | C | Share page detail view overflows a phone horizontally when a clip has a long camera filename | `broll/web/static/share.css:62-64; broll/web/static/share.js:283` |
| ui-broll-web-3 | medium | C | Detail-view hotkeys hijack Enter/Space on focused buttons, including buttons in drawers open over the detail view | `broll/web/static/app.js:1528-1586 (onKeydown), app.js:962` |
| ui-broll-web-4 | medium | C | Folder rail sticks under the sticky header: --header-h is a constant 88 px, the real header is 98-230 px | `broll/web/static/style.css:42, style.css:1012-1026` |
| ui-broll-web-5 | medium | C | '+ new folder' from the popover shows success even when adding the clip fails | `broll/web/static/clientfolders.js:205-224, 588-600` |
| ui-broll-web-2 | low | C | Share page brand line overflows a phone when the site org name is long | `broll/web/static/share.html:27; share.js:174; style.css:168-171` |
| ui-broll-web-6 | low | C | A send to Resolve keeps its SYNCING label and locked buttons across clip changes | `broll/web/static/app.js:1666-1818, 1120-1199` |
| ui-broll-web-7 | low | C | Share page instructs phone clients to hover, and scrubbing is mouse-only | `broll/web/static/share.html:36, 63-65; sprite.js:101-107` |
| ui-broll-web-8 | low | C | Share grid captions clip long filenames mid-word and slice a third line | `broll/web/static/share.css:37-45` |
| ui-broll-web-9 | low | C | Share prev/next look enabled at the first/last clip | `broll/web/static/share.js:273-274; style.css:503-512` |
| ui-broll-web-10 | low | C | Transient share-page load errors are headed 'This link is not available' | `broll/web/static/share.js:160-167` |
| ui-broll-web-11 | low | C | Client-folder popover: stale counts, unclamped off-screen placement, invisible '+' for keyboard and touch | `broll/web/static/clientfolders.js:556-586, 607-615; style.css:1562-1576, 1692-1705` |
| ui-broll-web-12 | low | C | Caption edits appear reverted when the editor reorders right after typing; a caption save has no success feedback | `broll/web/static/clientfolders.js:410-423, 450-455` |
| ui-broll-web-13 | low | C | Two hamburgers and two gears in the b-roll header, meaning different things | `broll/web/static/index.html:68-69; dashboard/templates/partials/topbar.html:27-29, 223` |
| ui-broll-web-14 | low | C | The ingest Running list prints raw state tokens such as proxies_live | `broll/web/static/ingest.js:1453 (vs ingest.js:1669)` |
| ui-broll-web-15 | low | C | Ingest live controls offer Pause, Resume and Start now at once, whatever the state | `broll/web/static/index.html:253-257; ingest.js:1463-1477` |
| ui-broll-web-16 | low | C | Mode fallback toast says 'keyword' while the page switches to Hybrid | `broll/web/static/app.js:709-719` |
| ui-broll-web-17 | low | C | Long error toasts vanish after 5 s, including the tray-unreachable self-test instructions | `broll/web/static/app.js:154-159, 1735-1742` |
| ui-broll-web-18 | low | C | The b-roll SPA has no narrow layout: 718 px wide at a 390 px viewport | `broll/web/static/style.css (no @media; 468-476, 1013, 542-546, 705)` |

**ui-music-ytdl-web** (10)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| ui-music-ytdl-web-1 | medium | C | The YouTube page has no phone layout: its search row is 365 px wider than the screen | `ytdl/web/static/style.css:391 (.search-bar, no wrap; no @media rule in the file); ytdl/web` |
| ui-music-ytdl-web-2 | medium | C | Clicking a clip's title to watch it on YouTube also ticks or unticks it for download | `ytdl/web/static/app.js:2011-2023 (card())` |
| ui-music-ytdl-web-3 | medium | C | The attestation lock on SEARCH / GET LINKS is undone by the project loader and by every submit | `ytdl/web/static/app.js:3098-3108, 752-753, 2222, 2293` |
| ui-music-ytdl-web-4 | medium | C | The music player pane is a fixed 150 px box that cuts off its own buttons and messages | `music/web/static/style.css:752 (.pane.open height 150px, overflow hidden); music/web/stati` |
| ui-music-ytdl-web-5 | medium | C | Rail filters, sort and '[ include them ]' silently throw away the typed search while the box still shows it | `music/web/static/app.js:694-700, 906, 910-913, 927-931, 623` |
| ui-music-ytdl-web-6 | low | C | A length filter that matches nothing says 'The library is empty' | `music/web/static/app.js:547-564` |
| ui-music-ytdl-web-7 | low | C | One failed stats or facets call at load kills the whole music page, with a raw status line | `music/web/static/app.js:873-946 (init)` |
| ui-music-ytdl-web-8 | low | C | Music's sticky header is taller than the --header-h the rail parks under, and on a phone it takes a third of the screen | `music/web/static/style.css:32, 138, 400-414` |
| ui-music-ytdl-web-9 | low | C | After a music batch finishes, the panel still says 'Running' and offers Run again on the same staged tracks | `music/web/static/ingest.js:994-1054, 910-921, 928` |
| ui-music-ytdl-web-10 | low | C | YouTube's Recent searches list prints the raw phase enum that the progress strip translates | `ytdl/web/static/app.js:2791 vs PHASE_LABEL at 32-44` |

**ui-comp-windows** (11)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| ui-comp-windows-1 | medium | C | confirm_dialog never wraps its body, so the WIRED TO THE SERVER confirm is about 2,280 px wide with its buttons off-screen | `companion/src/ccsync_companion/popup.py:2419 (also notice_dialog :2477; callers settings_w` |
| ui-comp-windows-2 | medium | C | Nothing ticked is drawn as a red blocking line in Settings and pulls HELP to the top | `companion/src/ccsync_companion/settings_window.py:199, :96, :1626; tray.py:170` |
| ui-comp-windows-3 | medium | C | The licence dialog shows raw EULA.md, including the internal 'DRAFT FOR COUNSEL / TODO(legal)' comment | `companion/src/ccsync_companion/app.py:6200-6240; popup.py:2617-2627; assets/EULA.md:1-15` |
| ui-comp-windows-4 | low | C | The Settings jump strip overflows the 720 px window, so ADVANCED is cut and HELP is invisible; Lines use a fixed wraplength of 660 | `companion/src/ccsync_companion/settings_window.py:1769-1823, :1761-1762, :1830` |
| ui-comp-windows-5 | low | C | Settings says 'disconnected ... plug it back in' about a drive that is plugged in but not answering or mounted in the wrong place | `companion/src/ccsync_companion/settings_window.py:1356-1361 (vs tray.py:2478-2488, drive_r` |
| ui-comp-windows-6 | low | C | The local-stop release is 'Clear the sync stop' in the tray but the retired 'START SYNCING AGAIN' in Settings | `companion/src/ccsync_companion/settings_window.py:1410-1412; tray.py:3130-3138, :2069-2073` |
| ui-comp-windows-7 | low | C | The licence refusal in the tooltip is cut before its action; inside Settings it tells the reader to open Settings | `companion/src/ccsync_companion/tray.py:4234; settings_window.py:218-234; eula.py:425-434` |
| ui-comp-windows-8 | low | C | The fixer popup's 52-character destination combobox hides the default destination's last folder | `companion/src/ccsync_companion/popup.py:1160-1161; fixer.py:454` |
| ui-comp-windows-9 | low | P | The fixer popup's width is unbounded; with a long path the right-aligned FIX ALL bar goes off-screen | `companion/src/ccsync_companion/popup.py:1148-1152, :1003, :1170-1186` |
| ui-comp-windows-10 | low | C | Fleet-jobs toggles: re-enabling omits the restart note, and the kind sentence is ungrammatical | `companion/src/ccsync_companion/settings_window.py:1099-1106, :1196-1200` |
| ui-comp-windows-11 | low | C | Muted text fails contrast (3.98:1), no neon_button shows keyboard focus, and confirm/notice dialogs have no keys | `companion/src/ccsync_companion/theme.py:19,23,430-452; popup.py:2393-2453` |

**ui-onboarding** (12)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| ui-onboarding-1 | medium | C | Fixed 660x560 window clips CLOSE / BACK / log path off finish and install pages | `onboarding/onboard.py:71, 1333-1381, 832-953` |
| ui-onboarding-2 | medium | C | Licence page shows raw draft EULA: TODO(legal) HTML comment, markdown, 14 em dashes | `onboarding/onboard.py:365; onboarding/steps.py:680-698; onboarding/assets/EULA.md:1-19` |
| ui-onboarding-3 | medium | C | Interrupted-install page and close warning always claim 'no P drive', even for base installs and Macs; resume defaults to the editor radio | `onboarding/onboard.py:279-286; onboarding/steps.py:2164-2174` |
| ui-onboarding-4 | medium | C | winget Tailscale install: stale NOT INSTALLED, green on failure, and join check cannot find new tailscale.exe | `onboarding/onboard.py:635-639, 681-696; onboarding/steps.py:943-969` |
| ui-onboarding-5 | low | C | Finish page copy contradicts itself and the install | `onboarding/onboard.py:1371-1378, 1105-1133; onboarding/steps.py:2094-2097` |
| ui-onboarding-6 | low | C | Welcome says 'nothing else' is needed but next page requires dashboard URL | `onboarding/onboard.py:417-418, 427-428 vs 500` |
| ui-onboarding-7 | low | C | Role-override note uses internal 'base'/'editor' instead of the radio labels | `onboarding/onboard.py:857-862` |
| ui-onboarding-8 | low | C | No Enter key, no initial focus, no visible focus ring | `onboarding/onboard.py (no bind/focus calls); companion/src/ccsync_companion/theme.py:436-4` |
| ui-onboarding-9 | low | C | Muted text 3.98:1 and field borders 1.86:1 contrast | `companion/src/ccsync_companion/theme.py:19,23; onboarding/onboard.py:474, 500, 939-945` |
| ui-onboarding-10 | low | C | Wrong-account refusal says sign in as the already-signed-in account | `onboarding/steps.py:2210-2215` |
| ui-onboarding-11 | low | C | Bootstrap end banner prints hand-run 'remaining manual steps' into wizard installs | `installer/windows_bootstrap.ps1:2446-2469` |
| ui-onboarding-12 | low | P | VERIFY & CONTINUE / CHECK CONNECTION not disabled while in flight | `onboarding/onboard.py:698-733, 759-821` |

**ui-copy** (7)

| Id | Sev | Conf | Title | Where |
|---|---|---|---|---|
| ui-copy-1 | medium | C | The fleet-halt alert sends the admin to a button and a page that do not exist | `dashboard/src/ccsync_dashboard/alerts.py:1402 (and :1415); dashboard/src/ccsync_dashboard/` |
| ui-copy-2 | medium | C | 'Settings, Diagnostics' is not a page, and the notice links land on an empty div | `dashboard/src/ccsync_dashboard/notices.py:241,290,315,1001,1515; db.py:3390-3399,3506-3508` |
| ui-copy-3 | low | C | More next-action sentences name buttons or pages by names they do not have | `dashboard/src/ccsync_dashboard/alerts.py:3515; notices.py:392; collector.py:656; invariant` |
| ui-copy-4 | low | C | Companion copy outside the vocabulary scan still says 'machine', 'lane B' and 'parked' to editors and admins | `companion/src/ccsync_companion/ytdl_executor.py:349-386; file_moves.py:473,483,517; proxy_` |
| ui-copy-5 | low | C | The companion and the dashboard give two different routes to Copy diagnostics | `companion/src/ccsync_companion/ui_copy.py:40; dashboard/src/ccsync_dashboard/health.py:534` |
| ui-copy-6 | low | C | The ' -- ' stand-in em dash is banned in templates but ships in dashboard Python copy | `dashboard/src/ccsync_dashboard/ui.py:847,3315,1265,773; api.py; dashboard_update.py; relea` |
| ui-copy-7 | low | C | 'Sign in' everywhere except the topbar's [ LOGIN ] / [ LOGOUT ] / [ LOGOUT ALL ] | `dashboard/templates/partials/topbar.html:150,162,164,236,238; partials/admin_sessions.html` |

## The disprove pass on the 124 mediums (2026-09-25)

Eighteen adversarial Opus verifiers (`VERIFIER_BRIEF.md`, verdict files in `verifiers/`, merged in `verdicts.json`) judged every medium against HEAD `63d4290`: **92 CONFIRMED medium, 30 real but DOWNGRADED to low, 2 REFUTED, 0 upgraded, 0 uncertain.** Not fixed: the fix wave that followed took the eleven highs (plus `bug-comp-core-2`, the same mechanism as `bug-comp-core-1`).

| Id | Verdict | Title | Reason |
|---|---|---|---|
| bug-comp-media-2 | REFUTED | Companion job gate ignores the dashboard's per-kind idle floor (60 s for audio-extract/peaks) and the base-rig exemption | This is intended. docs/CONFIG.md says the dashboard's per-kind floor and the machine's jobs_idle_seconds must BOTH pass, and the key 'is never loosened by the server's'. The documented ways past the companion's gate are the tray volunteer and forced (--now) jobs, both implemented in _gate. The base-rig rank is a 60 s grace period, not a filter, so the job falls through to other machines. |
| logic-ytdl-jobs-2 | REFUTED | An editor's own requester-first download blocks that editor's queued searches while the server worker has nothing to do | Mechanism real but documented intent: docs/YTDL_TERMS_AND_QUEUE.md section 4 states a locally downloading job is BUSY for its own editor and invisible to the worker 'which is both of the properties it had before'; claim_next_job's docstring scopes the lease clause to not queuing the FLEET. Same one-job-per-editor rule as a server download; a feature request, not a defect. |
| bug-comp-broll-3 | DOWNGRADE | Insert's download-done branch is unreachable: finished fetch jobs leak, and the stand-in row is never rewritten before import | It is true that is_file() makes the DONE branch unreachable and the DONE job leaks. But the next poll still imports as a stand-in with upgrade_rel, so start_proxy_upgrade sets upgrade=pending. settle_intents also settles the row within 120 s, by geometry or by job_state, and job_state answers 'done' because of the leak itself. What remains: a stuck row only with no geometry plus a restart inside 120 s, a small per-clip registry leak (not counted toward the cap), and one misleading 'is the share  |
| bug-comp-core-4 | DOWNGRADE | Sign-in /verify path carries no arch and no upgrade_none_reason: offers a wrong-CPU build and clears a standing refusal | The mechanism is real: verify sends no arch, VerifyIn does not declare it, api_verify passes no withheld, and sign_in feeds {upgrade: X} to note_report_response, which clears a refusal. But it only fires on a rare interactive sign-in. In (a) the swap guard rolls the wrong-CPU binary back and the next report, which carries arch, withdraws the offer. In (b) the only loss is a diagnostic chip and an alert. Self-correcting or display-only, so low. |
| bug-comp-media-3 | DOWNGRADE | out_stem is used as a filename unvalidated: absolute stems write anywhere, '/' or ':' in a multicam name land in the wrong place | out_stem is appended unchecked after job_paths.resolve has guarded the other inputs, so the escape is real. But POST /api/v1/jobs is admin-only and the other submitter is the in-process Cards engine, so 'write anywhere' needs an admin. The realistic case, a multicam name with '/' or ':', is shared with MulticamPipeline's own standalone engine (library_engine.py:3154 uses the same unsanitised name), not introduced by the fleet path. |
| bug-comp-ui-2 | DOWNGRADE | macOS: async menu rebuild landing while the menu is open drops its weak delegate, leaving ui_state.menu_open set forever | The mechanism holds: _apply_menu_now unconditionally rebinds _targets/_delegate, and NSMenu's delegate is weak, so a rebuild during tracking loses menuDidClose_. But the window is milliseconds, it is macOS-only and has not been run on a Mac, and it is not 'forever': the next open and close of the new menu clears ui_state.menu_open. That makes it a rare stall that clears itself. |
| bug-comp-ytdl-3 | DOWNGRADE | A mid-job 403 (sign-out, expired or revoked identity) never stops the executor; it downloads the whole job unrecorded while the server re-downloads it | The mechanism is real: heartbeat discards the status, _call raises only on a transport failure or 410, require_fleet_caller answers 403 before the lease check, and sign_out does not stop the executor. The outcome is wasted work (a duplicate download of the remaining clips under the same [id] names, with lane A already stopped by the sign-out), not data loss. The triggers (a sign-out, token expiry or user deletion in the middle of a local job) are rare. |
| bug-dash-cards-jobs-3 | DOWNGRADE | Pinned jobs wait for ever, with no alert, whenever no episode happens to be open | The mechanism holds (tick returns while no engine exists; DDIAG-6 is silent for mounted+running+unclaimed), but the jobs are delayed, not lost. The pool has no idle release, and the drain resumes as soon as any episode opens, since any engine runs any pinned job. The outputs (proxy/audio/peaks) are consumed only by open Cards pages. The real cost is a jobs page that says in hand. |
| bug-dash-diag-2 | DOWNGRADE | check_failed alerts can never recover, so they stay open forever and a later failure is reported as weeks old | Mechanism holds: checked_kinds comes from ALERT_KINDS, CHECK_FAILED is outside it, so check_failed never gets a .ok and _is_open stays True. But the UI's open counts come from the live scan (alerts.open_counts(findings)), not the ledger, and a later failure is still mailed (SEV_ERROR daily repeat). The only losses are the recovery message and the digest mislabelling a new failure as a 49-day repeat. |
| bug-music-ytdl-3 | DOWNGRADE | The first fleet drop into a new library refuses itself from item 2 onward | Real: the tracks row is written before upload, and share_root_ready then samples only unlanded rows in a brand-new library and returns 503 share_not_ready. It is narrow: first drop into an empty library only, only while item 1's upload has not landed by the end of item 2's embedding, the 503 is retried with backoff for about 20 min, and success is cached. It fails permanently only with uploads paused or very slow. |
| bug-ops-2 | DOWNGRADE | ship.cmd -Resume cannot resume past the publish and tells the operator to bump the version | Mechanism holds: ship.ps1's already-published probes (:423-470) run unconditionally after -Resume sets ResumeFrom, so -Resume after journal step publish/current always exits 1 with 'bump VERSION'. Low because it is operator release tooling, the same exit-3 message names MAKE CURRENT in Settings as the first path, and no fleet state is harmed. |
| bug-ops-3 | DOWNGRADE | The recall command publish_latest.py prints does not parse | Real: the printed recall line uses --retract --kind/--platform/--version, but --retract takes one KIND/PLATFORM/VERSION value and --feed-dir/--github-repo are required for --github-upload. Low because it fails instantly and loudly, the correct form is in publish_feed's docstring and --help, and it cannot retract the wrong thing. |
| logic-admin-3 | DOWNGRADE | An expired fleet stop leaves a banner on every page that nothing in the UI can clear | The mechanism holds: the expired banner persists for everyone and the admin panel's expired branch has no release/acknowledge control, so only set_fleet_halt(active=False) via JSON or triage mail clears it. But the banner is truthful, blocks nothing and risks no data. It is a stale notice that cannot be dismissed. |
| logic-admin-4 | DOWNGRADE | After a restore, the recovery page still shows the same files as missing and invites the same restore again | The preview correctly still reports the files as missing from the live folder. By design (recovery.py:872-880) a restore copies into .restored-<ts> for the owner to move by hand, and a repeat restore is an additive duplicate that the module says costs only disk. The page names the folder and lists past restores. Real UX gap, no data loss. Related but distinct: bug-dash-diag-3. |
| logic-alerts-4 | DOWNGRADE | Replies to all-clear/fallback server-check emails are refused and leave a warn card with an impossible fix | The mechanism holds. Every report and fallback is sent with Reply-To, and a report that offers no actions carries no CCT reference. A reply from an authenticated owner to one of these is refused under 'reference', which opens a triage_reply_refused warn whose fix cannot be followed. It is low because it needs the owner to reply to an email with no actions, nothing is changed or lost, the card can be dismissed and closes under CR-320's rule, and the silence is the designed refusal behaviour, only |
| logic-cards-3 | DOWNGRADE (duplicate of bug-dash-cards-jobs-2) | Routing is by person, not by computer: opening a second episode (even one still loading) moves your Resolve, and one editor's two machines cannot be in two episodes | Mechanisms real (one _where slot per editor so two machines share one engine, contradicting CARDS_TWO_PROJECTS 1a; /cards/open note_visit on a LOADING entry makes engine_for return None during the build - probed). But the harmful half is the same root cause as bug-dash-cards-jobs-2: the laptop page's own polling re-flips _where within seconds, so the detach window is transient unless no page is open (where following the last opened episode is the design rule), and routing on (editor, machine) fi |
| logic-onboarding-3 | DOWNGRADE | windows_upgrade.ps1 sends a licence-only problem to the full reinstall wizard, alongside the companion's own licence dialog | Route is real (windows_upgrade.ps1 launches onboard.exe on a stale EULA while the companion's _licence_watch also prompts, and the -SkipWizard copy is stale). Low because the wizard is the designed consent path, documented safe to re-run and config-merging, and the trigger is a rare hand-run package upgrade across an EULA bump. |
| logic-onboarding-4 | DOWNGRADE | The wizard and both bootstraps still ask for a 'TrueNAS username and password' | Real: five wizard/bootstrap strings still say 'TrueNAS username and password' despite site.server_phrase existing for this. Copy-only, true on every site deployed today, recoverable in one sentence; belongs in the next copy pass for commercial sites. |
| logic-plans-2 | DOWNGRADE | Project page's person-level mode buttons tick the project onto computers that never had it, with no confirm or capacity check | Mechanism true: the mode buttons go through add_selection_for_person and insert rows on every non-wired computer. But docs/UPLOAD_ONLY_TICK.md section 2 documents them as the PERSON's control, like the tick button, so ticking the other computers and flattening a mixed plan is the design. Left: SWITCH TO FULL SYNC has no UX-1 capacity hx-confirm, unlike TICK. |
| logic-release-5 | DOWNGRADE | publish_latest's SYS-7 ordering gate judges a requires_dashboard that sign_release strips, and swallows the note that says so | Verified: sign_release drops requires_dashboard without --emit-kind-extras, publish_latest never passes it and discards stderr on success, and 0 of 99 local channel records carry the field. But the missing customer-side ordering check is the deliberate REL-4/REL-16 overlap policy (sign_release.py:369-389); what remains is an advisory-only gate and a swallowed note. The false refusal cannot fire today (REQUIRES_DASHBOARD 0.7.23 < bundle 0.7.44) and has --allow-behind. |
| logic-sync-truth-3 | DOWNGRADE | Tray says 'downloading N files' and pulses amber for ever when lane C has no connected peer | Real: check_once reports syncing with no peer check and no progress token, and the tray never reads _lane_peer_states. But the named scenarios are covered: NAS Syncthing down raises the dashboard syncthing_reachable alert, a tailnet drop gives transport_offline, which _sync_line renders before the counts, and editor_status turns behind-and-disconnected red. Left: a narrow single-machine Syncthing-only outage where the status is misleading. |
| ui-broll-web-1 | DOWNGRADE | Share page detail view overflows a phone horizontally when a clip has a long camera filename | The mechanism holds: grid-template-columns 1fr is minmax(auto,1fr), and .video-meta .title has no overflow-wrap, so an unbroken filename widens the column on a phone. The effect is horizontal scrolling with the player still reachable. It is cosmetic and the same class as ui-broll-web-2/-8, which the hunter filed as low. |
| ui-broll-web-4 | DOWNGRADE | Folder rail sticks under the sticky header: --header-h is a constant 88 px, the real header is 98-230 px | Confirmed that --header-h is a constant 88px with no writer anywhere, while the sticky #app-header (z-index 10) is taller, so the rail's head row is covered while scrolled. It clears on scrolling back up. Layout nuisance only, no loss of function. |
| ui-broll-web-5 | DOWNGRADE | '+ new folder' from the popover shows success even when adding the clip fails | The mechanism holds: the afterCreate items POST is awaited with no catch, which gives an unhandled rejection, no error toast and no popover re-render. The trigger is narrow, because the create just succeeded on the same session. The missing 'Added to' toast and the empty folder are visible and recoverable, with no data lost. |
| ui-comp-windows-1 | DOWNGRADE | confirm_dialog never wraps its body, so the WIRED TO THE SERVER confirm is about 2,280 px wide with its buttons off-screen | Partly refuted. The body Label has no wraplength and the WIRED body is one 330-character line, but the buttons are not pushed off-screen. Tk clamps a toplevel to wm maxsize, which defaults to the screen size. A replica probe requested 2289 px and got 516 (= maxsize), and the OK button sat inside the window. What gets clipped is the Label, so the dialog is screen-wide and its sentence is cut off before the end, while CANCEL and OK stay reachable. Worth adding a wraplength, but low. |
| ui-comp-windows-3 | DOWNGRADE | The licence dialog shows raw EULA.md, including the internal 'DRAFT FOR COUNSEL / TODO(legal)' comment | It is true that the dialog inserts raw EULA.md, leading HTML comment included. But the visible body of the HEAD EULA already says 'DRAFT FOR COUNSEL' (line 19), names Cablewrap Creative (lines 21 and 196) and carries inline TODO(legal) markers. Stripping comments, the suggested fix, would leave all of that on screen. The EULA being a draft for counsel is an owner-documented state (CLAUDE.md), so what is left for this finding is cosmetic: raw Markdown and one comment block. Separately, the body n |
| ui-dash-admin-4 | DOWNGRADE | OTHER VERSIONS HELD (the rollback list) collapses every 30 s, and PREVIOUS STOPS every 60 s | Real: details.pkg-other and the halt-history details have no data-key/id, and both base.html's keeper and tab_memory.js need one, so the polls re-render them collapsed. But nothing is lost or mis-written, one click reopens, PREVIOUS STOPS is read-only history, and the rollback controls keep their confirms - a usability annoyance. |
| ui-dash-main-4 | DOWNGRADE | The fix-root computer picker drops ?as=, so an admin checking an editor's second computer lands back on their own queue | The mechanism holds: the fix_root chips link to / and /?machine= with no as=, _queue_editor falls back to the admin, and _queue_machine drops the machine. But it is a read-only full-page hop and the change is visible: the header reads SYNC QUEUE: <admin>, the switcher resets and the 'acting as' warning disappears. Nothing is written. |
| ui-dash-static-4 | DOWNGRADE | Refused dashboard update/rollback reason is written then immediately erased by reloadPanel | Real only for apply: the catch writes the refusal and then reloadPanel() replaces #dashupd-progress. The rollback catch does not reload, so its message stays. In the in-progress example the repainted panel shows 'working: step' and resumes watching. What remains is a vanished reason on an admin-only, retryable action, with no harm done. |
| ui-music-ytdl-web-1 | DOWNGRADE | The YouTube page has no phone layout: its search row is 365 px wider than the screen | Real: at HEAD, .search-bar is a non-wrapping flex row and ytdl style.css has no @media rule, so on a phone the row is wider than the screen. But /ytdl/ is a desktop tool that feeds the editing tree. The row fits at any desktop or half-screen width (847 px measured), and every control can still be reached by scrolling sideways. Nothing is lost, so this is layout polish. |
| ui-music-ytdl-web-3 | DOWNGRADE | The attestation lock on SEARCH / GET LINKS is undone by the project loader and by every submit | The mechanism holds. loadProjects() runs after loadAttestation() and sets #go and #golinks back to enabled whenever a project exists. setAttested(true) re-enables them over the no-projects disable, and dataset.title is never set, so the tooltip goes stale. But the server is the real gate (as loadAttestation's own comment says), the attestation notice stays on screen, and pressing SEARCH ends in a refusal toast that gives the reason. No download goes through that shouldn't, and nothing is lost. |
| ui-onboarding-3 | DOWNGRADE | Interrupted-install page and close warning always claim 'no P drive', even for base installs and Macs; resume defaults to the editor radio | The mechanism is real: the copy ignores the breadcrumb's clean_slate:<role>, and the base and macOS plans never unmap the drive. But the only harm is one wrong sentence on a rare path. The instruction on the page is still right. The resume radio default is overridden by the dashboard role when the account is verified (CR-66). The Mac wizard itself talks about a P:\ mapping. |
| bug-broll-1 | CONFIRMED | Client-folder item matching ORs a stale stored id with the current name: remove/note hit the wrong clip, add refuses a new one | The panel sends the stored item.video_id, and remove_item/set_note OR that id with the current index's identity of the same number. Reproduced with the HEAD module: items X(5), Z(9), renumbered to Z=5, X=12, then remove_item(5) deleted both items (folder empty). |
| bug-broll-2 | CONFIRMED | HttpBackend posts the LOCAL shadow id to /api/ingest/index and /api/ingest/moved, so descriptions land on a different clip and it gets renamed | write_index_result and record_moved post the local shadow id, although the docstring says the id spaces are independent. routes_ingest /index looks body.video_id up directly in the canonical DB and replaces that clip's segments, themes and flags. Dormant on the base rig (sqlite mode), but db.mode api is a documented config and the corruption is silent. |
| bug-broll-3 | CONFIRMED | Posters/sprites are keyed by videos.id in one shared folder while the base rig and the live dashboard mint ids independently, and the drain re-mints ingested ids without moving their stills | Every link read at HEAD: claim mints AUTOINCREMENT ids on the live DB while the base rig mints the same range, stills are posters\|sprites/{id}.jpg served by id, build_archive overwrites by id, and broll_drain re-inserts with new ids. No code renames stills. Not run end to end (needs a publish), but the mechanism is complete. |
| bug-comp-app-2 | CONFIRMED | Consolidate ignores the halt, the licence gate and the sign-in gate, and runs lane A upload and lane B rclone sync directly | consolidate_project checks only sync_enabled, the tray pause, config_problems and the root. It never calls _lanes_refusal(), which is where the halt and the EULA are checked. It then calls lane_a/lane_b.run_once directly, and rclone_lane._run_once_locked has no halt check. A lane that was never started has its stop event clear, and the button is always offered in Settings ADVANCED. lane_b_allowed still blocks a lane B pull whose dry run showed deletions, so there is no data loss: an admin's halt |
| bug-comp-app-3 | CONFIRMED | An admin's diagnostics request is marked answered before the upload, so a failed upload is never retried | _apply_diagnostics_request saves applied_request to disk before it starts the fire-and-forget upload thread, and that thread's False result (signed out, POST failure) is thrown away. The dashboard clears the request only when the bundle arrives (api.py:10171) and resends it on every reply, but the saved stamp refuses every resend. A failed upload is never retried until the admin clicks again. This contradicts the reporter docstring's 'try again later' contract. |
| bug-comp-broll-2 | CONFIRMED | keep sub-folders unticked, or a folder name safe_name rewrites, fails every sub-folder clip with 'not on this computer any more' | The server stores rel_dir as '' when keep_subfolders is off, and otherwise runs each part through safe_name. The companion stages the raw rel_dir, and _item_from_manifest matches on the exact (name, rel_dir) string. A probe against HEAD returned local_path '' for staged 'Day1' vs manifest '' and for 'Shoot A.' vs 'Shoot A', so _crunch_item fails the clip with 'not on this computer any more'. Retry fails the same way. The trigger differs from bug-comp-broll-1, but the matcher is the same and one  |
| bug-comp-broll-4 | CONFIRMED | Staging held after one original failure is held for ever, and the disk-full message names a button that cannot free it | held_for_base_rig is only ever appended to (:3632, :3650). A later successful original upload through retry_failed (proxies_live -> pending) never removes the name. prune_staging skips held entries even at max_age_days=0 (CLEAR FINISHED STAGING). staging_report counts held entries that have ended_at as finished_bytes, so _space_refusal names a button that frees nothing. The hold is intended only while the original is still owed, so after that the staging space is never reclaimed. |
| bug-comp-broll-5 | CONFIRMED | Ingest encodes have a fixed 15-minute ceiling, so long clips on CPU-only machines lose their proxy or the whole item | _run_media never passes a timeout, so the preview and editing-proxy encodes run under RUN_TIMEOUT_SECONDS=900, whose comment says it was sized for stills and sprites. On a machine without NVENC, own_proxy_cmd is libx265 -preset medium 10-bit, so a clip of roughly 10 minutes or more can exceed 15 min. The timeout becomes 'did not decode': the proxy is lost, or the whole item when the preview times out. proxy_gen runs the same recipe with max(1800, duration*60). Not measured on hardware. |
| bug-comp-core-3 | CONFIRMED | Site manifest (feature flags, smb_unc, sftp tuning) refreshed once per process start | refresh_site has one caller, a single fire-and-forget thread in App.start (app.py:9342-9356). save_site is only called from it, and no report reply carries the manifest. feature_enabled reads the cache only. So a companion that started offline, or any flag an admin flips later (auto_update, youtube_download), never reaches a running tray until it restarts. |
| bug-comp-core-5 | CONFIRMED | Manifest scan that overlaps a drive unplug reports zero files for the remaining projects | refresh_once checks root presence only before the walk. os.walk has no onerror and size errors are skipped, so a drive pulled mid-walk yields 0/0 rollups that replace the cache. Later refreshes are then skipped because the root is absent, so the zeros persist for the whole unplugged period, and the reporter sends them with no presence gate. This is exactly the report the guard was built to prevent. The effect is a wrong presence view, not data loss. |
| bug-comp-media-4 | CONFIRMED | Peaks job on a clip with no audio is a retryable failure, so it tours the fleet cooling every machine down before it pins | Reproduced: the exact peaks_cmd argv on a video-only mp4 prints 'Output file does not contain any stream' with a non-zero exit. _peaks discards the probed codec, so the code != 0 branch raises a retryable MediaJobError, unlike _audio and _proxy. Cards falls back to the clip's own media when there is no reference WAV, so a video-only source is reachable. The peaks budget is 4 retries, each putting a machine on a 120 s cooldown for every kind, then a pin that fails again. |
| bug-comp-media-6 | CONFIRMED | Music insert at playhead fails whenever a clip straddles the playhead on the target track (ripple moves it into the new cue) | affected = items with GetEnd() > rec includes a clip that straddles the playhead. It is re-appended at start+shift with its full duration, so it always overlaps the new cue's [rec, rec+shift). Either Resolve refuses the span, so the verify fails and the insert rolls back every time, or it overwrites and silently cuts the cue's tail. Both are wrong for the common 'sting in the middle of a bed' case. No existing ledger entry covers it. |
| bug-comp-rclone-2 | CONFIRMED | Server-followed moves on small passes stay on the breaker's cumulative counter and trip 'slow leak' | note_pass adds the full per-pass deletes to _deletes before anything else, and it runs the relocation probe only when the pass would trip or reaches the eager threshold (25 at the default cap). _discount_relocations is clamped to the current pass, and _deletes resets only on resume. Moves the lane already followed (_moved_out_of_trash, _server_relocated_keys) on passes of 1-24 files therefore build up and later trip BREAKER_CAUSE_LEAK at 200. Lane B parks until a human resumes it, with no data l |
| bug-comp-rclone-3 | CONFIRMED | The disk-floor park keeps a stale sentence and never clears after the trash prune frees space | DiskFloorLatch.check updates _free_bytes while parked but keeps the reason sentence from when it parked, so the stated free-space figure goes stale. The park clears at 2x the floor, while prune_trash's disk-pressure loop (passed disk_floor.min_free_bytes) stops at 1x. The sequencer's per-pass prune therefore cannot bring about the automatic release: the lane stays parked until a manual RESUME and shows the wrong number meanwhile. |
| bug-comp-resolve-1 | CONFIRMED | An admin's undo sent during Resolve's launch window is recorded FAILED and never retried | I replayed HEAD's PARK_HINTS/RETRY_HINTS/'resolve'+'not' logic over the messages. STARTING_MESSAGE 'DaVinci Resolve is starting up' classifies as failed, while the other disconnection messages classify as retrying. undo_last_relink passes through get_media_pool_items' describe_disconnection() text, and _apply_resolve_undo records the failed state for good. |
| bug-comp-resolve-2 | CONFIRMED | The 20 s project-name cache files a relink under the PREVIOUS project, which brings back the cross-project undo hazard | _project_name_cache has no invalidation, and _before_mutation, the link_proxy journal entry and both automatic limiters (app.py 3258/4653) read it with the 20 s TTL. After a switch, B's unprompted relink is journalled as A. The undo in A then passes the A==A check and replays B's entries against A's pool through shared Assets paths, which reopens CR-51. It needs a cache read just before the switch, so it stays medium. |
| bug-comp-resolve-3 | CONFIRMED | Cards sweep_items runs the API walk under _API_LOCK when no library is available, throws the result away, and speeds up the watcher's full-walk valve | sweep_items calls get_timeline_items(allow_cached=True). That call falls through to _get_timeline_items_locked under the API lock when the library answers None, and only afterwards does sweep_items discard the api-sourced items. It also shares the watcher's poll cache, and each hit bumps _polls_since_full_walk, which speeds up the per-clip full-walk valve. ResolveEngine._sweep calls it on every sweep, and being library-less is the documented default. |
| bug-comp-resolve-4 | CONFIRMED | Restarting the Cards role after ONE loop dies leaves the other loop running on the released engine | _clear_dead does not stop the surviving daemon loop, and role.call has no current-client check, so the orphaned pull_loop keeps staging /cards/agent/pending requests on the old engine. This is worse than filed: _release_engine is a no-op, because SyncEngine/LibraryEngine/ResolveEngine have no stop() in the MulticamPipeline checkout (git grep 'def stop' HEAD finds only keys.py and project_agent.py), so engine #1's Resolve thread keeps running beside engine #2. Confidence goes from PLAUSIBLE to CO |
| bug-comp-syncthing-1 | CONFIRMED | A proxy that cannot follow turns a completed move into a failed one, and the retry drops the relink | apply_move puts src.replace(dest) and move_proxy_siblings in one try/except OSError. A locked proxy therefore returns (False, 'could not move it...') after the original has already moved, and the app records it as retryable, not applying. On the redelivery src is gone, and the resume arm accepts only STATE_APPLYING or a lane B relocation row. It answers (True, 'nothing at the old path', None), so there is no relink, relink_pending is False, and the proxy is left behind. |
| bug-comp-syncthing-2 | CONFIRMED | An upload-only borrower still builds a borrowed lender, so the tray tells the editor to ask an admin who cannot fix it | _build_borrowed never skips upload-only items, so an upload-only borrower's includes land in _borrowed_lenders. The collector shares a lender only for FULL borrower ticks, so the offer never comes. BorrowedFolderManager returns not-offered, and the tray and fleet permanently say 'Ask your admin to approve it'. The hunter's 'still pulling down' sub-case is weaker than stated (enforce would unshare the folder), but the permanent false problem and the wrong subtree attribution are real. |
| bug-comp-syncthing-3 | CONFIRMED | A full borrower whose lender is ticked upload-only never receives the borrowed subtree (both sides) | The dashboard's _expand_includes marks an include covered when the lender is in the machine's selection in any mode, and the companion's _build_borrowed drops an include under any selected rel, upload-only rels included (rel_to_slug is built from every valid item). An upload-only lender's turn is lane A only, so a full borrower's borrowed subtree never comes down and nothing reports it. This breaks the CLAUDE.md FULL-only rule on both sides. Related to syncthing-2, not a duplicate. |
| bug-comp-syncthing-4 | CONFIRMED | A sequencer thread that outlives stop() can unpause a folder after the halt paused it | halt_all_sync pauses lane C folders once, after sequencer.stop() has returned (a 5 s idle wait plus a 10 s join), and nothing re-asserts the pause. In the sequencer thread, _maybe_auto_accept -> accept_folder ends in an unconditional unpause after a set_ignores on the 30 s write timeout. _verify_current_folder_unpaused runs after _wait_for_folder_sync with no stop check, and _set_paused has no halt check. The borrowed-folder manager guards the same accept_folder call with halted(), and the seque |
| bug-comp-ui-1 | CONFIRMED | Ingest file picker builds a Tk root outside _popup_active_lock, and its timeout abandons a live dialog whose answer is dropped | On win32 ui_dispatch.dispatch is inline, so _tk_pick builds a Tk root on the ccsync-ingest-picker helper thread with no _popup_active_lock anywhere on the path (pick_ingest_sources -> pick_media_sources -> _tk_pick). pick_media_sources returns [] after 300 s and never closes the dialog, so a late pick is dropped as 'cancelled' and a second click opens a second root. No existing entry covers this. |
| bug-comp-ytdl-2 | CONFIRMED | Stop, quit or lease loss during 'converting' leaves the unconverted original as a finished clip | A stop during conversion deletes the .editready tmp and returns (name, None, None). _download_one then calls only _cleanup_current, which clears sweepable partials only. disown_output is reached only via _fail_clip, so the VP9 original stays under its deliverable '[id].mp4' name for the importer and lane A. The root is shared with high bug-comp-ytdl-1 (converting in place under the final name), but this trigger makes the bad file permanent, so it is not a duplicate. They should be fixed together |
| bug-comp-ytdl-4 | CONFIRMED | One failed yt-dlp install or floor update switches local downloads off for 24 hours; nothing retries sooner | _loop waits CHECK_INTERVAL_SECONDS (24 h) after any enabled pass, including ACTION_FAILED with ok=False from a failed install or floor update. capabilities() then refuses every job. The only other ensure() caller is the per-job poke, which cannot fire because no job is ever claimed. So one network blip at boot or during a floor raise sends that machine's downloads to the slower, flag-prone server path for a day. |
| bug-dash-api-1 | CONFIRMED | Approving a second computer's SSH key erases the first computer's key on the NAS | approve_pending_ssh_key in NAS mode calls create_or_update_editor, and both TrueNAS (PUT sshpubkey) and Synology (printf > tmp; mv -f authorized_keys) replace the key. The approve button has no confirm/warning (unlike UPDATE SSH KEY), and each computer's wizard offers its own key, so approving a second machine locks the first out of SFTP lanes A/B. Recoverable, so medium. |
| bug-dash-api-2 | CONFIRMED | File move that renames leaves the proxy under the old name; a same-folder rename reports PARTIAL falsely | _move_proxy_siblings targets Proxy/<old candidate.name>. HEAD probe: same-folder rename returned (0, ['A001.mov (something is already at the destination)']) with the proxy unmoved (PARTIAL/207, false reason); a cross-folder rename left the proxy as B/Proxy/A001.mov, which no longer pairs with the renamed original. The companion renames case-only proxies to dest.stem, so NAS and machine diverge. |
| bug-dash-auth-2 | CONFIRMED | The session touch blocks the event loop for up to 20 s and then 500s a valid session | SessionStore.validate's once-a-minute last_seen UPDATE opens with the 20 s BUSY_TIMEOUT_BACKGROUND_MS and runs synchronously from the async login_gate/csrf_gate middlewares, so any write-lock holder stalls the single-worker event loop up to 20 s, then OperationalError (503 via the CR-282F handler) refuses a valid session. CR-282F fixed the same shape only in the error handler. |
| bug-dash-auth-3 | CONFIRMED | UNDO of a site change empties the project template and the shared asset folder list | _settings_fallback returns '' for template_folders/shared_asset_folders, diff records from:'' and undo upserts an explicit '' row, so resolved_manifest yields empty lists instead of provision defaults. Probe on a temp DB: before 8 folders/2 assets, after undo-style restore [] []. Affects sites whose rows were never env-seeded. |
| bug-dash-auth-4 | CONFIRMED | A shared asset folder name with no ASCII letter or digit 500s /api/v1/site and every manifest reader | validate_many accepts 'Assets/Luts,音效'; resolved_manifest then raises ValueError from provision.slugify, and api_site has no fallback, so the open manifest (installers, wizard, companions) and project creation 500 until an admin rewrites the row. Reproduced in the dashboard venv. |
| bug-dash-cards-jobs-1 | CONFIRMED | A double slash walks past CardsGate: /cards/p/<slug>//api/root reaches engine.set_root() | _child builds '/'+tail so the gate sees '//api/root' (not in BLOCKED_PATHS), a2wsgi keeps it as PATH_INFO, and CPython 3.12 parse_request collapses '//' so the handler dispatches /api/root. Reproduced on HEAD's full stack with test_cards_pool fixtures: '//api/root' reached the handler (engine.posts recorded it) and '//api/restart' set restarted=True. Needs a signed-in session. |
| bug-dash-cards-jobs-2 | CONFIRMED | An agent's traffic follows whichever of its editor's devices made the last request, so pending and result land on different engines | note_visit overwrites one _where[editor] slot on every dispatched request, and cards_tunnel routes /state, /pending and /result independently via engine_for, so with one account in two episodes (the pool docstring's everyday pair) a pending from engine A can have its result routed to engine B, which refuses it, and A reports an applied edit as lost. |
| bug-dash-cards-jobs-4 | CONFIRMED | After a session-secret rotation the b-roll and music gates mint no identity, so ingest panels and client folders 401 | BrollGate/MusicGate._identified_scope call read_session_cookie with the current secret only, while login_gate's _resolve_session accepts previous_session_secrets (DASH-2). Cookies are never re-minted per request and sessions last up to 7 days, so for the whole drain window pre-rotation browsers reach /broll and /music with no identity header and the header-gated panels 401. ytdl gate has the same shape. |
| bug-dash-db-1 | CONFIRMED | The scheduler only ever looks at the first 200 queued jobs, so a block of jobs nobody can run starves every job behind it | queued_jobs is LIMIT 200 by priority/id and both offers_for_machine and claim_next_job walk only that window; queued jobs never age out and unclaimable whisper never pins. HEAD probe: 200 GPU whisper jobs + 1 peaks -> peaks absent from window, claim_next_job(allowed_ids=[peaks]) None, offered []. |
| bug-dash-db-2 | CONFIRMED | A project with more than 2,000 proxies shows the extra proxies as downloads owed for ever, and the note says the total may be too low | Companion caps the proxy list at 2000 per project (sets truncated); fetch_sync_backlog's down diff counts every NAS proxy without an editor_media row, so proxies past 2000 are permanent phantom downloads, and transfers.html says 'may undercount' (wrong direction). CR-314 fixed only the combined-kind cap. |
| bug-dash-diag-1 | CONFIRMED | A NAS that does not answer turns a MISSING protection line into a 'this has cleared' mail and closes its notice | protection.run_cycle keeps MISSING notices only for lines BROKEN this pass. A line that drops to NOT_CHECKED when tasks() is None closes its notice and leaves protection_missing findings, so deliver mails a recovery. invariants.run_cycle has the stored_broken carry for exactly this case (dash-collector-2), and protection lacks it. |
| bug-dash-diag-3 | CONFIRMED | A snapshot restore's .restored-<ts> folder is replicated by Syncthing to every editor who has the project ticked | restore_missing creates .restored-<ts> inside the live project folder, which is the Syncthing root. The dot prefix hides it only from provision's own walks. Syncthing syncs dot-dirs, and build_stignore_lines ignores only video, partials, ytdl fragments and Proxy, so every restored non-video file (and the copied marker) goes to every full-tick machine. Established by reading the code, not reproduced live. |
| bug-dash-ops-1 | CONFIRMED | The 'same Message-ID is in my Sent folder' proof is an IMAP substring search, so a forged From passes the authentication check | _in_sent does an IMAP HEADER substring search (RFC 3501), and handle_message counts a hit as authentication when sender == alerts_smtp_user, overriding even an explicit dkim/dmarc fail. A Message-ID fragment such as 'mail.gmail.com' matches the Sent folder. It is bounded by the 48 h CCT token, and the feature is off in the vendor build. |
| bug-dash-ops-2 | CONFIRMED | /ytdl drops the identity of every session signed with a previous DASH_SESSION_SECRET | login_gate (_resolve_session) accepts cookies signed with session_secrets_previous, but YtdlGate._identified_scope calls read_session_cookie with the current secret only (gate built at ytdl.py:555), so no identity header is added and ytdlweb returns 401 after a rotation. broll.py and music.py gates have the same shape. |
| bug-dash-ops-4 | CONFIRMED | crash_report.redact misses the secrets its own docstring names (env-style and quoted keys) | Ran HEAD's _REDACTIONS. DASH_SESSION_SECRET=, TRUENAS_PW=, SYNCTHING_API_KEY:, {'password': ...} and {"api_key": ...} all come back unchanged, because \b does not match after _ and a closing quote defeats the pattern, while token=abc is redacted. The redactor also feeds notices.error_detail. |
| bug-music-ytdl-4 | CONFIRMED | Cancelling a batch strands every indexed-but-not-uploaded track as a permanent row with no audio | release(cancelled) moves indexed/uploading items (whose tracks row exists) to terminal 'cancelled'. The companion's cancel() stops uploads first, retry_failed moves only failed items, and only a manual --prune deletes tracks rows, so orphan rows stay searchable with 404 audio. This contradicts release's own docstring. Also reached via expire_stale_leases, and not covered by CR-286E/CR-304A. |
| bug-wire-1 | CONFIRMED (duplicate of bug-comp-app-4) | The "Resolve relinked after all" answer is dropped by the dashboard, so a move shows "Resolve not repointed yet" for ever | Companion _relink_pending_moves queues a second ok answer, but db.mark_file_move_applied matches only applied_at IS NULL, so it updates zero rows; relink_pending stays 1 and project_detail.html:250 renders 'moved, Resolve not repointed yet' for good. Same defect as bug-comp-app-4, whose 'nothing renders it' premise is wrong. |
| bug-wire-2 | CONFIRMED | A session-secret rotation drain keeps the report and jobs alive but cuts every un-re-signed machine out of b-roll ingest, music ingest and ytdl | broll/music/ytdl sub-apps verify X-CCSync-Identity with DASH_SESSION_SECRET only (no PREVIOUS anywhere under them), while the dashboard's report path accepts retired-key tokens and says the editor must sign in by hand to re-sign. During a rotation drain, ingest/ytdl 403 per machine while the grid shows it accepted. Related to bug-dash-ops-2 (session cookies in the gates), not a duplicate. |
| bug-wire-3 | CONFIRMED | Lane last_error/detail/current_project caps still raise, and the companion never caps them: one long lane error 422s the whole report | LaneReportIn keeps raising max_length caps with no before-validator and ReportIn's truncation never touches lanes; probe LaneReportIn(last_error='x'*2001) -> string_too_long. Companion lane C joins every errored folder's Syncthing error text unbounded and the reporter sends it verbatim, so ~15 folders in error 422 the whole report at the worst moment. |
| logic-admin-1 | CONFIRMED | The "fleet-wide stop expired" alert can never fire | db._halt_state returns active = active and not expired, and _check_fleet_halt_expired needs active and expired both true; Ctx.halt comes from get_fleet_halt, so the registered alert can never fire. |
| logic-admin-2 | CONFIRMED (duplicate of ui-dash-admin-2) | The halt's release time reads "releases itself 0s ago", on every page | Both halt templates pass the future expires_at through ui.ago, which clamps negative deltas to 0 and prints '0s ago'. Same defect as ui-dash-admin-2. |
| logic-alerts-1 | CONFIRMED | A mount a site deliberately left OFF raises a permanent 'check the bind mounts' warning | _check_feature_mounts skips only 'mounted'. The 'disabled' status is recorded for ytdl (off in the vendor build) and for cards (no checkout or vault configured), and each gets a feature_not_mounted warn whose fix is to check the bind mounts and restart. So the card stands permanently on every vendor install, and a restart cannot clear it. Neither DDIAG-7 nor the 09-11 hunt covers the disabled case. |
| logic-alerts-2 | CONFIRMED | versions_behind counts staged/feed builds, so a computer on the CURRENT build is told to UPDATE NOW | _check_versions_behind counts staged rows and feed-offered builds that are newer than the running version. Counting them is intended (SYS-2), but the text then says 'current is X', which equals the running version, and the fix it names is UPDATE NOW. That button pushes the build the machine already runs, and triage_actions refuses it. So on a site with a build held for soak or waiting on manual adoption, the alert is contradictory and cannot be acted on. |
| logic-alerts-3 | CONFIRMED | platform_channel_stale says 'build it' when the Mac build already exists staged, and names repo commands on customer sites | _check_platform_channel_stale compares only the two platforms' current versions. It never looks for a newer build that is already published but staged on the lagging platform. It always says nothing will offer that build until somebody builds it, and its fix is hard-coded release_macos.sh commands. Those are wrong when the build is already staged (a re-publish is refused), cannot be run on a customer site with no repo, and still say 'On a Mac' when Windows is the platform behind. It is a separat |
| logic-broll-music-1 | CONFIRMED | After an index rebuild, removing one client-folder clip can delete a second clip, and adding a clip can be refused as 'already' | Reproduced from the broll/web venv against the HEAD copy of client_folders.py. The panel sends each item's stored video_id, and remove_item and set_note also match on the (share, rel_path) the current index gives that id. After a rebuild renumbers the ids, a stored id can belong to a different clip. In the probe, adding clip C returned already=[9], and removing A (DELETE items/5) deleted both A and B from a live client folder. The OR clause added by broll-4 and MEDIA-23 causes this. |
| logic-broll-music-2 | CONFIRMED | settle_intents calls a stand-in 'the real original' for any heavy original of 1080 lines or less | _what_landed treats a probed width x height equal to the original's geometry as the real original. The preview is scaled to min(1080, ih) and never upscaled, so for any original 1080 lines or smaller the stand-in has exactly the original's geometry. Such clips are still heavy under edit_weight (codec not h264/hevc/prores, or bitrate over the cap) and get a stand-in when they are .mov, .mp4 or .m4v. If that fetch lands while nobody is watching, the ledger row is retired and the preview bytes stay |
| logic-broll-music-3 | CONFIRMED | Cancelling a music batch mid-upload leaves tracks that stay searchable but have no audio and cannot be retried | write_item_result creates the tracks row and marks the item 'indexed' before the audio is uploaded. A cancel release then turns indexed and uploading items into terminal 'cancelled' items while their tracks rows stay. retry_failed only moves failed items, and load_matrix loads every track that has an embedding. The result is a searchable track with no audio that also holds its name, which contradicts release's own docstring. |
| logic-cards-1 | CONFIRMED | Anyone in a shared episode can close it under the other person, and the confirm names the person pressing the button | HEAD may_close returns '' for any listed occupant even with others present (probe: occupants ['alex','ruskin'], may_close(slug,'ruskin',False)==''), and the confirm is built from last_in(), i.e. the presser ('ruskin is in it now'), never naming alex. Contradicts cards_close docstring and the template's Who-column comment; tests never cover two occupants. Medium: drops a colleague's live engine behind a misleading confirm, no data loss. |
| logic-cards-2 | CONFIRMED | An always-on companion keeps its editor 'in' their last episode indefinitely, so the idle release never fires | local_engine stamps note_agent on every routed agent call; _where is only cleared by drop(); the companion role long-polls pending for as long as the tray runs. Probe: entry idle 3 h reads occupants [] and closable; one note_agent makes it ['bob'] and may_close refuses a non-admin. So idle release, the cap sentence and Who column never lapse for editors with the Cards role on - a side effect of security-1 that restores the admin-only state security-2 fixed. |
| logic-onboarding-2 | CONFIRMED | On a site with the shared report token retired, the wizard says DONE for a machine with no fleet credential | With the shared token retired, /verify returns an empty report_token and report_token_kind='editor', which no code reads; nothing writes editor_report_token except preserving an existing one, and the wizard has no field for it. The finish page says DONE for a machine whose every report 401s. |
| logic-plans-4 | CONFIRMED | A file moved out of a borrowed (shared) folder is never announced to the machines that borrow it | file_move_target_machines is the only target list (api.py:2856, collector.py:1908). It unions the plan holders of from_slug (no borrower expansion) with the editor_media holders. A borrower's manifest lists files only for selection rels, so a borrower machine that uploaded into the lender folder is never told and its lane A re-uploads the file to the old path. fetch_borrowers_by_lender exists but is not consulted, and there is no existing KNOWN_BUGS entry (CR-283J is the opposite direction). |
| logic-release-2 | CONFIRMED | Retracting the current build makes every policy=current site auto-publish a newer build the vendor only STAGED | retract_record pops current[kind/platform] and neither it nor the CLI branch sets a replacement; select_offered_records then falls back to the highest version, so a newer staged record becomes what every policy=current site publishes and makes current after soak, contrary to publish_latest's 'offered to nobody' promise. |
| logic-release-3 | CONFIRMED | Staged vendor records (and a pointer moved back) count as 'offered': invariant 11, the versions-behind alert and the HEALTH box cry wolf | record_offer_state writes every companion record into feed_offered rather than select_offered_records, while _apply_policy honours the pointer; invariant 11 takes the max and reports 'vendor offers X, not published here' for staged or pointer-withdrawn builds, and the alert/HEALTH readers follow. The feed_publish_refused half is currently dormant because no record carries requires_dashboard. |
| logic-release-4 | CONFIRMED | Dashboard auto-update ignores the channel's current pointer, so a STAGED dashboard bundle is applied at every policy=current site | dashboard_update.status builds code_updates from every newer dashboard record and _dashboard_auto_apply_reason takes updates[0] after only failed-before and soak-age checks; channel_current for dashboard/linux (present in feed/channel.json as 0.7.44) is never read, so a staged bundle is auto-applied with a restart at every image-mode policy=current site, and a pointer moved back rolls nothing back. |
| logic-release-6 | CONFIRMED | 'Re-run with --make-current when you are ready' is a no-op: the re-run skips the version as already published | The 'already published' skip precedes publish() and ignores --make-current, printing 'nothing to do', while the staged summary tells the operator to re-run with --make-current. RELEASE_PATHWAYS.md step 4 documents that this cannot work and names a per-record publish_feed recovery, so the tool's advice contradicts the runbook and leaves the fleet on the old build. |
| logic-resolve-1 | CONFIRMED | RETRY FAILED and 'run FIX ALL again' copy the whole file in a second time when only the relink failed | fix_clip always claims a new name via _claim_destination_path and never reuses copied_to; relink-failed and partial-relink results both go into popup _failed_rows (failures + aborted), so RETRY FAILED copies the whole file again as 'name (2).ext', which lane A uploads. |
| logic-resolve-2 | CONFIRMED | UNDO LAST FIX only undoes the last 2-minute burst of a FIX ALL, and a second press falsely says the clips left the media pool | SESSION_GAP_SECONDS=120 splits a FIX ALL with any copy over 2 minutes into several journals; undo_last_relink replays only latest_session and never retires it, so a second press finds no clip at the new paths and says they left the media pool. Admin dashboard undo by id (resolve_undo.py) is a partial mitigation only. |
| logic-resolve-3 | CONFIRMED | Share LUTs offers Resolve's factory LUTs, and a Resolve version mismatch turns that into a permanent offer that copies nothing | search_dirs is Resolve's factory LUT folder and stray_luts has no factory exclusion, against the module's own rule; same-name different-size files stay strays while copy_into_library skips existing dests, so the offer never clears. P:\Assets\Luts already holds the factory set (ACES, Arri, Blackmagic Design, DCI, Invert Color.ilut ...), checked by directory listing. |
| logic-resolve-4 | CONFIRMED | With no matched project, FIX ALL's default destination is the tree root, which lane A never uploads, yet the dialog reports Fixed | pick_project_prefix falls back to '' and suggest_destination then returns B-roll/Editor Added/<editor> at the tree root; fix_clip only checks containment in local_root, lane A walks Projects/<rel> only, and the dialog says Fixed. The only warning is a config log line. |
| logic-sync-truth-1 | CONFIRMED | A laptop that is merely asleep gets a red "Upload has stopped" headline after 15 minutes | lane_chip reds every lane at 15 min of silence while report_freshness says amber until 6 h. why_causes has no not-reporting reason, so fleet_headline falls to _lane_fault and says 'Upload has stopped: this companion has been silent...' in red. Reproduced in the dashboard venv against HEAD code. |
| logic-sync-truth-2 | CONFIRMED | The per-kind manifest cap still makes phantom proxy downloads, the UI says 'may undercount', and safe-to-close can miss uploads | The companion and replace_editor_media both cap at 2000 per kind. down_totals_q counts proxies past the cap as owed for ever, and up_totals_q misses originals past the cap. safe_to_close ignores manifest_truncated, so it can say 'Safe to close', and transfers.html only warns of an undercount. This is the part CR-314 did not cover. It is display-only, so medium. |
| logic-sync-truth-4 | CONFIRMED | Disk-floor park never clears on its own: the trash prune stops at 1x the floor, the latch clears at 2x | DiskFloorLatch clears only at 2x min_free_bytes, but prune_trash's pressure loop stops at 1x (rclone_lane passes disk_floor.min_free_bytes). So lane B stays parked until a human resumes, while trash that could have released it remains. CHIP_HELP['disk'] still says the trash cannot prune while proxy download is stopped, which has been false since SYNC-16. |
| logic-ytdl-jobs-1 | CONFIRMED | The scheduler judges a computer by its LAST report however old it is, so a machine switched off weeks ago keeps a job schedulable/transient for ever | fleet_facts/fetch_capabilities_map have no reported_at bound (only cap_at IS NOT NULL); nothing in jobs.py asks whether a machine is still reporting, so a long-silent machine is ranked on its last idle_seconds, can make explain say schedulable or keep idle_wait/transient, and can win the 60 s grace. A live machine still claims after the grace, so misleading why/schedulable plus delay: medium. |
| logic-ytdl-jobs-3 | CONFIRMED | The page never shows why the editor's computer gave a download back: handed_back_reason has no consumer, and claim refusals set no reason | app.js at HEAD never reads handed_back_reason or a handed_back phase, and only polls /ytdl/progress when download_mode is local, so pre-lease hand-backs are never fetched. CR-171 built the companion half and LOOPBACK_API.md promises the page learns why; CR-172's page wave never consumed it. The claim-refusal silence is deliberate per FleetClient.claim's docstring, but the unconsumed reason alone keeps the finding real. |
| ui-broll-web-3 | CONFIRMED | Detail-view hotkeys hijack Enter/Space on focused buttons, including buttons in drawers open over the detail view | onKeydown on document exempts only INPUT/SELECT/TEXTAREA while a detail is open. Enter calls preventDefault and then sendToResolve unconditionally. No other listener stops Enter or Space: the clientfolders capture handler and ingest.js handle only Escape. So focused buttons in the detail view and in the drawers cannot be activated, and Enter can append a clip to the live timeline. |
| ui-comp-windows-2 | CONFIRMED | Nothing ticked is drawn as a red blocking line in Settings and pulls HELP to the top | In HEAD settings_window.py, _blocked_line is tagged BLOCKING at the producer, and BLOCKING is styled 'warning'. A probe from the companion venv against HEAD source gave _lane_advisories(no_selection) -> ('blocking', 'No projects are ticked for this computer'), which rank_advisories turns into Line(style='warning'). _help_first then moves HELP to the top. This breaks the owner rule that nothing ticked is never an error or warning on any surface, and every new computer would see it. |
| ui-copy-1 | CONFIRMED | The fleet-halt alert sends the admin to a button and a page that do not exist | [ RELEASE THE HALT ] exists only in alerts.py and a test allow-list. The real controls are [ START SYNCING AGAIN ] / [ KEEP IT STOPPED ] on /admin/users#admin-fleet-halt, and SYNC STATUS has no halt control. In the alert case, the halt banner on every page links to the right place, which softens it. The recovery step still sends the admin to /fleet before any halt exists, so there is no banner and no control, in the middle of a restore that depends on the fleet being halted. |
| ui-copy-2 | CONFIRMED | 'Settings, Diagnostics' is not a page, and the notice links land on an empty div | Five notice fixes name 'Settings, Diagnostics', which is not in SETTINGS_NAV_GROUPS. At least nine notice kinds link to /fleet#fleet-diagnostics, a bare div that is filled only by the [ READ THE ANSWER ] / partial links; no JS loads it from the hash. Server-error notices therefore lead to an empty anchor and a page that does not exist. |
| ui-dash-admin-2 | CONFIRMED | The fleet halt says it "releases itself 0s ago" for the whole time it is active | ui.ago clamps negative deltas to 0 and appends 'ago'; halt.expires_at is a future stamp, piped through ago in fleet_halt.html:38 and fleet_halt_banner.html:13, so every page says the stop 'releases itself 0s ago' for the whole 24 h. Reproduced: ui.ago(now+23h) -> '0s ago'. No until filter exists; not in KNOWN_BUGS. |
| ui-dash-admin-3 | CONFIRMED | The Users and Packages polls wipe typed input and orphan in-flight writes (undoing DUI-4's busy labels) | admin_users.html:16 and admin_packages.html:14 poll innerHTML every 30 s around partials whose write forms target 'closest .admin-users-box/.admin-packages-box'. No hx-sync/hx-preserve/focus guard anywhere in static or templates, so typed input is wiped and in-flight responses swap into a detached node (or swapError), hiding the result and inviting a duplicate NAS create/publish. |
| ui-dash-admin-5 | CONFIRMED | Results and refusals on Alerts, Recovery and Protection render far above the button that caused them | admin_alerts.html:15-16, recovery.html:16-17, protection.html:19-20 render error as class='banner' (not error-banner) and notice at the panel top, with outerHTML swaps that preserve scroll; htmx_errors.js only relocates .error-banner and KNOWN_BUGS:13040 shows DUI-6 was applied to six other partials only. SEND A TEST / RESTORE / REHEARSE results land off-screen. |
| ui-dash-admin-6 | CONFIRMED | Site Settings SAVE answers at the top of a long page, and a stale 'saved' stays beside a later error | site_settings.js:794-799 writes 'saved' into #settings-saved on success and never clears it; showError only touches #settings-error; both sit above the first field while SAVE is at admin_settings.html:183, with no scrollIntoView. A refused save after a good one shows contradictory lines off-screen, and a refused dashboard_url/prefix change looks identical to an accepted one. |
| ui-dash-admin-7 | CONFIRMED | HEALTH says every check answered when a source raised | _health_rows (ui.py:1868-1941) swallows each source's exception with log.exception and adds no row, so a failing source vanishes silently and all four failing renders 'Every check this server runs answered' (admin_health.html:113-117). This violates the module's own and CLAUDE.md's 'unverified is NOT CHECKED, never OK' rule; the per-line unknown band exists but the source-level failure bypasses it. |
| ui-dash-main-1 | CONFIRMED | Ticking a project in the sidebar folds the whole project tree back up, and on a phone closes the sheet | Sidebar tick swaps outerHTML on the nav and gets the whole sidebar partial back (button plus nav). htmx 1.9.12 sets detail.target to the original target, which is detached after an outerHTML swap, so base.html's details keeper reapplies state to the removed tree. The new popover nav starts closed and a second sheet-handle is inserted. |
| ui-dash-main-2 | CONFIRMED (duplicate of ui-dash-static-1) | The DUI-6 'refusal next to the button' mover does nothing on the outerHTML panels it was written for | htmx_errors.js searches detail.target, which is the detached old panel after an outerHTML swap (u.target=c, then Ie removes it). Every DUI-6 panel (admin_users, admin_packages, admin_jobs) swaps outerHTML, so the banner is never moved. This is the same defect and fix as ui-dash-static-1. |
| ui-dash-main-3 | CONFIRMED | The project page's 10 s poll wipes what an admin is typing into MOVE and SHARE A FOLDER | #project-detail polls every 10 s with innerHTML, and the SHARE and MOVE forms (inputs with no id, no hx-preserve) sit inside it. No script pauses the poll on focus. Typed values and focus are lost on each beat. Nothing is destroyed, because an empty move is refused. |
| ui-dash-main-7 | CONFIRMED | [ UNDO ] on a TICKED row takes a project off computers with no confirm, unlike every other untick | [ UNDO ] has no hx-confirm. On a TICKED person-level row, partial_plan_change_undo removes the selection from every computer the person owns, which is a one-click person-wide untick. Every other such untick carries the DASH-8 confirm added after CR-49. It is recoverable by re-ticking and no files are deleted, so it stays medium. |
| ui-dash-main-8 | CONFIRMED | The sidebar's projects sheet shuts under a phone user every 30 s | Every page's aside polls /partials/sidebar every 30 s with innerHTML, and on a phone it holds the popover nav. Removing an open popover hides it and the replacement starts closed. Nothing in the JS, base.html or ui.py records or reopens the open state, and pwa.js does not slow 30 s polls, so the sheet closes and its scroll resets on each beat. |
| ui-dash-static-1 | CONFIRMED | Refusal-banner mover (DUI-6) and chip tabindex pass never work on outerHTML-swapped panels | In HEAD's htmx 1.9.12, afterSwap carries the ORIGINAL target (I={xhr,target:u,...}), and the outerHTML swap removes that node. htmx_errors.js:227 therefore searches a detached panel, and every outerHTML admin panel with an .error-banner (jobs, packages, report_tokens, users, fleet_halt) keeps its refusal at the top. The chip focusable() pass has the same stale root. |
| ui-dash-static-2 | CONFIRMED | Every 4xx refusal shown as 'THIS PAGE HAS STOPPED UPDATING'; server detail discarded; 403 called an ended session | The responseError handler never reads the response body. It labels 401/403 as 'session has ended' and every other 4xx as 'THIS PAGE HAS STOPPED UPDATING'. No beforeSwap lets a 4xx swap. The toggle route answers 403 to a signed-in user and 409 with a detailed reason, and both are misreported; the next successful poll clears the banner. |
| ui-dash-static-3 | CONFIRMED | Settings [ UNDO ] confirm quotes a stale history entry after an in-page save, then reverts the save just made | The history and the undo closure's `latest` load once at DOMContentLoaded. The save and the CLI flag both PUT /admin/site, which records a new 'save' entry, and neither reloads the history. undo-last-change reverts entries[0]. The confirm names the old change while the server reverts the save just made. |
| ui-music-ytdl-web-2 | CONFIRMED | Clicking a clip's title to watch it on YouTube also ticks or unticks it for download | In HEAD card(), the checkbox calls stopPropagation but the title <a target=_blank> has no handler of its own. A click on it bubbles up to n.onclick, which toggles selection and POSTs select. The change is optimistic and saved on the server, and DOWNLOAD uses what the database says. So previewing a clip silently changes what gets downloaded. |
| ui-music-ytdl-web-4 | CONFIRMED | The music player pane is a fixed 150 px box that cuts off its own buttons and messages | At HEAD, .pane.open has a fixed height of 150px with overflow:hidden. openPane adds the wave (64 px), the bar (36 px) and padding, then the no-waveform caption on fleet tracks and the Resolve status/error line (.rmsg), and .pacts wraps. With a caption and an error message this passes 150 px even on desktop, so the line explaining why Resolve did not answer is clipped with no scroll. On the 900 px layout the wrapped buttons are clipped too. |
| ui-music-ytdl-web-5 | CONFIRMED | Rail filters, sort and '[ include them ]' silently throw away the typed search while the box still shows it | At HEAD, the axis onchange, sort, includeUnknown and applyRange handlers, and the '[ include them ]' button that noteUnknownHidden adds after a search, all call loadTracks(), which is the browse route. None of them checks #q, and only selectFacet and #clear empty the box. So the ranked search results are replaced by an unranked 'All tracks' list while the box still shows the query. MUSIC-4 only fixed the other direction (search carrying the filters). |
| ui-onboarding-1 | CONFIRMED | Fixed 660x560 window clips CLOSE / BACK / log path off finish and install pages | Nav bar packed last with side=bottom. Measured at the default 660x560 (the page frame is 524 px): the install page requests 555 px, or 611 px with the role note, and BACK is unmapped in both cases. The finish page hides CLOSE once there are 3 or more real-length warnings. The only escape is resizing the window, and nothing tells the editor to do that. |
| ui-onboarding-2 | CONFIRMED (duplicate of ui-comp-windows-3) | Licence page shows raw draft EULA: TODO(legal) HTML comment, markdown, 14 em dashes | eula_text() returns EULA.md verbatim and show_eula inserts it untouched. At HEAD the file opens with the DRAFT FOR COUNSEL / TODO(legal) comment, then markdown markup, and it has 14 em dashes. This is the same defect as ui-comp-windows-3 on the companion's licence dialog, and one fix should cover both. |
| ui-onboarding-4 | CONFIRMED | winget Tailscale install: stale NOT INSTALLED, green on failure, and join check cannot find new tailscale.exe | All three parts hold. The status label is computed once. The winget returncode is ignored, so a failure still shows green. On Windows the only CLI candidate is bare 'tailscale', resolved against the wizard's PATH from launch, so tailscale_up returns False after a fresh install. NEXT is enabled only by tailscale_up plus the probe, so the editor is stuck on the page until they restart the wizard. |
