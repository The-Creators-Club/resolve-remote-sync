# Ledger, wave 2, group onboarding

## Chunk 1 of 2

## logic-onboarding-1 - The wizard takes a machine's role from the PERSON
- Status: FIXED. Severity medium (verifier DOWNGRADE from high; the known CR-66 / data-model-2 item, whose deferral reason CR-88 removed).
- Verified as: HEAD `steps.effective_install_role` returned the verified role whenever it was "base"/"editor"; `onboard._on_verify` snapped `role_var` to it; `ensure_config` forces it into config.toml `mode`; `build_cleanup_plan` and the worker dispatch key on it. api.py `role` is `"base" if is_admin else "editor"`. Companion `effective_mode` reads config `mode` only since CR-88, so the amber "which the companion obeys anyway" note was false. The destructive half of B20 (unmapping a real NAS P:) is now guarded mechanically: `execute_cleanup` gates the unmount on `p_mapping_is_ours` (site `smb_unc`), the bootstrap on `$PIsForeign`, and `validate_local_root` refuses an editor local_root on the tree drive or a UNC path, so the radio can win safely (MULTI_BASE_RIG_PLAN WP5).
- Fix: onboarding/steps.py `effective_install_role` - the radio decides; unusable radio -> "base" (non-destructive); `verified_role` kept in the signature, unread. New `read_config_mode` (text read of config.toml `mode`, None if absent/unknown) and `initial_install_role` (config mode on a re-run, "editor" on a first run). onboarding/onboard.py - `role_var` seeded from `steps.initial_install_role()`; the verify-time snap, `picked_role` and the false amber note removed; `_effective_role` / `_on_begin_install` comments rewritten. onboarding/README.md steps 2/4 and docs/MULTI_BASE_RIG_PLAN.md WP5 status updated. installer/windows_bootstrap.ps1's foreign-drive advice ("run the wizard again and pick PHYSICALLY CONNECTED") is now true and unchanged. Not built: seeding the radio from WP1 detection / a WP3 per-machine role (plan items, not this finding).
- Regression test: onboarding/tests/test_bug_hunt_2026_09_24_w2_onboarding.py::TestTheRadioDecidesTheMachinesRole (6 tests: both failure scenarios, unknown-radio fallback, config-mode seeding, first-run default, source pin that `_on_verify` no longer sets `role_var` and the note is gone) - fail on HEAD (HEAD returns the verified role; helpers absent). onboarding/tests/test_steps.py::TestEffectiveInstallRole CHANGED on purpose (it pinned the old precedence: `("editor","base") == "base"`).
- Tests run: `python -m pytest tests -q` in onboarding/ -> 461 passed, 1 skipped. New file against a `git archive HEAD` copy of onboarding/ -> 28 failed, 1 passed (the passing one is a guard that the raw EULA is still what is versioned).
- Skew / deploy order: wizard-only; no wire change. Takes effect with the next installer package.
- OWED: d-api, dashboard/src/ccsync_dashboard/api.py `api_verify` `role` comment (~2232): it still says "The companion uses this to flip its sync behavior on sign-in"; since CR-88 and this fix nothing obeys it (companion diagnostics, wizard ignores it). Comment-only.

## logic-onboarding-2 - On a site with the shared report token retired, the wizard says DONE for a machine with no fleet credential
- Status: FIXED. Severity medium (CONFIRMED).
- Verified as: api.py returns `report_token: ""` and `report_token_kind: "editor"` when `DASH_SHARED_REPORT_TOKEN_ENABLED=0`; nothing in the wizard read the kind, it had no field for a `cce1.` token, and `install_warnings` never got an entry for it. Found while verifying: HEAD `steps.write_identity` called `save_identity(path, username, token, role=role)` with no `editor_report_token`, so a wizard RE-RUN on a migrated machine silently dropped its per-editor token from identity.json (GOTCHAS "a sign-in never demotes a migrated machine" held for the tray's sign-in only). Same failure (DONE, every report 401), fixed here.
- Fix: onboarding/steps.py - `verify_account` returns `report_token_kind` (None from an older dashboard); `EDITOR_REPORT_TOKEN_RE` / `looks_like_editor_report_token` (the dashboard's shape); `existing_editor_report_token` (identity.json `editor_report_token`, then config.toml `report_token`); `fleet_token_needed` (kind == "editor", no shared token, nothing held); `fleet_token_problem` (blank ok, else must be a cce1 token); `missing_fleet_token_warning`; `ensure_config(report_token=...)` forces config `report_token` when given, never blanks it; `write_identity` carries `editor_report_token` and the shared `report_token` through the rewrite. onboarding/onboard.py - install page shows an optional "report token" field only when needed (validated before the clean slate), `_write_config_and_identity` writes it or adds the NOT READY warning; the base worker resets `install_warnings` and `show_finish_base` now renders warnings ("INSTALLED WITH N WARNING(S): NOT READY YET") instead of an unconditional DONE.
- Regression test: test_bug_hunt_2026_09_24_w2_onboarding.py::TestFleetCredential (8 tests incl. ::test_write_identity_keeps_a_per_editor_token, ::test_ensure_config_writes_the_pasted_token_and_a_blank_never_erases, ::test_an_older_dashboard_sends_no_kind_and_is_never_asked) - fail on HEAD (no kind key, helpers absent, identity rewrite drops the token).
- Tests run: as above. Rendered the install page (with the field) and the base finish page with the warning in a real Tk window (screenshots in the scratchpad): both fit, nav visible.
- Skew / deploy order: reads an optional key present since dash-core-2 (2026-08-21); an older dashboard sends none and the wizard behaves exactly as before. Writes config `report_token`, which every companion since 2026-08-17 already reads (identity.preferred_report_token). No order.
- OWED: none

## ui-onboarding-1 - Fixed 660x560 window clips CLOSE / BACK / log path
- Status: FIXED. Severity medium (CONFIRMED).
- Verified as: reproduced against HEAD's onboard.py in a real Tk window: install page requests 591 px, BACK unmapped (height 1); finish page with 5 real-length warnings requests 672 px, "Send these to your admin" and CLOSE unmapped.
- Fix: onboarding/onboard.py - new `_pack_bottom_bar(frame, bar)` packs every page's button bar with `before=` the page's first widget, so it is first in pack order and the expanding middle shrinks instead; used by `_nav_bar`, the interrupted page, the licence page and the wrong-account page. New `_fit_window` (after_idle) grows the window HEIGHT to the page's requested height, capped at screen height - 80, never shrinks, never widens.
- Regression test: test_bug_hunt_2026_09_24_w2_onboarding.py::test_every_bottom_bar_goes_through_the_helper, ::test_the_bar_is_packed_first_and_keeps_its_space (real Tk, skips without a display), ::test_fit_window_grows_height_only_and_is_capped_by_the_screen - fail on HEAD (no helper; bars packed on their own).
- Tests run: as above, plus the measurement script: with the fix and fitting DISABLED, the 660x560 window keeps BACK and CLOSE mapped (the log path / lower text is what clips); with fitting, install 660x706 and finish 660x706, nothing unmapped.
- Skew / deploy order: installer only.
- OWED: none

## ui-onboarding-2 - Licence page shows the raw draft EULA
- Status: FIXED (display). The agreement's WORDS are not changed: the body still names "Cablewrap Creative" (a retired brand, owner memory) and has bracketed `TODO(legal)` placeholders in sections 13-14; that is counsel's / the owner's text in docs/legal/EULA.md, a decision, not a code fix.
- Verified as: HEAD `show_eula` inserted `steps.EULA_TEXT` verbatim; EULA.md opens with the DRAFT FOR COUNSEL / TODO(legal) comment, uses `#`/`**` markup and has 14 U+2014.
- Fix: onboarding/steps.py `eula_display_text` (strips HTML comments, headings -> upper case for levels 1-2, drops bold/italic/code markup, links as "text (url)", em dash -> " - ", collapses blank runs) and `EULA_DISPLAY_TEXT`; onboard.py `show_eula` inserts the display form. The acceptance hash and `EULA_VERSION` still read the raw file, so the record matches what the companion checks. The companion's twin surface (popup.licence_dialog, ui-comp-windows-3) is c-ui's finding; this helper is a reference for it.
- Regression test: test_bug_hunt_2026_09_24_w2_onboarding.py::TestEulaDisplayText (4 tests) - fail on HEAD (no helper; page inserts EULA_TEXT).
- Tests run: as above; screenshot of the licence page shows clean text.
- Skew / deploy order: installer only; the acceptance record format and hash are unchanged.
- OWED: none (legal text: owner/counsel, docs/legal/EULA.md, no group).

## ui-onboarding-4 - winget Tailscale install: stale status, green on failure, join check cannot find the new exe
- Status: FIXED. Severity medium (CONFIRMED).
- Verified as: all three parts at HEAD: status computed once at render; `subprocess.run` return code ignored (green "finished" on any non-exception exit); Windows CLI candidates only the bare `tailscale`, resolved on the wizard's launch-time PATH, while `tailscale_installed` checks Program Files.
- Fix: onboarding/steps.py - `tailscale_exe_windows()`; `_tailscale_cli_candidates` tries it after the bare name on Windows; `winget_tailscale_outcome(returncode, installed)` - success is Tailscale on disk afterwards (winget's non-zero "nothing newer" counts as success), failure names the exit code and points at OPEN DOWNLOAD PAGE. onboarding/onboard.py - the status line is `self.ts_status_lbl`, refreshed by `_refresh_tailscale_status` after the winget run and on every CHECK CONNECTION.
- Regression test: test_bug_hunt_2026_09_24_w2_onboarding.py::TestTailscaleAfterWinget (3 tests) - fail on HEAD (second candidate never tried; helpers absent). Existing tailscale tests in test_steps.py / test_macos_steps.py unchanged and passing.
- Tests run: as above.
- Skew / deploy order: installer only.
- OWED: none

## bug-ops-4 - macOS --full uninstall reports the sign-in as removed without checking
- Status: FIXED. Severity low (unverified low; verified here).
- Verified as: HEAD macos_uninstall.sh section 5 loop did `rm -rf "$item"` then `removed+1` regardless and printed "removed your sign-in and settings ... including config.toml, identity.json"; the closing verdict never heard about it. Windows half fixed by install-onboard-3.
- Fix: installer/macos_uninstall.sh - `remove_profile_contents` removes everything except `state/`, RE-LISTS the directory, and either says what it removed or warns with each survivor and returns 1; section 5 sets `REMOVAL_INCOMPLETE` and `PROFILE_SURVIVED`; the "your saved sign-in is gone" line is conditional; the closing "NOT complete" line no longer names only `$CCSYNC_LOCAL`. Dry run unchanged ("would delete ...").
- Regression test: installer/tests/test_macos_uninstall_profile.sh (slices the function; shadows `rm` so config.toml refuses to go) - fails on HEAD (no function to slice; HEAD's loop by reading prints "removed" with a survivor).
- Tests run: `bash installer/tests/test_macos_uninstall_profile.sh` -> all passed; `bash -n` on both mac scripts OK; `bash installer/tests/test_macos_site_values.sh` -> pass. LF endings kept.
- Skew / deploy order: installer package only.
- OWED: release-tools - tools/run_all_tests.ps1 (the "installer/macos" row, ~line 198) and .github/workflows/ci.yml (~line 454) run only `installer/tests/test_macos_site_values.sh` by name; add `installer/tests/test_macos_uninstall_profile.sh` (or enumerate `installer/tests/test_*.sh`, as the .ps1 row already enumerates).

## logic-onboarding-3 - windows_upgrade.ps1 sends a licence-only problem to the full reinstall wizard
- Status: FIXED. Severity low (verifier DOWNGRADE).
- Verified as: HEAD step 6 `Start-Process`es onboard.exe for a missing/unreadable/older acceptance whenever not base rig / -SkipWizard, after relaunching a companion whose `_licence_watch` (app.py ~6348, started at ~11488) offers the same licence 3 s later. The -SkipWizard and summary copy ("all three tray lines read 'this machine isn't set up yet'") is stale since CR-88.
- Fix: installer/windows_upgrade.ps1 - new pure `Select-LicenceRoute` (none / tray / skip / base / dryrun / missing / wizard): a running relaunched companion (`$copySucceeded -and $relaunchAlive`) owns the question ("tray": tells the editor to accept in its window or Tray > Accept the licence agreement; the wizard is not launched); the wizard stays only for a machine where nothing else can ask. Stale tray-line copy replaced in the -SkipWizard warning and the summary.
- Regression test: installer/tests/Test-LicenceRoute.ps1 (9 route cases + two source pins) - fails on HEAD (no function to slice). Test-LicenceGate.ps1 (slices the same region) still passes.
- Tests run: `powershell -NoProfile -ExecutionPolicy Bypass -File installer\tests\Test-LicenceRoute.ps1` -> all passed; Test-LicenceGate.ps1 -> pass; Test-ForeignDriveMiss.ps1 -> pass; PowerShell parser on windows_upgrade.ps1 -> 0 errors.
- Skew / deploy order: package-only; the relaunched companion is the package's own build, which has `_licence_watch` (CR-27). A failed copy (previous build relaunched) still takes the wizard route, since that build may predate the watch.
- OWED: none
- Review round (2026-09-25, adversarial reviewer: PROBLEM, accepted): in a `-DryRun`, `$copySucceeded` is set true and `$relaunchAlive` keeps its initial true (the dry-run branch never clears it), so every dry run routed to "tray" and printed "The companion that was just relaunched is asking for it now" although nothing was relaunched; the "dryrun" route was dead. The test tabled Dry only with Running=$false, a combination the script never produces. Also a literal TAB in the new comment ("installer<TAB>ests", an unescaped `\t`) and the comment named Test-LicenceGate.ps1 instead of Test-LicenceRoute.ps1. Fix: `Select-LicenceRoute` returns "tray" only for `$CompanionRunning -and -not $DryRun`, and the call site passes `-not $DryRun` too; the dryrun branch now says what a real run would do ("a real run would leave this to the relaunched companion's licence window, and launch onboard.exe only if that companion failed to start") and no longer sets `$wizardLaunched`, so the summary no longer says "The setup wizard is open" after a dry run (that half was pre-existing at HEAD); comment fixed (no TAB, right file name). Test-LicenceRoute.ps1 gains the three combinations step 6 really produces in a dry run (Running+Dry -> dryrun, +Skip -> skip, +Base -> base), a pin that the dryrun branch does not set `$wizardLaunched`, and a no-literal-TAB check on windows_upgrade.ps1; against the pre-review script those 4 checks fail (measured on a scratch copy), against the fix all pass. Test-LicenceGate.ps1, Test-ForeignDriveMiss.ps1: pass; parser: 0 errors. Non-dry-run routing unchanged.

## logic-onboarding-4 - The wizard and both bootstraps still ask for a "TrueNAS username and password"
- Status: FIXED. Severity low (verifier DOWNGRADE).
- Verified as: onboard.py welcome (both platforms) and sign-in page, windows_bootstrap.ps1:2463, macos_bootstrap.sh:2836 at HEAD.
- Fix: neutral copy ("the username and password your admin set up for you"), true on TrueNAS, Synology and local-auth sites alike: onboarding/onboard.py (welcome x2, sign-in page, module docstring), installer/windows_bootstrap.ps1 and installer/macos_bootstrap.sh end banners (one line each; ui-onboarding-11 in chunk 2 owns the rest of that banner). Not `site.server_phrase`: on a local-auth site the account is not a server account either.
- Regression test: test_bug_hunt_2026_09_24_w2_onboarding.py::test_no_storage_vendor_in_the_sign_in_copy - fails on HEAD.
- Tests run: as above.
- Skew / deploy order: copy only.
- OWED: none

## logic-onboarding-5 - The editor finish page contradicts itself about signing in and asks for a key already sent
- Status: FIXED. Severity low (unverified; verified here from the source text).
- Verified as: HEAD hint "One more step: right-click the tray icon -> "Sign in..." is already done for you"; heading "SEND THESE TWO VALUES" even when `_offer_ssh_key` had posted the key.
- Fix: onboarding/steps.py `finish_page_copy(ssh_key_sent, username)`; onboarding/onboard.py records `self.ssh_key_sent` in `_offer_ssh_key` and `show_finish` renders the helper's heading/intro/hint/send line. Key sent: "DONE: SEND YOUR DEVICE ID TO YOUR ADMIN", the hint says the key is waiting on the dashboard's Users page (the key field stays as a fallback). Not sent: the two-values copy, with "You are already signed in." Warning path unchanged.
- Regression test: test_bug_hunt_2026_09_24_w2_onboarding.py::TestFinishPageCopy (4 tests) - fail on HEAD.
- Tests run: as above; screenshot of the key-sent finish page checked.
- Skew / deploy order: copy only.
- OWED: none

## Chunk 2 of 2

## ui-onboarding-3 - "Last install did not finish" page and the close-mid-install warning always say "no P drive"
- Status: FIXED. Severity low (verifier DOWNGRADE from medium, med-18).
- Verified as: HEAD `show_interrupted` and `steps.install_close_warning` always said "no CCSync and no <letter> drive"; `build_cleanup_plan` sets `unmount_p=(role == "editor")` and the macOS plan `unmount_p=False`, so a base install or a Mac never loses the drive; the breadcrumb's `clean_slate:<role>` phase was written (onboard `_clean_slate`) and never read. Resume radio: chunk 1 seeds it from config.toml `mode`, but a first run closed during the clean slate (before `ensure_config`) has no `mode`, so it still landed on "editor".
- Fix: onboarding/steps.py - `install_close_warning(drive_letter, role=None, is_macos=None)` (no drive named for base or a Mac; `role=None` keeps the editor wording), `_half_installed_loss`, `_drive_word`, new `breadcrumb_role(crumb)` and `interrupted_install_message(drive, role, is_macos)` (base: "Your P: drive mappings were not touched, but the companion is not running"; Mac base: NAS mounts; unreadable breadcrumb: the editor wording, the worse of the two). onboarding/onboard.py - `show_interrupted` renders the helper with the breadcrumb's role; `__init__` preselects `role_var` from the breadcrumb role on an interrupted run; `_on_close_request` passes `self._effective_role()` and `IS_MACOS`.
- Regression test: onboarding/tests/test_bug_hunt_2026_09_24_w2_onboarding.py::TestInterruptedInstallCopy (5 tests, one a real Tk wizard opened on a `clean_slate:base` breadcrumb: page text and preselected radio) - fail on HEAD (helpers absent; the real page says "no P drive" and the radio is "editor"; measured on a `git archive HEAD` copy). test_steps.py::test_install_close_warning_uses_the_sites_drive_letter CHANGED: it now passes `is_macos=False`, since on the macOS CI job the default wording correctly names no drive.
- Tests run: see "Chunk 2: tests run" below.
- Skew / deploy order: wizard only; the breadcrumb format is unchanged (it already carried the role).
- OWED: none

## ui-onboarding-5 - Finish page copy contradicts itself and the rest of the install
- Status: FIXED. Parts (a) the self-contradicting sign-in hint and (b) asking for an SSH key `_offer_ssh_key` had already sent were fixed in chunk 1 as logic-onboarding-5 (`steps.finish_page_copy`); part (c) is fixed here.
- Verified as: (c) at HEAD `finish_warning_lines`' truncation line said "use COPY LOG on the previous page", and both finish pages are built with `_nav_bar(back=None)`: no BACK, no COPY LOG. With more than six warnings the full list was out of reach on the page that says "send them this list".
- Fix: onboarding/onboard.py - `_finish_nav(frame)` (CLOSE plus a COPY LOG of its own, bottom-left, rebinding `self.copy_log_btn`), used by `show_finish` and `show_finish_base`; `_nav_bar` publishes its bar on `self._last_nav_bar`. `_on_copy_log` already reads the log FILE first and survives the destroyed log widget (TclError). onboarding/steps.py `finish_warning_lines`: "(use COPY LOG at the bottom of this page, or the file named there)".
- Regression test: test_bug_hunt_2026_09_24_w2_onboarding.py::TestFinishPageCopyLog (truncation copy; real Tk: both finish pages carry a COPY LOG button; invoking it puts the log on the clipboard) - fail on HEAD (no button, "previous page"). Rendered: the finish page with 8 warnings (scratchpad p4_finish_warnings.png): COPY LOG bottom-left, CLOSE right with its focus ring, window grown to 609 px by ui-onboarding-1's fit.
- Skew / deploy order: wizard only.
- OWED: none

## ui-onboarding-6 - Welcome says you need "nothing else" but the next page requires a dashboard URL
- Status: FIXED. Severity low (unverified; verified here).
- Verified as: HEAD welcome text (both platforms) ended "-- nothing else"; `_on_role_next` refuses NEXT on an empty url and `DEFAULT_DASHBOARD_URL` is `os.environ.get("CCSYNC_DASHBOARD_URL", "")`. Chunk 1 (logic-onboarding-4) had only taken "TrueNAS" out of the same sentence.
- Fix: onboarding/steps.py new `welcome_text(drive_letter, is_macos)`: "You'll need two things from your admin: the dashboard address, and the username and password they set up for you." onboard.py `show_welcome` renders it (the two inline copies are gone).
- Regression test: test_bug_hunt_2026_09_24_w2_onboarding.py::TestWelcomeNamesTheDashboardAddress (both platforms + source pin) - fail on HEAD. Rendered (p2_welcome.png): fits, NEXT has focus.
- Skew / deploy order: copy only.
- OWED: none

## ui-onboarding-7 - Role-override note speaks the internal words 'base'/'editor'
- Status: ALREADY_FIXED (by chunk 1 of this wave, logic-onboarding-1, uncommitted in the working tree; not by a commit).
- Verified as: the note ("note: you picked '{picked_role}', but the dashboard says ...", HEAD onboard.py:858) no longer exists: logic-onboarding-1 removed the account-role override it explained, so the install runs as the radio the person picked and there is no override to describe. The destructive direction the hunter worried about (a wired office machine losing its NAS mapping) is guarded by `p_mapping_is_ours` in the cleanup and `$PIsForeign` in the bootstrap.
- Fix: none here.
- Regression test: chunk 1's test_bug_hunt_2026_09_24_w2_onboarding.py::TestTheRadioDecidesTheMachinesRole::test_the_wizard_seeds_the_radio_and_no_longer_snaps_it pins that the note is gone.
- Skew / deploy order: n/a.
- OWED: none

## ui-onboarding-8 - Keyboard: no Enter on sign-in, no initial focus, no visible focus ring on any button
- Status: PARTIAL (the wizard is fixed; the companion's own windows share `theme.neon_button`, which is c-ui's file: OWED).
- Verified as: HEAD onboard.py had no `bind`/`focus` call; `theme.neon_button` draws `highlightthickness=0`; a tk.Button answers Space, not Enter.
- Fix: onboarding/onboard.py - `_button()` wraps `theme.neon_button` with `takefocus=1`, a 1 px focus ring (`highlightcolor=theme.TEXT`, `highlightbackground=theme.BG`, so invisible until focused) and Return/KP_Enter -> `invoke()` (a disabled button ignores it); every wizard button goes through it. `_bind_enter()`: Enter in the sign-in fields is VERIFY, Enter in the dashboard url field is NEXT. Initial focus: `_nav_bar` focuses an enabled NEXT/primary; the role page focuses an empty url field; the sign-in page focuses the first empty field; the Tailscale page focuses CHECK CONNECTION and moves focus to NEXT once it enables. The licence page deliberately focuses nothing (Enter must not accept an agreement).
- Regression test: test_bug_hunt_2026_09_24_w2_onboarding.py::TestKeyboard (5 tests: source pin that `theme.neon_button` appears only in `_button`; real Tk: ring + takefocus + Enter presses and a disabled button ignores Enter; sign-in focus and Enter; a filled username starts on the password; url field Enter and focus, and a prefilled url starts on NEXT) - fail on HEAD (no `_button`; the requested focus is the root). The four real-Tk focus tests skip on darwin (a CI Mac does not give keyboard focus to a background Tk app); the same code is checked on the Windows job.
- Skew / deploy order: wizard only.
- OWED: c-ui, companion/src/ccsync_companion/theme.py `neon_button` (~436-449): give the companion's windows the same keyboard affordance (`takefocus=1`, `highlightthickness=1`, `highlightbackground=BG`, `highlightcolor=TEXT`, and bind Return/KP_Enter to `btn.invoke()`). onboarding's `_button` then becomes redundant but harmless (it re-sets the same options).

## ui-onboarding-10 - Wrong-account refusal tells the person to sign in as the account that is already signed in
- Status: FIXED. Severity low (unverified; verified here).
- Verified as: HEAD `steps.console_user_mismatch` and windows_bootstrap.ps1 `Test-ConsoleUserMismatch` both ended "Sign in as <signed_in> and run it again", where <signed_in> is by definition already signed in; the cause is how the installer was started (a UAC credential prompt answered with another account, or Run as administrator).
- Fix: onboarding/steps.py `console_user_mismatch`: "... <signed_in> does not need to sign in again: close this and start the installer again by double-clicking it, and do not choose Run as administrator or type another account's password if Windows asks (it does not need administrator rights)." installer/windows_bootstrap.ps1 `Test-ConsoleUserMismatch`: the same first two sentences, then the hand-run form ("run the script again from a PowerShell window opened normally in <signed_in>'s session, not through Run as administrator or with another account's password"). The refusal rule itself (casefold, domain/UPN folding, unknown = no refusal) is unchanged.
- Regression test: test_bug_hunt_2026_09_24_w2_onboarding.py::TestWrongAccountRefusal (wizard wording + the .ps1 twin) - fail on HEAD. test_steps.py::test_console_user_mismatch_refuses_and_names_both_accounts CHANGED on purpose (it pinned the old exact sentence). installer/tests/Test-ConsoleUser.ps1 unchanged and passing (its `-like` pattern still matches).
- Skew / deploy order: copy only.
- OWED: none

## ui-onboarding-11 - Bootstrap end banner streams "remaining manual steps" into a wizard run that has done them
- Status: FIXED. Severity low (unverified; verified here).
- Verified as: HEAD windows_bootstrap.ps1 (~2456) and macos_bootstrap.sh (~2825) printed "Remaining manual steps (see docs/EDITOR_SETUP.md): 1. tailscale up 2. ssh-keygen ... send the .pub file 3. SIGN IN" unconditionally; `run_bootstrap` streams every line into the wizard's log and the saved install log. By then the wizard has joined Tailscale (the Tailscale page gates NEXT on a live check), made the key (`ensure_ssh_key`) and offered it (`_offer_ssh_key`), and written identity.json.
- Fix: onboarding/steps.py `FROM_WIZARD_ENV = "CCSYNC_FROM_WIZARD"`, set to "1" in the bootstrap's environment by `run_bootstrap` (both platforms). installer/windows_bootstrap.ps1 new pure `Get-FirstSetupSteps -FromWizard -KeyFilePath` (wizard: "1-3. DONE BY THE SETUP WIZARD ... The wizard's last page says whether the key still has to be sent"; hand run: the old lines verbatim); the banner prints it. installer/macos_bootstrap.sh new `print_first_setup_steps` (same; a missing companion stays step 3 "INSTALL THE SYNC APP" on a wizard run too; `${VAR:-}` for `set -u`); LF kept. Steps 4 onward unchanged.
- Regression test: test_bug_hunt_2026_09_24_w2_onboarding.py::test_the_wizard_tells_the_bootstrap_it_ran_it, ::test_both_bootstraps_read_the_wizard_marker; new installer/tests/Test-FirstSetupSteps.ps1 (parser-extracted table + call-site pin); new installer/tests/test_macos_first_steps.sh (sliced function: wizard / wizard without companion / hand / unset under set -u / call site) - all fail on HEAD (no env var set, no function to extract or slice).
- Skew / deploy order: an environment variable, not a flag, so a bootstrap that predates it ignores it and a hand run without it prints the old banner. The frozen wizard ships its own paired script (`find_bootstrap_script` prefers `_MEIPASS`). No order.
- OWED: release-tools - tools/run_all_tests.ps1 (the "installer/macos" row) and .github/workflows/ci.yml run only `installer/tests/test_macos_site_values.sh` by name; add `installer/tests/test_macos_first_steps.sh` (the same item as chunk 1's `test_macos_uninstall_profile.sh`; enumerating `installer/tests/test_*.sh` covers both). `Test-FirstSetupSteps.ps1` needs nothing: the .ps1 row enumerates the directory.

## ui-onboarding-12 - VERIFY & CONTINUE and CHECK CONNECTION stay clickable while their request is in flight
- Status: FIXED. Severity low (PLAUSIBLE in the hunt; reproduced here).
- Verified as: reproduced on a `git archive HEAD` copy with a real Tk wizard and a blocking `verify_account`: the button never disables and a second click starts a second verification; a success that arrives after BACK calls `show_install` from the role page. Same shape for CHECK CONNECTION.
- Fix: onboarding/onboard.py - `_verify_in_flight` / `_check_in_flight` guards (a second click is ignored; on the sign-in page it says "still verifying the last attempt..."); the button is greyed out via `_set_busy` and restored when the result lands; each result is dropped if `self.page_frame` is no longer the page that asked (no jump to the install page from wherever the editor went, no password cleared on a page that moved on); the workers catch their own exceptions so a flag can never stay set; the dashboard url is read on the Tk thread before the worker starts (the worker no longer touches a Tk variable). Worth knowing: the old stale-result path could not rebuild a LIVE install page (BEGIN INSTALL disables BACK), so the real risks were the double token and the page jump, not a replaced log widget mid-install.
- Review round (2026-09-25, adversarial reviewer: PROBLEM, accepted): the guards were booleans, so BACK during a sign-in and NEXT again (which builds a NEW sign-in page) left a VERIFY that was not greyed out but only printed "still verifying the last attempt..." and returned; the old result was then dropped (different page) after clearing the flag, and nothing told the editor to press VERIFY again, so the new page read "still verifying" forever. CHECK CONNECTION had the same shape (silently ignored until the abandoned check returned, up to ~25 s). Fix: onboard.py replaces `_verify_in_flight` / `_check_in_flight` with `_verify_page` / `_check_page`, the page that started the request (None when idle). The guard refuses only a second request from the SAME page (whose button is already greyed out, so only Enter reaches it, and that page's own result will update it); a revisited page starts its own request at once. A result clears the slot only if it is still its own page's, so an abandoned request landing mid-way through the new one does not re-enable the new page's button, and it is dropped as before. Cost, deliberately accepted: an abandoned sign-in can still complete server-side (one unused identity token), the same as closing the window mid-verify at HEAD. New tests TestOneRequestAtATime::test_a_revisited_signin_page_is_not_stuck_behind_the_old_attempt and ::test_a_revisited_tailscale_page_checks_at_once (held workers, a `_pump_until` helper because `_settle` waits for every worker): the new page's button is enabled, a click starts a second request, the old result neither re-enables the new button nor writes its error, and the new result lands on the new page. Both fail against the pre-review code (measured on a scratch copy with only this change reverted: the second request never starts); the three original tests still pass on both.
- Regression test: test_bug_hunt_2026_09_24_w2_onboarding.py::TestOneRequestAtATime (3 tests, behavioural only, driven through the real Tk mainloop so HEAD's thread-side Tk reads work: a double click verifies once and the button disables/re-enables; a result for a page that was left is dropped; CHECK CONNECTION runs once and enables NEXT) - all three fail on HEAD (measured).
- Skew / deploy order: wizard only.
- OWED: none

## Chunk 2: tests run
- `python -m pytest tests -q` in onboarding/ -> 484 passed, 1 skipped (the group's test file alone: 52 passed, five consecutive runs). After the ui-onboarding-12 review round: 486 passed, 1 skipped; the group file 54 passed, three consecutive runs. The chunk-2 tests against a `git archive HEAD` copy of onboarding/installer/companion: every one fails (source/helper tests by assertion or missing helper; the Tk tests by behaviour, the fixture tolerating HEAD's missing `initial_install_role`).
- `powershell -NoProfile -ExecutionPolicy Bypass -File installer\tests\<each Test-*.ps1 that reads windows_bootstrap.ps1>`: Test-BinDirLeftovers, Test-ConsoleUser, Test-DriveMapParser, Test-FirstSetupSteps (new), Test-ForeignDriveMiss, Test-SmbShareGone, Test-UninstallEntry -> all exit 0. PowerShell parser on windows_bootstrap.ps1 -> 0 errors; no literal TAB in the file (chunk 1's trap; I hit it once while writing the new comment and removed it).
- `bash installer/tests/test_macos_first_steps.sh` (new), `test_macos_site_values.sh`, `test_macos_uninstall_profile.sh` -> all pass; `bash -n installer/macos_bootstrap.sh` OK; no CR in either .sh.
- Rendered in a real Tk window (scratchpad p1_interrupted_base.png, p2_welcome.png, p3_signin_focus.png, p4_finish_warnings.png): all fit, nav bars visible, the focus ring shows on the focused button.
- Note for the gate: creating a Tk interpreter on this machine intermittently fails with "couldn't read file ... init.tcl: No error" when many are made back to back; the new fixture retries five times before skipping, so the checks do not go quietly vacuous.

## Owed round

### ui-onboarding-9 (from c-ui) - wizard input and box outlines below 3:1
- Status: FIXED (the onboarding half; c-ui fixed the text half and added `theme.FIELD_BORDER`).
- Verified as: REAL. HEAD onboard.py had `highlightbackground=theme.RED_DIM` on four outlines: `_entry` (every wizard Entry), the licence text box frame, the install log frame and the readonly copy field on the finish page. Measured here: FIELD_BORDER `#6f6f7a` is 3.98:1 on BG and 3.63:1 on FIELD (RED_DIM was 1.86:1 / 1.69:1 per c-ui).
- Fix: onboarding/onboard.py - those four now use `theme.FIELD_BORDER`; `highlightcolor=theme.RED` (the focus outline) unchanged, so focus still reads as a change from grey to red. The RULE label under each page heading stays RED_DIM, as asked (decoration, not a boundary). No other `highlightbackground=theme.RED_DIM` remains.
- Regression test: test_bug_hunt_2026_09_24_w2_onboarding.py::test_wizard_input_and_box_outlines_use_the_field_border_colour - fails on HEAD (4 RED_DIM outlines in `git show HEAD:onboarding/onboard.py`, and HEAD's theme has no FIELD_BORDER).
- Tests run: `python -m pytest tests -q` in onboarding/ -> 487 passed, 1 skipped.
- Skew / deploy order: none; the wizard freezes the companion's theme.py from the same tree.
- OWED: none
