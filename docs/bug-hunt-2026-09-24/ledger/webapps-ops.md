# Ledger: webapps-ops (2026-09-24 fix wave)

## bug-music-ytdl-1 - The "share vanished" guard runs after the download phase has created the project folder, so it can never fire
- Mechanism (as you traced it): as reported. `_phase_download` ran `ensure_outdir(outdir)` (makedirs of `<PROJECTS_ROOT>/<label>/Youtube/<term>`) and `_sweep_stale` first. The ytdl-web-5 guard (`routes_api.tree_missing_note` -> `tree_is_gone`) ran about 60 lines later, and `tree_is_gone`'s first test is `project_dir.is_dir()`, which the makedirs had just made true. So on an emptied mount point the guard always said "present". `_no_room_note`'s own copy of the check ran after the same makedirs. The folder left in the container overlay then also made the press-time `_refuse_if_the_tree_is_gone` say "present" on every later press.
- Fix: `ytdl/web/ytdlweb/worker.py` `_phase_download` (about lines 1617-1636 and 1697). The tree question is asked before anything is created. When the tree is gone, `ensure_outdir` and `_sweep_stale` are skipped, so nothing lands in the overlay. The phase then fails at the existing ytdl-web-5 spot with the same sentence, and the rows stay `pending` so RETRY re-queues them. The lease reclaim and the requester's claim grace still run in between, on purpose: a companion writing to its own disk does not need this container's mount, and `_landed_file`/`_id_bearing_files` already treat a missing directory as empty. I did not move the check to the very top, because that would change when a created-local job can still be claimed.
- Regression test: `ytdl/web/tests/test_bug_hunt_2026_09_24_webapps_ops.py::test_the_download_phase_refuses_a_vanished_share_it_would_have_created`. It drives `_phase_download` itself, not the helper. Fails on HEAD because the phase reached yt-dlp: the stub recorded a download into `<root>/2026/FF5/Energy Transition/Youtube`. I ran it against `git show HEAD:ytdl/web/ytdlweb/worker.py` swapped in place and then restored.
- Other tests run: `cd ytdl/web; ..\..\dashboard\.venv\Scripts\python.exe -m pytest tests/test_bug_hunt_2026_09_24_webapps_ops.py tests/test_worker.py tests/test_local_download.py tests/test_bug_hunt_2026_09_18_webapps_tools.py tests/test_bug_hunt_2026_09_18b_ytdl.py tests/test_bug_hunt_2026_09_11_ytdl_web.py tests/test_bug_hunt_2026_09_11b_ytdl_web.py tests/test_retry_failed.py tests/test_no_em_dash.py -q` -> 277 passed.
- Skew / deploy order: server-only (it ships in the dashboard image). No wire change. Any overlay folder a pre-fix run already created stays where it is until the container is recreated. Until then that folder still answers "present", which is the old behaviour, not a new one.
- OWED: none

## bug-ops-1 - publish_db.py --rollback picks the .prev-<ts>-wal sidecar and renames it over the live index
- Mechanism (as you traced it): as reported. The publish swap moves `<target>-wal/-shm` to `<target>.prev-<ts>-wal/-shm`. `list_prev_command`'s `find -name '<file>.prev-*'` lists those sidecars too, and `newest_prev` took the last one by plain sort. A bare name sorts before its own `-shm` and `-wal` names, so the newest entry was the WAL journal. `build_rollback_script` then ran `mv <prev>-wal <target>` and printed "rolled back". The same wrong file could also be typed by hand with `--from-prev`.
- Fix: `server/publish_db.py`. `newest_prev` now drops every name that ends in a sidecar suffix (the new `is_sidecar_name`, keyed on `SIDECARS`), so only a database can be a candidate; its sidecars still travel with it inside `build_rollback_script`. `do_rollback` refuses a `--from-prev` that names a sidecar before anything runs on the NAS, and says to name the `.prev-<ts>` file itself.
- Regression test: `server/tests/test_bug_hunt_2026_09_24.py`, three tests: `test_the_newest_prev_is_a_database_never_its_wal`, `test_the_rollback_script_moves_the_database_over_the_live_path` (end to end through `do_rollback`, asserting the script it sends restores from the bare `.prev-<ts>`), and `test_a_sidecar_named_by_hand_is_refused_before_the_nas_is_touched`. All three fail on HEAD, where the chosen prev was `...T101500-wal` and the sidecar `--from-prev` went through. I ran them against `git show HEAD:server/publish_db.py` swapped in place and then restored.
- Other tests run: from Git Bash, `cd server; ../dashboard/.venv/Scripts/python.exe -m pytest tests/test_backup_restore.py tests/test_broll_drain.py tests/test_bug_hunt_2026_08_21.py tests/test_bug_hunt_2026_09_18_webapps_tools.py tests/test_bug_hunt_2026_09_24.py -q` -> 123 passed.
- Skew / deploy order: operator tooling on the base rig only. Nothing is deployed.
- OWED: none

## logic-release-1 - Documented key rotation signs the overlap release with the NEW key, and the REL-7 refusal tells the operator to override it
- Mechanism (as you traced it): as reported. RELEASE.md step 1 said `release_key.py new --force` then `bake --add`, "with the OLD key still signing". But `cmd_new --force` moves the old key to `release.key.superseded` and writes the new key at `release.key`, the path every signer reads (`key_path()`: sign_release, publish_feed's record and channel signatures, publish_latest's preflight). So the "overlap" build was signed with a key the current fleet does not trust. REL-7 caught it correctly, but its text said "a rotation costs an overlap release: bake --add, ship THAT ... Pass --allow-key-rotation if this is that deliberate step". An operator who had just done exactly that would pass the flag and strand the fleet.
- Fix:
  - `docs/RELEASE.md` "Rotating" is rewritten into a procedure that works. Step 1 makes the new key at a SIDE path (`release_key.py --path ...\release-next.key new`, `backup`, `bake --add`) and ships as usual, so `release.key`, still the OLD key, signs the overlap build and REL-7 passes with no override. Step 2 adds the new public key to `DASH_RELEASE_PUBKEYS` on every dashboard before anything is signed with it, because the feed channel is signed by the same key and a dashboard that pins only the old key would reject the whole channel. Step 3 switches the keys by moving the files once the fleet runs the overlap build, then drops the old key a release later. The doc says outright that `new --force` is not a rotation step, and that `--allow-key-rotation` does not perform a rotation.
  - `tools/publish_feed.py` has a new `key_rotation_refusal()`, used by the REL-7 raise. When the refused build already bakes both the current key and its own signing key, the refusal says it IS the overlap release, that it must be SIGNED WITH THE OLD KEY, names that key id, and says how to use it (`release.key`, `CCSYNC_RELEASE_KEY` or `--key`). Otherwise it points at the two-release procedure. In both cases the flag is described as publishing a build the fleet cannot take, not as a rotation step.
  - `tools/release_key.py` `cmd_new`: `--force` on the signing path now prints "THE NEW KEY NOW SIGNS EVERYTHING", says that is the wrong order for a rotation, and says how to put the old key back. A key made at a side path, while a signing key exists, is announced as a SIDE key, and the output gives the `--path ... bake --add` next step.
- Regression test: `tools/tests/test_bug_hunt_2026_09_24_webapps_ops.py`:
  - `test_the_documented_rotation_ships_the_overlap_without_an_override` reads the `release_key.py` commands out of RELEASE.md's Rotating section and executes them against a tmp key, a tmp `release_pubkey.py` copy and a tmp feed. It then publishes the overlap build with no override (it must be signed by the old key id) and, after the step 3 file switch, the next build (signed by the new key id), again with no override. On HEAD the section held no runnable command line, so the test fails.
  - `test_an_overlap_signed_by_the_new_key_is_refused_toward_the_old_key` runs HEAD's step 1 order. On HEAD it fails: `new --force` gave no warning, and the refusal said "if this is that deliberate step" with no "SIGNED WITH THE OLD KEY".
  - `test_a_build_that_does_not_bake_the_old_key_is_sent_to_the_procedure` fails on HEAD because the old text neither named "Rotating" nor said "does not perform a rotation".
  - `test_the_runbook_no_longer_starts_a_rotation_with_new_force` fails on HEAD because step 1 was `new --force`.
  - I ran all four against the HEAD copies of `publish_feed.py`, `release_key.py` and `RELEASE.md`, swapped in place and then restored. All four failed.
- Other tests run: `cd tools; ..\dashboard\.venv\Scripts\python.exe -m pytest tests/test_publish_feed.py tests/test_release_key.py tests/test_release_scripts.py tests/test_publish_latest.py tests/test_sign_release.py tests/test_release_notes_flag.py tests/test_docs_index.py tests/test_bug_hunt_2026_09_24_webapps_ops.py -q` -> 212 passed, 1 failed. The failure is `test_docs_index.py::test_every_doc_is_listed`, and it is not mine: `docs/README.md` does not list the orchestrator's untracked `docs/bug-hunt-2026-09-24.md`. It fails the same way without my changes.
- Skew / deploy order: release tooling and docs only. No record or channel format change, and the refusal still fires on the same condition and exits the same way.
- Review round (2026-09-24, reviewer logic-release-1):
  - Finding 1 (pathway A step 3 was false): the reviewer is right. `build_editor_package.ps1`'s REL-7 compares this rig's key with the key that SIGNED the current build (`$current.pubkey_id`); the dashboard stores no baked list. After the switch the current build is the overlap build, signed by the OLD key, so the first new-key build through `ship.cmd` is refused. I took option (b), because (a) is a dashboard schema/wire change plus a ps1 change, both outside my group. `docs/RELEASE.md` step 3 now splits by pathway: pathway B passes with no override (publish_feed checks `baked_pubkey_ids`); pathway A refuses that one build, and the doc says `-AllowKeyRotation` is correct there only when the refusal names the OLD key's id as the current build's signer (`release_key.py --path ...release.key.superseded pubkey` prints it), this rig's id is the NEW key's, and the version named as current is the overlap build or later. Step 1 now says "no override is ever needed for the overlap release" (true on both pathways). The paragraph after the section no longer says "a correct rotation never trips the check"; it lists the override's only two uses. `tools/ship.ps1`'s `-AllowKeyRotation` help says the same thing.
  - Finding 2 (the ps1 refusal text; RELEASE.md:135-136): the ps1 is the installer group's file, so its text is still OWED (below, now with exact text). RELEASE.md:135-136 no longer calls the flag "the deliberate override"; it says it publishes anyway, is not a rotation step, and a rotation needs it once, on pathway A only.
  - Finding 3 (`new --force` then "Next: bake"): fixed. `cmd_new` prints no "Next: bake" after the THE NEW KEY NOW SIGNS EVERYTHING warning (`elif not (args.force and path == signer)`). A first key with no `--force` still gets the prompt.
  - Finding 4 (the side key's backup overwrote `backup.json`): fixed. `backup_record_path` keeps `backup.json` for the signing key's own name (`release.key`, the path `ship.ps1:593` reads) and gives any other key file its own `<name>.backup.json`. Step 3 moves the records with the keys (`backup.json` to `release.key.superseded.backup.json`, then `release-next.key.backup.json` to `backup.json`).
  - New regression tests in `tools/tests/test_bug_hunt_2026_09_24_webapps_ops.py`: `test_step3_says_pathway_a_needs_the_override_once_and_when` (models the ps1's signed-by comparison on the real overlap record and pins the doc and the ship.ps1 help), `test_new_force_on_the_signing_path_does_not_then_say_bake`, `test_backing_up_the_side_key_keeps_the_signing_keys_record`, plus the guard `test_a_first_key_still_gets_the_bake_prompt`. Ran the file in a scratch copy with HEAD's `release_key.py`, `publish_feed.py`, `ship.ps1` and `RELEASE.md`: 7 failed, and the 1 that passed is the guard, which is meant to pass on both. Against my first-round files the three new ones fail too, by the lines quoted in the review ("passes again without an override", the unconditional Next line, the shared `backup.json`).
  - Tests re-run: `cd tools; ..\dashboard\.venv\Scripts\python.exe -m pytest tests/test_publish_feed.py tests/test_release_key.py tests/test_release_scripts.py tests/test_publish_latest.py tests/test_sign_release.py tests/test_docs_index.py tests/test_bug_hunt_2026_09_24_webapps_ops.py -q` -> 211 passed, 1 failed (`test_docs_index`, the orchestrator's unlisted `docs/bug-hunt-2026-09-24.md`, unchanged from round 1).
  - Also repaired while here: a scripted edit of RELEASE.md briefly wrote CRLF endings; the file is back to LF, and the diff is only the rotation text.
- OWED:
  - installer group (no builder this wave): `installer/build_editor_package.ps1` lines 460-462 still say "A rotation costs an overlap release: bake --add, ship THAT ... Pass -AllowKeyRotation if this IS that deliberate step". Until that changes, the finding is fixed for pathway B and in the runbook, but the refusal an operator reads on pathway A still invites the override on the wrong build. Suggested replacement for those three lines: "A rotation's overlap release is signed with the OLD key ($currentKey), not this one: put that key back at release.key and ship again (docs\RELEASE.md, Rotating). -AllowKeyRotation does not perform a rotation. Its one rotation use is the FIRST build after the key switch (step 3), when v$($current.version) is the overlap build and $currentKey is the old key. Nothing was built." The proper fix is option (a) from the review: carry `baked_pubkey_ids` into the dashboard's package record (optional on read, deploy the dashboard first) and have the ps1 compare against that list, as `publish_feed.py` does; then pathway A needs no override at all and step 3's pathway A paragraph can go.
  - orchestrator: add `bug-hunt-2026-09-24.md` to `docs/README.md` (the red `test_docs_index`).

- Round 2 review (2026-09-25, second rework, builder webapps-ops). The reviewer confirmed round-1 items 1, 3 and 4 fixed and item 2 landing through the installer builder's `build_editor_package.ps1` edit (the "Round 2 (owed-installer)" section below). Three problems remained. All three were real, and all three are fixed:
  - (A) The trust list on this studio's dashboard, fixed in the code as well as the runbook. `tools/load_secrets.ps1` set `DASH_RELEASE_PUBKEYS` to `release.key`'s public half ONLY, and `server/install_dashboard_app.py:2623` writes that env into the container on every `ship.cmd` dashboard deploy. So each ship in a rotation's window redeployed the studio dashboard trusting one key. After the switch that meant not the old key, even though the current build, the vendor channel and the OTA code trees (`select_code_root.py`) are still signed by it. That is exactly what step 2 exists to prevent. The fix:
    - `tools/release_key.py` gets a new `trusted` subcommand (`trusted_key_paths` / `cmd_trusted`). It prints `release.key`'s public half, then the halves of `release-next.key` and `release.key.superseded` whenever they sit beside it, deduplicated. `--quiet` prints the comma-joined value. An unreadable neighbour exits with `read_secret`'s message and is never skipped, because a list quietly missing half a rotation is the stranding it exists to prevent.
    - `load_secrets.ps1` calls `trusted --quiet` instead of `pubkey --quiet`. With more than one key it reports "N keys: a key rotation is under way". The file stays CRLF, as its eol attr requires.
    - The window is now defined by the files on disk, so the runbook needs no hand-set env line (a hand-set value is what the next ship deploys). RELEASE.md step 2 splits in two. For this studio's dashboard: reload `load_secrets.ps1` after step 1, never hand-set the value, and on pathway B run `ship.cmd -DashboardOnly` once. For customer dashboards: add the key by hand, as before. Step 3 says to keep `release.key.superseded` on the rig until step 4, because while it is there the old key stays trusted. The old trailing "drop" sentence becomes step 4: bake without `--add` in a later release, then move `release.key.superseded` (and its backup record) off the rig, reload and ship, and edit customer dashboards by hand.
    - Not changed: `server/install_dashboard_app.py:1660`'s fix hint (`$env:DASH_RELEASE_PUBKEYS = python tools/release_key.py pubkey`). `server/tests/test_image_mode.py:258` pins it, it is outside this finding's review, and the base rig takes the load_secrets path.
  - (B) Mac signing. Step 3 gains the bullet "The Mac signs too, and nothing checks it". `release_macos.sh` and `build_onboard_macos.sh` sign with the Mac's hand-copied `~/.ccsync-release/release.key` and have no REL-7 check. At the switch, replace it on the Mac with the new key and confirm `python3 tools/release_key.py pubkey` there prints the NEW id. The bullet also says what a stale Mac key costs: accepted until step 4, then refused silently by every Mac on the drop build. I did not add a REL-7 check to the two `.sh` scripts. That is a real gap, but it is new behaviour on a path I cannot run here (no Mac). OWED below.
  - (C) The step-3 test is honest now. `test_step3_says_pathway_a_needs_the_override_once_and_when` no longer models the ps1 as a Python equality. It cuts `Get-PubkeyId` + `Test-SigningKeyTheFleetTrusts` out of `installer/build_editor_package.ps1` and runs them under PowerShell. Only `Invoke-RestMethod` (returning a packages view built from the REAL feed record), `Write-Step` and `Write-Warn2` are stubbed. It asserts three things. The overlap release signed by the old key over an old-signed current build passes ("also signed", and the ps1's id equals `release_pubkey.pubkey_id`). The first new-key build over the overlap record exits 1 with "WILL REFUSE THIS BUILD", names the old id as "signed with", and prints "Nothing was built.". The same build with `-AllowKeyRotation` returns. If the ps1's comparison changes (for example to a baked list, option (a)), this test goes red, and the doc's pathway A paragraph must change with it. The doc assertions are kept.
- Regression tests (in `tools/tests/test_bug_hunt_2026_09_24_webapps_ops.py`, round 2 section):
  - `test_the_trust_list_holds_both_keys_for_the_whole_rotation`: `trusted` gives [old] before, [old, new] in steps 1-2, [new, old] after the switch, and [new] once the superseded key is moved off.
  - `test_an_unreadable_rotation_key_refuses_rather_than_trusting_one`.
  - `test_load_secrets_exports_the_whole_trust_list`: runs load_secrets.ps1's own DASH_RELEASE_PUBKEYS block, cut out of the file, under PowerShell with USERPROFILE/CCSYNC_RELEASE_KEY pointed at a tmp ring holding a side key. Both ids must be in the exported value.
  - `test_the_runbook_covers_the_trust_list_and_the_mac_key`: step 2 / step 3 / step 4 text, tied to the code. It checks that load_secrets really calls `trusted --quiet` and that both Mac scripts really read `~/.ccsync-release/release.key`.
- Fails on HEAD: RAN. I made a `git archive HEAD tools docs companion/src installer` copy in the scratchpad and ran the new test file against it: 11 failed, 1 passed (the guard `test_a_first_key_still_gets_the_bake_prompt`). The four new tests fail for the intended reasons: `invalid choice: 'trusted'`, `SystemExit(2)`, load_secrets exporting ONE key (`LOADED=DASH_RELEASE_PUBKEYS (derived)`), and the runbook sections missing. The rewritten step-3 test's PowerShell half PASSES against HEAD's ps1, as it should: the comparison is unchanged at HEAD, only its text changed. It fails there on the doc half.
- Tests run: `cd tools; ..\dashboard\.venv\Scripts\python.exe -m pytest tests/test_bug_hunt_2026_09_24_webapps_ops.py tests/test_release_key.py tests/test_publish_feed.py tests/test_publish_latest.py tests/test_sign_release.py tests/test_release_scripts.py -q` gave 214 passed. `tests/test_build_dashboard_bundle.py tests/test_publish_package.py tests/test_release_notes_flag.py` (the other users of release_key) gave 37 passed. The PowerShell-driven tests ran and did not skip (`-v -rs`).
- Skew: operator tooling and docs only. `DASH_RELEASE_PUBKEYS` was always a comma/whitespace list on every reader (`settings.py:891`, `select_code_root.py:188`), so a two-key value needs no dashboard change. Outside a rotation, `trusted` gives exactly what `pubkey --quiet` gave.
- OWED (round 2):
  - A REL-7-style check in `tools/release_macos.sh` / `tools/build_onboard_macos.sh` (compare the Mac's key id with the current macOS record's signer, as the ps1 does). For now the runbook's step-3 Mac bullet is the only guard.
  - Unchanged from round 1: option (a), baked ids on the dashboard record, removes pathway A's one override.

## ui-dash-admin-1 - A project name with an apostrophe breaks the ARCHIVE confirm (a crafted folder name can run script)
- Mechanism (as you traced it): as reported. `admin_assignments.html` put `{{ p.label }}` inside a single-quoted JS string in `onsubmit`. Autoescape's `&#39;` is decoded before the handler compiles, so `Editor's Cut` is a SyntaxError: the handler is null and the form submits with no confirm. A crafted name runs as script in the admin's session.
- Fix:
  - `dashboard/templates/admin_assignments.html` (the ARCHIVE form, about lines 165-173): the label moves to `data-archive-label="{{ p.label }}"`, where it is HTML-escaped and safe, and the handler reads `this.dataset.archiveLabel`. `tick_count` is forced through `| int`.
  - Sweep: I checked every `on*=` / `hx-on*=` attribute, every inline `<script>`, and every `hx-headers`/`hx-vals` in `dashboard/templates/**` (the `ytdl/web` UI is a static SPA with no server-rendered templates). One more instance was the same shape and is fixed. `partials/project_detail.html`, the MOVE form's `hx-on::confirm`, had `{{ project.label }}` inside a `&quot;`-quoted JS string, so a label with `"` or `\` broke the confirm or injected script. The label is now read from `data-project-label` via `f.dataset.projectLabel`, and `{{ project.editors | length | int }}` stays numeric.
  - Checked and left alone:
    - `partials/topbar.html:160`, the sign-out-everywhere confirm: static text, no value.
    - `partials/editor_switcher.html:11` `onchange`: no value.
    - `base.html:61`, the inline `<script>`: no Jinja.
    - `base.html:128` `hx-headers` JSON: carries only `csrf_token`, an HMAC hexdigest (auth.py:698), whose alphabet cannot break out.
    - Every `hx-confirm="... {{ x }} ..."` (admin_users, admin_jobs, admin_packages, admin_report_tokens, admin_sessions, admin_suspend_button, fleet_grid, my_queue, project_detail, recovery, sidebar): htmx hands these to `confirm()` as DATA, never compiled as JS, so HTML escaping is the right and sufficient encoding there.
    - `cards_landing.*`: excluded from my group; the sweep test below scans it too, and it passes.
- Regression test: `dashboard/tests/test_bug_hunt_2026_09_24_webapps_ops.py`:
  - `test_the_archive_confirm_never_carries_the_label_in_its_source` and `test_the_move_confirm_never_carries_the_label_in_its_source` render the real pages through the app with a label containing `'`, `"`, `\` and an injection payload. Each HTML-unescapes the handler the way the browser does, and asserts the label is not in the JS source and round-trips through the data attribute.
  - `test_the_archive_confirm_compiles_and_asks_about_the_real_name` and the MOVE test also compile and run the handler under node (`new Function`) with a stub `confirm()`. They assert it compiles, asks about the exact name, and never sets the payload's flag. They skip only when node is absent; node v24 was present here.
  - `test_no_template_splices_a_string_value_into_an_inline_handler` pins the sweep: any `{{ }}` in an `on*`/`hx-on*` attribute of any template must be `| int`. Jinja comments are stripped first, because one inside the MOVE handler holds a raw `"`.
  - On HEAD all four fail. The archive handler source contained `Archive 2026/Editor's "Cut" \ x');window.__pwned=1;('`. The sweep listed `onsubmit {{ p.label }}`, `{{ tick_count }}`, `hx-on::confirm {{ project.label }}` and `{{ project.editors | length }}`. I ran them against the HEAD templates swapped in place and then restored.
- Other tests run: `cd dashboard; .venv\Scripts\python.exe -m pytest tests/test_admin_assignments.py tests/test_file_moves.py tests/test_sessions.py tests/test_mobile_admin.py tests/test_no_em_dash.py tests/test_templates_wave3_2026_09_04.py tests/test_bug_hunt_2026_09_24_webapps_ops.py -q` -> 313 passed. `tests/test_admin_tick_for_editor.py tests/test_fleet_scope.py tests/test_bug_hunt_2026_09_18_dashboard_lows.py tests/test_upload_only.py tests/test_settings_hub.py` -> 102 passed.
- Skew / deploy order: dashboard templates only (they ship with the dashboard). No wire change and no new user-visible text; both confirm sentences read exactly as before.
- OWED: none

## Round 2 (owed-installer)

### logic-release-1 (the pathway A refusal text, OWED from round 1)
- Mechanism (as traced): as the round-1 ledger and the reviewer said. `Test-SigningKeyTheFleetTrusts` in `installer/build_editor_package.ps1` compares this rig's key id with the key that SIGNED the current windows companion record (`$current.pubkey_id`); the dashboard stores no baked list. Its refusal still said "A rotation costs an overlap release: bake --add, ship THAT build ... Pass -AllowKeyRotation if this IS that deliberate step", which on pathway A (`tools\ship.cmd`) invites the override on exactly the build that must instead be signed with the OLD key.
- Fix (`installer/build_editor_package.ps1`):
  - The refusal now says: if this is a rotation's overlap release it must be signed with the OLD key (named by id), put that key back at `release.key` and ship again (docs\RELEASE.md, Rotating); the overlap release never needs an override; `-AllowKeyRotation` does not perform a rotation; its one rotation use is the FIRST build after the key switch (step 3), when the named current version is the overlap build or later and the named signer is the OLD key; anything else is a wrong key. The ledger's suggested text, split over the existing Write-Warn2 lines. No em dash anywhere (the file is pure ASCII, CRLF kept).
  - The `-AllowKeyRotation` warning (override passed) now adds that the fleet refuses the build unless it already runs one that bakes this key in, true only for the first build after the switch.
  - `.PARAMETER AllowKeyRotation` help said "the signing key is not baked into the build the fleet is currently on", which is not what the check compares; it now says "not the one that signed", and that the flag is no rotation step and the overlap release never needs it.
  - Comment at the refusal cites logic-release-1 and both dates.
- `docs/RELEASE.md`: re-read; the REL-7 bullet (lines 132-137) and Rotating steps 1 and 3 already say what the new refusal says (round 1 fixed them), so no edit.
- Regression test: new `installer/tests/Test-KeyRotationRefusal.ps1` (picked up by `run_all_tests.ps1`'s `Test-*.ps1` enumeration). It slices `Test-SigningKeyTheFleetTrusts` out of the script (the Test-DriveMapParser trick), turns its `exit 1` into a throw, stubs `Invoke-RestMethod` with a current record signed by an old key id and `Write-Warn2`/`Write-Step` with a capture, and checks: the mismatch is refused; the text says the overlap release is signed with the OLD key (by id) and never needs an override, that `-AllowKeyRotation` does not perform a rotation, names the first build after the switch, points at RELEASE.md Rotating; "deliberate step" and "ship THAT build" are gone; no U+2014; with the override it publishes and names the first-build case; the same key passes quietly.
- Fails on HEAD: RAN it with `-ScriptPath` against `git show HEAD:installer/build_editor_package.ps1` in the scratchpad: 8 failures (the seven text checks on the refusal plus the override's first-build line). On the working tree: all 15 pass.
- Also: `docs/README.md` gains the `bug-hunt-2026-09-24.md` row (after `bug-hunt-2026-09-18b.md`), so `tools/tests/test_docs_index.py` (the test lives in `tools/`, not `dashboard/`) is green: 2 passed. It failed on the working tree before, because the doc is new and unlisted.
- Tests run: `powershell -NoProfile -ExecutionPolicy Bypass -File installer\tests\Test-KeyRotationRefusal.ps1` (15/15), and against HEAD's script (8 fail); `cd tools; ..\dashboard\.venv\Scripts\python.exe -m pytest tests/test_docs_index.py -q` (2 passed).
- OWED: the proper fix is still option (a) from the round-1 review: carry `baked_pubkey_ids` into the dashboard's package record (optional on read, dashboard deployed first) and have this ps1 compare against that list as `publish_feed.py` does. Then pathway A needs no override at all, the refusal's step-3 sentence and RELEASE.md step 3's pathway A paragraph can go. That is a dashboard schema/wire change outside this group.
